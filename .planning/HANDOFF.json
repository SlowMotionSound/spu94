{
  "status": "between_steps",
  "session_date": "2026-04-24",
  "milestone": "v1.0 close-out (M1 remediation)",
  "phase": "M1 close-out — Stage 1 of ARCHITECTURAL-AUDIT.md migration plan",
  "plan": "ARCHITECTURAL-AUDIT.md Part 6 ordered migration plan",
  "current_step": "Step 12: Land witness-diff tolerance gate (Step 11 blocked on human GitHub setup)",
  "total_steps": 15,
  "completed_steps": [
    "Step 1: Fix grep-guard scope + CLI tail-seconds integer parse (commit 262930a)",
    "Step 2: Fix reverb input wiring CR-01 + goldens regen + ADR-Phase-6-I (commit 4fcad49)",
    "Audit artifacts captured (commit 92cca7c)",
    "Step 3: work-buf size contract + load_preset validation (commit 72f2270, ADR-0022)",
    "Step 4: OOB tap counter via spu94_get_error_counters (commit bce2c13, ADR-0023)",
    "Step 5: bump Python default work_buf_size to SPU94_WORK_BUF_MAX_BYTES (commit fdeeb57)",
    "Step 6: assert oob_tap_count==0 + non-silence floor in self_test + modulation harness (commit efdf9a5; modulation_report regen committed separately as ce8385e)",
    "Step 7: tighten NULL-state-on-mutation in set_reg_i16/u16 to SPU94_INVALID_STATE per ADR-0022 (commit e81b360)",
    "Step 8: CLI + Python critical and high findings closed across 6 atomic commits — C-01 24-bit WAV gate (3a6a9f5), C-02 tail-seconds parser hardening tests (0b48be8), H-03 null state on api.destroy (c329649), H-04 distinct error for over-long JSON keys (77197e3), H-05 flat-config missing-register pre-pass (fff3b73), H-06 hex-digit-required after 0x prefix + caught real strtol whitespace bug (c9154d8)"
  ],
  "in_progress_step": null,
  "uncommitted_files": [],
  "remaining_steps": [
    {"id": 11, "name": "Push to GitHub and verify CI green on GitHub Actions (FIRST real CI run; see ARCHITECTURAL-AUDIT.md Root Cause #4)"},
    {"id": 12, "name": "Land witness-diff tolerance gate (ADR + witness_diff_thresholds.json)"},
    {"id": 13, "name": "Land external-anchor test (hand-computed impulse response for a known preset)"},
    {"id": 14, "name": "Write Phase 6 VERIFICATION.md (from existing UAT + SUMMARY evidence)"},
    {"id": 15, "name": "Re-run /gsd-audit-milestone v1.0, then /gsd-complete-milestone v1.0, git tag v1.0"}
  ],
  "blockers": [
    "Step 11 is blocked: no `origin` git remote configured (the project's pyproject.toml points at https://github.com/anthonyaccurso/spu94 as the intended URL, but the repo may not exist on GitHub yet and there are no credentials in this session). `gh` CLI is also not installed on this workstation. Step 11 requires Anthony to (1) create the GitHub repo (or confirm its existence + URL), (2) `git remote add origin <url>` locally, (3) install `gh` and `gh auth login` for run monitoring, then `git push -u origin master`. Steps 12-15 do not depend on Step 11 and have been advanced first."
  ],
  "human_actions_pending": [
    "Step 11 setup: create GitHub repo, add origin remote, install gh CLI, authenticate. After that, push and confirm CI green.",
    "Step 15 git tag: `git tag -s v1.0` (signed tag) requires Anthony's GPG key on this workstation; happens at the very end of close-out."
  ],
  "next_action": "Step 12 (Step 11 advanced past as blocker): land the witness-diff tolerance gate. Read .planning/REVIEW-tests-ci.md and .planning/ARCHITECTURAL-AUDIT.md for the full motivation; the short version is that the existing witness diff test (tests/witness/) prints a number but does not enforce a per-preset tolerance — a regression that doubles the diff would still pass CI silently. Create config/witness_diff_thresholds.json with one number per preset (start liberal, tighten in M2 once we have the lv2-psx-reverb baseline calibrated), wire the existing witness test to read the JSON and assert per-preset, and add an ADR (next number after ADR-0023, so ADR-0024) recording the threshold policy. Single commit if it stays small.",
  "context_notes": [
    "The audit that drove this remediation is in .planning/ARCHITECTURAL-AUDIT.md — read it first for full context on the 4 root causes and target architecture.",
    "Three review reports in .planning/REVIEW-c-core.md, REVIEW-cli-python.md, REVIEW-tests-ci.md provide per-finding detail.",
    ".planning/v1.0-MILESTONE-AUDIT.md is the pre-remediation audit — its status was gaps_found; after Stage 1 it should flip to passed.",
    ".planning/v1.0-GOLDENS-REGEN.md documents the regen event that happened during Step 2.",
    "Phase 8 (MCU cross-compile) is parked per Anthony's 2026-04-24 decision — moves to between M4 and M5. Do NOT attempt to close Phase 8 as part of M1.",
    "Step 3 shipped as commit 72f2270 (feat(api): work-buf size contract + load_preset validation). Three new SPU94_INVALID_STATE/WORK_BUF_TOO_SMALL/INVALID_ARG result codes, SPU94_WORK_BUF_MAX_BYTES constant, spu94_preset_min_work_buf_size function. CLI + Python load_preset surface the errors. 79/79 ctest green.",
    "Step 4 shipped as commit bce2c13 (feat(observability): OOB tap counter via spu94_get_error_counters). uint64 oob_tap_count field on spu94_state; public spu94_error_counters_t + accessor; Python api.get_error_counters returns a dict. tests/unit/state/test_error_counters.c (4 sub-tests). 80/80 ctest green.",
    "Step 5 shipped as commit fdeeb57 (feat(python): default work_buf_size = SPU94_WORK_BUF_MAX_BYTES). Removes the four Step-3 bandaids that passed explicit SPU94_WORK_BUF_MAX_BYTES (test_binding_numpy_contract.py + modulation_harness.py). 80/80 ctest green.",
    "Step 6 shipped as commit efdf9a5 (test(observability): assert oob_tap_count==0 + non-silence floor). api.self_test() now requires >=100 non-zero output samples and oob_tap_count==0; modulation harness ModulationResult grows an oob_tap_count field; test_stability asserts ==0 across all 105 cases. 80/80 ctest green. modulation_report.json regen was committed separately as ce8385e (chore — was generated by Step 6 but not staged).",
    "Step 7 shipped as commit e81b360 (feat(api): tighten NULL-state-on-mutation in set_reg_i16/u16 to SPU94_INVALID_STATE). Two engine-layer setters' state==NULL branches changed from SPU94_UNKNOWN_REG to SPU94_INVALID_STATE per ADR-0022; test renamed; spu94_registers.h doc updated. 80/80 ctest green.",
    "Step 8 closed all critical+high CLI/Python review findings across 6 atomic sub-commits (C-01..H-06). H-06 testing exposed a real strtol-whitespace bug the review's analysis missed; closed alongside. 80/80 ctest green throughout. Mediums and lows (M-01..M-07, L-01..L-05, N-01..N-04) deferred to a post-M1 cleanup pass per the review's own 'recommended close-out blocker vs ship-with-known-issues' triage.",
    "Step 11 mechanics: there's a GitHub Actions workflow already wired (see .github/workflows/). If `gh` is not authenticated in this session, the user will need to do `gh auth login` interactively — surface that as a blocker rather than guessing credentials. Watch for the FIRST cold cache build to take ~10 minutes (cibuildwheel job dominates wall time)."
  ],
  "handoff_markdown": ".planning/HANDOFF.md"
}
