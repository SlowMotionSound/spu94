---
phase: 38-integration-cross-feature-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/voice/test_voice_tick.c
autonomous: true
requirements: [INT-01, INT-02]

must_haves:
  truths:
    - "Processing order test proves sweep runs before ADPCM decode"
    - "Processing order test proves PMON pitch modify happens before voice_tick"
    - "Processing order test proves noise ticks globally once before the voice loop"
    - "Processing order test proves VxOUTX is stored post-ADSR, pre-volume"
    - "NON voice output feeds PMON factor for the next voice, producing measurable pitch variation"
  artifacts:
    - path: "tests/unit/voice/test_voice_tick.c"
      provides: "Integration tests for processing order proof and PMON+NON cross-feature"
      contains: "test_int01_processing_order"
  key_links:
    - from: "tests/unit/voice/test_voice_tick.c"
      to: "src/spu94/spu94_voice.c"
      via: "spu94_voice_mixer_tick"
      pattern: "spu94_voice_mixer_tick"
---

<objective>
Write integration tests that prove the mixer tick processing order matches PS1 hardware
(INT-01) and that NON+PMON cross-feature interaction works correctly (INT-02).

Purpose: These tests lock in the correct processing order as a regression gate and verify
that noise-driven pitch modulation -- an emergent cross-feature behavior -- is not blocked.

Output: New test functions appended to test_voice_tick.c, all passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/unit/voice/test_voice_tick.c
@src/spu94/spu94_voice.c
@include/spu94/spu94_voice.h
@include/spu94/spu94_sweep.h
@include/spu94/spu94_noise.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write INT-01 processing order proof test</name>
  <files>tests/unit/voice/test_voice_tick.c</files>
  <read_first>
    src/spu94/spu94_voice.c (lines 98-323 for voice_tick, lines 532-643 for mixer_tick)
    tests/unit/voice/test_voice_tick.c (lines 2079-2143 for main() and existing test registrations)
    include/spu94/spu94_voice.h
  </read_first>
  <action>
    Add a test function test_int01_processing_order_sweep_before_decode that proves sweep
    modifies vol_l/vol_r BEFORE the volume multiply step uses them. Setup: configure a voice
    with sweep_l active (linear increase, shift=0, step=0 for max rate), initial vol_l=0x100.
    Run one mixer tick. The output should reflect the SWEPT volume (higher than 0x100), not
    the initial 0x100. Compare against a control run with sweep inactive at the same vol_l.
    The swept output must differ from the unswerpt output, proving sweep ran first.

    Add a test function test_int01_processing_order_pmon_before_decode that proves PMON pitch
    modification happens before the voice_tick call (and thus before ADPCM decode uses the
    pitch). Setup: voice 0 as loud modulator with ADSR at max sustain, voice 1 with PMON
    enabled. After N ticks, voice 1's current_addr must differ from a control run without
    PMON -- proving PMON altered the pitch BEFORE the counter advanced. This is structurally
    similar to test_pmon_formula_positive_modulator but frames the assertion as order-proof.

    Add a test function test_int01_processing_order_noise_global_before_voices that proves the
    noise generator ticks exactly once before any voice processes. Setup: 2 NON-enabled voices,
    noise_gen at a known state with shift=15, step=7 (fast shift rate). After one mixer tick,
    both voices must have identical outx values matching the noise_gen.level that existed at
    the start of the voice loop (not different values from per-voice noise ticks).

    Add a test function test_int01_outx_post_adsr_pre_volume that proves outx captures the
    signal after ADSR but before volume scaling. Setup: voice with ADSR enabled at a known
    sustain level (e.g., 0x4000), vol_l=0x2000. After stabilization, outx must equal
    gauss_out * adsr_level (independent of vol_l). A second run with vol_l=0x7FFF but same
    ADSR must produce identical outx. This is similar to test_outx_stored_post_adsr_pre_volume
    but framed as processing-order verification.

    Register all four new tests in main() under a "Phase 38: Integration" comment block.
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb/build-test && cmake --build . --target test_voice_tick 2>&1 && ./tests/unit/voice/test_voice_tick 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test_int01_processing_order_sweep_before_decode exists and passes
    - test_int01_processing_order_pmon_before_decode exists and passes
    - test_int01_processing_order_noise_global_before_voices exists and passes
    - test_int01_outx_post_adsr_pre_volume exists and passes
    - Total test count is 55 (51 existing + 4 new), 0 failures
    - All 4 tests are registered in main() under "Phase 38: Integration" comment
  </acceptance_criteria>
  <done>Four processing-order proof tests pass, covering sweep-before-decode, PMON-before-voice_tick, noise-global-before-loop, and outx-post-ADSR-pre-volume</done>
</task>

<task type="auto">
  <name>Task 2: Write INT-02 PMON+NON cross-feature interaction test</name>
  <files>tests/unit/voice/test_voice_tick.c</files>
  <read_first>
    src/spu94/spu94_voice.c (lines 574-631 for mixer_tick voice loop with PMON and NON)
    tests/unit/voice/test_voice_tick.c (the test_pmon_* and test_non_* sections)
  </read_first>
  <action>
    Add a test function test_int02_non_voice_feeds_pmon that proves a NON-enabled voice's
    output feeds the PMON factor for the next voice, producing measurable pitch variation.

    Setup: voice 0 with NON enabled (outputs noise), voice 1 with PMON enabled (pitch
    modulated by voice 0's outx). Set noise_gen to a known state that produces a nonzero
    level. Voice 1 should have its pitch modulated by the noise level. Run N ticks and
    compare voice 1's current_addr against a control run where voice 0 is NOT NON-enabled
    (playing a silent ADPCM sample producing outx=0, which gives Factor=0x8000 unity).

    The NON run should produce a DIFFERENT current_addr for voice 1 than the control,
    proving that noise output fed through PMON to alter pitch. The noise-PMON combination
    is spec-orthogonal (features compose correctly even though the PS1 spec never explicitly
    describes this interaction).

    Add a second test function test_int02_non_pmon_random_pitch_jitter that demonstrates
    the "random pitch jitter" character. Setup: voice 0 NON-enabled with noise shifting
    rapidly (shift=15, step=7). Voice 1 PMON-enabled. Run 50 ticks collecting voice 1's
    per-tick effective pitch step (measure pitch_counter delta between ticks). Assert that
    at least 2 distinct step values appear across the 50 ticks -- proving the noise
    modulation creates pitch variation (jitter), not a constant offset.

    Register both new tests in main() under the "Phase 38: Integration" comment block
    (after the INT-01 tests).
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb/build-test && cmake --build . --target test_voice_tick 2>&1 && ./tests/unit/voice/test_voice_tick 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test_int02_non_voice_feeds_pmon exists and passes
    - test_int02_non_pmon_random_pitch_jitter exists and passes
    - Total test count is 57 (51 existing + 4 INT-01 + 2 INT-02), 0 failures
    - Both tests are registered in main() under "Phase 38: Integration" comment
    - test_int02_non_pmon_random_pitch_jitter asserts >= 2 distinct pitch step values across 50 ticks
  </acceptance_criteria>
  <done>Two PMON+NON cross-feature tests pass, proving noise-driven pitch modulation works and produces measurable pitch jitter</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness -> C core | Tests call spu94_voice_mixer_tick with crafted inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | test inputs | accept | Tests use controlled inputs; no external data |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
cd /home/ubuntu-studio/Desktop/PSX Reverb/build-test && cmake --build . --target test_voice_tick && ./tests/unit/voice/test_voice_tick
Expected: 57 Tests 0 Failures 0 Ignored
</verification>

<success_criteria>
- 6 new integration tests added to test_voice_tick.c (4 for INT-01, 2 for INT-02)
- All 57 tests pass (51 existing + 6 new)
- Processing order is proven by observable test assertions, not documentation
- PMON+NON cross-feature interaction is verified as spec-orthogonal (not blocked)
</success_criteria>

<output>
Create `.planning/phases/38-integration-cross-feature-verification/38-01-SUMMARY.md` when done
</output>
