---
phase: 45-auto-pan
plan: 01
type: execute
wave: 1
depends_on: [44-01]
files_modified:
  - src/plugin/PluginProcessor.h
  - src/plugin/PluginProcessor.cpp
  - tests/unit/voice/test_sweep.c
autonomous: true
requirements: [PAN-01, PAN-02, PAN-03, PAN-04, PAN-05]

must_haves:
  truths:
    - "A voice with auto-pan enabled audibly moves between left and right speakers"
    - "At 100% depth the sound reaches full left and full right"
    - "The PS1-faithful linear crossfade creates an audible volume dip at center"
    - "Asymmetric L/R rates produce evolving non-repeating stereo patterns"
    - "Auto-pan and tremolo cannot both be active simultaneously"
  artifacts:
    - path: "src/plugin/PluginProcessor.h"
      provides: "Auto-pan atomics (enabled, speed, depth, ratio) + audio-thread state vars"
      contains: "autoPanEnabled"
    - path: "src/plugin/PluginProcessor.cpp"
      provides: "Auto-pan activation block in processBlock with opposition-phase L/R sweep config"
      contains: "autoPanEnabled"
    - path: "tests/unit/voice/test_sweep.c"
      provides: "Integration test proving L/R opposition and linear crossfade dip"
      contains: "auto_pan"
  key_links:
    - from: "src/plugin/PluginProcessor.cpp"
      to: "spu94_voice_mixer_set_sweep_l/r"
      via: "processBlock auto-pan activation block"
      pattern: "direction.*0.*direction.*1"
---

<objective>
Auto-pan DSP activation: opposition-phase L/R retrigger sweep producing stereo movement.

Purpose: Create the stereo panning effect by configuring L and R sweeps to run in opposite directions -- L decreases while R increases, then both reverse on retrigger. This uses the same hardware mechanism as tremolo but with opposite direction values.

Output: Auto-pan activation block in processBlock, atomics for GUI control, integration test proving opposition behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-tremolo/44-01-SUMMARY.md

@src/plugin/PluginProcessor.h
@src/plugin/PluginProcessor.cpp
@tests/unit/voice/test_sweep.c
@include/spu94/spu94_voice.h
</context>

<interfaces>
From src/plugin/PluginProcessor.h (tremolo pattern to mirror):
```cpp
// Tremolo controls (Phase 44: continuous VCA oscillation via retrigger)
std::atomic<bool>&  getTremoloEnabled()  { return tremoloEnabled; }
std::atomic<float>& getTremoloSpeedHz()  { return tremoloSpeedHz; }
std::atomic<float>& getTremoloDepth()    { return tremoloDepth; }
std::atomic<int>&   getTremoloCurve()    { return tremoloCurve; }
std::atomic<float>& getTremoloRatio()    { return tremoloRatio; }
```

From include/spu94/spu94_voice.h:
```c
spu94_result_t spu94_voice_mixer_set_sweep_l(spu94_voice_mixer_t *m, int voice_idx,
    uint8_t mode, uint8_t direction, uint8_t phase,
    uint8_t shift, uint8_t step, uint8_t retrigger_enable);
// mode: 0=linear, 1=exponential
// direction: 0=increase, 1=decrease
// phase: 0=positive, 1=negative
// retrigger_enable: 1=auto-reverse at limits
```

Key difference from tremolo: tremolo uses direction=1 for BOTH L and R (both decrease together). Auto-pan uses direction=1 for L and direction=0 for R (opposition: L decreases while R increases, then both reverse on retrigger boundary).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Auto-pan atomics, activation logic, and mutual exclusion</name>
  <files>src/plugin/PluginProcessor.h, src/plugin/PluginProcessor.cpp</files>
  <read_first>
    - src/plugin/PluginProcessor.h lines 88-94 (tremolo getter pattern)
    - src/plugin/PluginProcessor.h lines 415-427 (tremolo private members + audio-thread state)
    - src/plugin/PluginProcessor.cpp lines 752-868 (tremolo activation block — the pattern to clone)
    - src/plugin/PluginProcessor.cpp line 96 (hzToShift helper — reuse, do not duplicate)
  </read_first>
  <action>
    Add auto-pan control surface to PluginProcessor, mirroring the tremolo pattern with opposite L/R directions.

    In PluginProcessor.h:
    - Add public getter methods immediately after the tremolo getters (line ~94):
      getAutoPanEnabled() returning autoPanEnabled (std::atomic bool)
      getAutoPanSpeedHz() returning autoPanSpeedHz (std::atomic float, default 2.0f — slower default than tremolo since panning is more noticeable)
      getAutoPanDepth() returning autoPanDepth (std::atomic float, default 1.0f — full range)
      getAutoPanRatio() returning autoPanRatio (std::atomic float, default 1.0f — symmetric)
    - Add private members immediately after the tremolo privates (after line ~427):
      autoPanEnabled (std::atomic bool, default false)
      autoPanSpeedHz (std::atomic float, default 2.0f)
      autoPanDepth (std::atomic float, default 1.0f)
      autoPanRatio (std::atomic float, default 1.0f)
      Audio-thread-only state: autoPanWasActive (bool, false), lastAutoPanHz (float, -1.0f), lastAutoPanRatio (float, -1.0f)
    - NO curve control for auto-pan — PAN-04 mandates linear-only crossfade (PS1-faithful). The mode parameter is always 0 (linear).

    In PluginProcessor.cpp, after the tremolo activation block (line ~869), add the auto-pan activation block:
    - Structure is identical to tremolo but with THREE critical differences:
      1. L channel: direction=1 (decrease), R channel: direction=0 (increase) — OPPOSITION
      2. mode is always 0 (linear) — no curve selection, PAN-04 mandates linear crossfade
      3. No curve tracking state needed (always linear)

    Mutual exclusion logic (all three effects share the same sweep hardware):
    - The VCA ramp ARM check (line ~730) must also be gated by autoPanEnabled (skip ARM when auto-pan active)
    - The tremolo activation block must be gated: skip if autoPanEnabled is true
    - The auto-pan activation block must be gated: skip if tremoloEnabled is true
    - Priority order: tremolo wins over auto-pan if both somehow enabled simultaneously (defensive)

    Auto-pan activation block structure:
    - if (panEn and not autoPanWasActive): configure L sweep direction=1, R sweep direction=0, both retrigger_enable=1, mode=0 (linear). Apply ratio to R channel shift (same octave-offset logic as tremolo).
    - else if (panEn and autoPanWasActive): check if hz or ratio changed, reconfigure if so.
    - else if (not panEn and autoPanWasActive): deactivate sweeps, reset state.

    Depth scaling for auto-pan:
    - Same formula as tremolo depth but applied per-channel with the opposite direction already baked into the sweep.
    - The depth controls how far from center the pan goes. At depth=1.0, vol ranges 0..0x7FFF (full L-to-R). At depth=0.5, vol ranges 0x3FFF..0x7FFF (half the excursion — sound stays closer to center).
    - Apply after the auto-pan activation block, same pattern as tremolo depth scaling.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && cmake --build build --config Release --target spu94_test_sweep 2>&1 | tail -5 && ./build/tests/unit/voice/spu94_test_sweep 2>&1 | tail -10</automated>
  </verify>
  <done>Auto-pan atomics exist, processBlock configures L/R sweeps in opposition when enabled, mutual exclusion prevents tremolo+auto-pan+VCA ramp conflicts, existing sweep tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integration test proving opposition-phase and linear crossfade dip</name>
  <files>tests/unit/voice/test_sweep.c</files>
  <read_first>
    - tests/unit/voice/test_sweep.c (find existing tremolo tests: test_tremolo_full_oscillation, test_tremolo_exponential_asymmetry — use as template)
  </read_first>
  <action>
    Add two integration tests to test_sweep.c proving auto-pan behavior:

    Test 1: test_auto_pan_opposition
    - Configure voice 0 with vol_l=0x7FFF, vol_r=0x7FFF
    - Set sweep_l with direction=1 (decrease), retrigger_enable=1, mode=0 (linear), shift=10, step=3
    - Set sweep_r with direction=0 (increase), retrigger_enable=1, mode=0 (linear), shift=10, step=3
    - Tick for enough cycles to observe one half-period
    - Assert: when sweep_l.level has DECREASED from 0x7FFF, sweep_r.level has INCREASED toward 0x7FFF (or stayed near max if it started there)
    - More precisely: after N ticks, assert sweep_l.level < 0x7FFF AND sweep_r.level >= initial_r_level (levels move in opposite directions)
    - This proves PAN-01: L and R in opposition

    Test 2: test_auto_pan_linear_crossfade_dip
    - Configure as above with linear mode (mode=0) on both channels
    - Tick through a half-period until L reaches minimum
    - Sample vol_l and vol_r at the midpoint (where levels cross)
    - Assert: at the crosspoint, BOTH levels are below 0x7FFF (the center dip). Specifically, the sum of L+R at the crosspoint should be less than 2*0x7FFF — this proves the linear crossfade volume dip at center (PAN-04).
    - This is the PS1-faithful character: unlike equal-power panning where L^2 + R^2 = constant, linear panning has L + R dipping below unity at center.

    Register both tests in the test runner (follow existing pattern in the file).
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && cmake --build build --config Release --target spu94_test_sweep 2>&1 | tail -5 && ./build/tests/unit/voice/spu94_test_sweep 2>&1 | grep -E "PASS|FAIL|auto_pan"</automated>
  </verify>
  <done>Two new tests pass: opposition-phase L/R sweep proves PAN-01, linear crossfade dip proves PAN-04. Total sweep test count increases by 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI thread -> audio thread | Atomic stores from GUI knobs consumed in processBlock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | autoPanSpeedHz atomic | mitigate | Clamp Hz to 0.5-43.0 range at point of use in processBlock (same as tremolo) |
| T-45-02 | Denial of Service | Mutual exclusion race | accept | Both effects read atomics independently; worst case is one audio block with stale config — no crash, self-corrects next block |
</threat_model>

<verification>
1. Full project builds clean: `cmake --build build --config Release`
2. All sweep tests pass: `./build/tests/unit/voice/spu94_test_sweep`
3. All voice_tick tests pass: `./build/tests/unit/voice/spu94_test_voice_tick`
4. Grep confirms opposition directions: `grep -A5 "auto.pan" src/plugin/PluginProcessor.cpp | grep "direction"`
</verification>

<success_criteria>
- Auto-pan activation block exists in processBlock with L direction=1, R direction=0 (opposition)
- Depth scaling applied per-channel after sweep tick
- Mutual exclusion: VCA ARM gated, tremolo gated, auto-pan gated against each other
- 2 new integration tests prove opposition and linear crossfade dip
- All existing tests continue to pass (no regression)
</success_criteria>

<output>
Create `.planning/phases/45-auto-pan/45-01-SUMMARY.md` when done
</output>
