---
phase: 09-verification-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/regenerate_goldens.py
  - tests/conformance/test_goldens_present.py
  - tests/golden/dac_isolated/impulse.wav
  - tests/golden/dac_isolated/white_noise.wav
  - tests/golden/dac_isolated/sine_1khz.wav
  - tests/golden/dac_isolated/silence.wav
  - tests/golden/dac_isolated/sweep.wav
autonomous: true
requirements:
  - DAC-TEST-01

must_haves:
  truths:
    - "55 DAC golden WAVs exist (50 full-pipeline + 5 isolated) with SHA-256 sidecars"
    - "Regeneration script reproduces identical DAC goldens on re-run"
    - "Conformance test validates entire 135-golden corpus (50 reverb + 30 ADPCM + 55 DAC)"
  artifacts:
    - path: "scripts/regenerate_goldens.py"
      provides: "--dac and --check-dac and --dac-isolated and --check-dac-isolated modes"
      contains: "render_dac_golden"
    - path: "tests/conformance/test_goldens_present.py"
      provides: "DAC corpus parametrized structural test"
      contains: "test_dac_wav_exists"
    - path: "tests/golden/hall/dac/impulse.wav"
      provides: "Example full-pipeline DAC golden"
    - path: "tests/golden/dac_isolated/impulse.wav"
      provides: "Example isolated DAC-only golden"
  key_links:
    - from: "scripts/regenerate_goldens.py"
      to: "spu94 reverb --dac"
      via: "subprocess call with --dac flag"
      pattern: "--dac"
    - from: "tests/conformance/test_goldens_present.py"
      to: "tests/golden/*/dac/*.wav"
      via: "parametrized pytest glob"
      pattern: "DAC_INPUTS"
---

<objective>
Extend the golden file infrastructure with DAC-enabled golden WAVs per D-01 through D-05.

Purpose: Lock down the DAC model's exact bit-level fingerprint before any future oversampling rework. These 55 WAVs + SHA-256 sidecars provide regression detection for any drift in the FIR filter or noise shaping.

Output: 55 DAC golden WAVs (50 full-pipeline in `tests/golden/<preset>/dac/`, 5 isolated in `tests/golden/dac_isolated/`), extended regeneration script, extended conformance test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-verification-documentation/09-CONTEXT.md

<interfaces>
<!-- Key patterns the executor needs from existing golden infrastructure -->

From scripts/regenerate_goldens.py:
- PRESETS: 10-element list ["off", "room", "studio_a", ...]
- INPUTS: 5-element list ["impulse", "white_noise", "sine_1khz", "silence", "sweep"]
- ADPCM_INPUTS: 3-element list ["impulse", "sine_1khz", "chirp"]
- render_golden(preset, input_name, out_path, tmp_in_path, spu94_bin) -- shells out to `spu94 --preset <preset> <in> <out>`
- render_adpcm_golden(...) -- shells out to `spu94 reverb --adpcm --preset <preset> <in> <out>`
- _generate_loop(presets, inputs, golden_root, render_fn, tmp_in, spu94_bin, label="")
- _check_loop(presets, inputs, golden_root, render_fn, tmpd, tmp_in, spu94_bin, failures, label="")
- sha256_of(path) -> str

From tests/conformance/test_goldens_present.py:
- Parametrized pytest tests: test_wav_exists, test_sidecar_exists_and_format, test_sidecar_matches_wav
- ADPCM pattern: test_adpcm_wav_exists, test_adpcm_sidecar_exists_and_format, test_adpcm_sidecar_matches_wav
- test_expected_count() -- asserts exactly 80 .wav + 80 .sha256

From CLI:
- `spu94 reverb --dac --preset <preset> <input.wav> <output.wav>` -- full-pipeline DAC golden
- `spu94 reverb --dac <input.wav> <output.wav>` -- no preset = Off preset, DAC-only processing for isolated goldens
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend regenerate_goldens.py with --dac and --dac-isolated modes</name>
  <files>scripts/regenerate_goldens.py</files>
  <read_first>
    - scripts/regenerate_goldens.py
    - src/cli/cmd_reverb.c (lines containing --dac for CLI flag syntax)
    - .planning/phases/09-verification-documentation/09-CONTEXT.md
  </read_first>
  <action>
Add DAC golden generation and verification to the existing regenerate_goldens.py script, following the exact ADPCM extension pattern:

1. Add `DAC_INPUTS` constant identical to `INPUTS` (all 5: impulse, white_noise, sine_1khz, silence, sweep) per D-02.

2. Add `render_dac_golden(preset, input_name, out_path, tmp_in_path, spu94_bin)` function. Identical to `render_adpcm_golden` except the subprocess args use `--dac` instead of `--adpcm`. Full command: `[spu94_bin, "reverb", "--dac", "--preset", preset, str(tmp_in_path), str(out_path)]`. Per D-03, these are full-pipeline goldens.

3. Add `render_dac_isolated(input_name, out_path, tmp_in_path, spu94_bin)` function for D-04's isolated DAC-only WAVs. No preset flag — just `[spu94_bin, "reverb", "--dac", str(tmp_in_path), str(out_path)]`. This runs the Off preset (no reverb) with DAC enabled, capturing the pure DAC model fingerprint.

4. Add four argparse flags:
   - `--dac` — regenerate 50 full-pipeline DAC goldens (10 presets x 5 inputs) in `tests/golden/<preset>/dac/<input>.wav`
   - `--check-dac` — verify committed DAC sidecars by re-rendering
   - `--dac-isolated` — regenerate 5 isolated DAC-only goldens in `tests/golden/dac_isolated/<input>.wav`
   - `--check-dac-isolated` — verify committed isolated sidecars

5. Add generation/check logic in `main()` following the existing reverb/ADPCM pattern:
   - `do_dac_gen = args.dac and not args.check_dac`
   - `do_dac_check = args.check_dac`
   - `do_dac_isolated_gen = args.dac_isolated and not args.check_dac_isolated`
   - `do_dac_isolated_check = args.check_dac_isolated`

6. For full-pipeline DAC goldens, call `_generate_loop(PRESETS, DAC_INPUTS, GOLDEN_ROOT, render_dac_golden, tmp_in, spu94_bin, label="dac")` and `_check_loop(...)` respectively.

7. For isolated goldens, write a small custom loop (no preset iteration — just 5 inputs into `GOLDEN_ROOT / "dac_isolated"`). Generate with `render_dac_isolated`, produce `.wav` + `.sha256` sidecar using `sha256_of()`.

8. Update the module docstring to document the new modes and the D-05 total: 55 DAC WAVs (50 full-pipeline + 5 isolated).

9. Update the summary output at the end of `main()` to report DAC golden counts.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && python3 scripts/regenerate_goldens.py --dac --dac-isolated && find tests/golden -path "*/dac/*.wav" | wc -l && find tests/golden/dac_isolated -name "*.wav" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - scripts/regenerate_goldens.py contains `def render_dac_golden(`
    - scripts/regenerate_goldens.py contains `def render_dac_isolated(`
    - scripts/regenerate_goldens.py contains `DAC_INPUTS`
    - scripts/regenerate_goldens.py contains `--dac` in argparse
    - scripts/regenerate_goldens.py contains `--check-dac` in argparse
    - scripts/regenerate_goldens.py contains `--dac-isolated` in argparse
    - scripts/regenerate_goldens.py contains `--check-dac-isolated` in argparse
    - 50 .wav files exist under tests/golden/*/dac/ directories
    - 50 .sha256 files exist under tests/golden/*/dac/ directories
    - 5 .wav files exist under tests/golden/dac_isolated/
    - 5 .sha256 files exist under tests/golden/dac_isolated/
    - `python3 scripts/regenerate_goldens.py --check-dac` exits 0
    - `python3 scripts/regenerate_goldens.py --check-dac-isolated` exits 0
  </acceptance_criteria>
  <done>55 DAC golden WAVs + 55 SHA-256 sidecars generated. --check-dac and --check-dac-isolated both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extend test_goldens_present.py for DAC corpus validation</name>
  <files>tests/conformance/test_goldens_present.py</files>
  <read_first>
    - tests/conformance/test_goldens_present.py
    - scripts/regenerate_goldens.py (after Task 1 edits, to sync allowlists)
  </read_first>
  <action>
Extend the conformance test to validate the 55 DAC goldens, following the exact ADPCM section pattern:

1. Add `DAC_INPUTS = ["impulse", "white_noise", "sine_1khz", "silence", "sweep"]` constant (mirrors the 5 standard inputs, per D-02).

2. Add DAC full-pipeline test section (after ADPCM section):
   - `test_dac_wav_exists(preset, input_name)` — parametrized across PRESETS x DAC_INPUTS, checks `ROOT / preset / "dac" / f"{input_name}.wav"` exists
   - `test_dac_sidecar_exists_and_format(preset, input_name)` — parametrized, checks `.wav.sha256` exists and matches `^[0-9a-f]{64}\s+\S+\s*$`
   - `test_dac_sidecar_matches_wav(preset, input_name)` — spot-check parametrized with `[("hall", "impulse"), ("room", "sine_1khz"), ("studio_a", "sweep")]`, re-hashes .wav and compares to sidecar

3. Add DAC isolated test section:
   - `test_dac_isolated_wav_exists(input_name)` — parametrized across DAC_INPUTS, checks `ROOT / "dac_isolated" / f"{input_name}.wav"` exists
   - `test_dac_isolated_sidecar_exists_and_format(input_name)` — parametrized, checks sidecar format
   - `test_dac_isolated_sidecar_matches_wav(input_name)` — spot-check with `[("impulse",), ("sweep",)]`, re-hashes and compares

4. Update `test_expected_count()`: change from 80 to 135 for both .wav and .sha256 counts (50 reverb + 30 ADPCM + 50 DAC full-pipeline + 5 DAC isolated = 135).

5. Update module docstring to document the 135-golden corpus (50 reverb + 30 ADPCM + 55 DAC).
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && python3 -m pytest tests/conformance/test_goldens_present.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/conformance/test_goldens_present.py contains `DAC_INPUTS`
    - tests/conformance/test_goldens_present.py contains `def test_dac_wav_exists(`
    - tests/conformance/test_goldens_present.py contains `def test_dac_sidecar_exists_and_format(`
    - tests/conformance/test_goldens_present.py contains `def test_dac_sidecar_matches_wav(`
    - tests/conformance/test_goldens_present.py contains `def test_dac_isolated_wav_exists(`
    - tests/conformance/test_goldens_present.py contains `def test_dac_isolated_sidecar_exists_and_format(`
    - tests/conformance/test_goldens_present.py contains `== 135`
    - pytest tests/conformance/test_goldens_present.py exits 0 with all tests passing
  </acceptance_criteria>
  <done>Conformance test validates entire 135-golden corpus. All parametrized tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI subprocess | regenerate_goldens.py shells out to spu94 binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | T (Tampering) | Golden WAV files | mitigate | SHA-256 sidecars detect any modification; --check modes verify integrity |
| T-09-02 | T (Tampering) | Path traversal in golden paths | accept | Existing mitigation: closed allowlist constants (PRESETS, INPUTS), no user input reaches Path() construction |
</threat_model>

<verification>
- `python3 scripts/regenerate_goldens.py --check-dac` exits 0
- `python3 scripts/regenerate_goldens.py --check-dac-isolated` exits 0
- `python3 -m pytest tests/conformance/test_goldens_present.py -v` all pass
- `find tests/golden -name "*.wav" | wc -l` returns 135
- `find tests/golden -name "*.sha256" | wc -l` returns 135
</verification>

<success_criteria>
55 DAC golden WAVs with SHA-256 sidecars committed. Regeneration produces bit-identical output. Conformance test validates the complete 135-golden corpus.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-documentation/09-01-SUMMARY.md`
</output>
