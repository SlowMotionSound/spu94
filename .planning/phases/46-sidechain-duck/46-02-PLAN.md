---
phase: 46-sidechain-duck
plan: 02
type: execute
wave: 2
depends_on: [46-01]
files_modified:
  - src/plugin/sampler/SamplerPanel.cpp
  - src/plugin/sampler/SamplerPanel.h
autonomous: false
requirements: [DUCK-06]

must_haves:
  truths:
    - "Per-voice duck source picker is visible in the sampler GUI"
    - "User can select which voice triggers the duck (None, or voice 0-23)"
    - "Release speed and depth knobs are visible and functional"
  artifacts:
    - path: "src/plugin/sampler/SamplerPanel.cpp"
      provides: "Duck section with source dropdown, release knob, depth knob"
    - path: "src/plugin/sampler/SamplerPanel.h"
      provides: "Duck GUI component declarations"
  key_links:
    - from: "src/plugin/sampler/SamplerPanel.cpp"
      to: "PluginProcessor getDuckSource/getDuckRelease/getDuckDepth"
      via: "atomic store on GUI change"
      pattern: "getDuckSource.*store"
---

<objective>
Add sidechain duck controls to the sampler GUI panel: a source voice dropdown
(None / Voice 0-23), a Release speed knob, and a Depth knob. These wire to
the per-voice duck atomics created in Plan 01.

Purpose: Makes the sidechain duck feature user-accessible. Without GUI controls,
the duck mechanism in processBlock has no way to be configured by the user.

Output: Duck section in sampler panel with three controls per voice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-sidechain-duck/46-01-SUMMARY.md
@.planning/phases/45-auto-pan/45-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: duck atomics on PluginProcessor -->
```cpp
// Per-voice duck getters (24 voices):
std::atomic<int>&   getDuckSource(int voice);   // -1=None, 0-23=source voice
std::atomic<float>& getDuckRelease(int voice);  // 0.03-6.8 seconds
std::atomic<float>& getDuckDepth(int voice);    // 0.0-1.0

// Existing sampler panel pattern (from Phase 45 auto-pan GUI):
// ComboBox for selection, Slider for continuous values
// timerCallback reads processor state, component listeners write back
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Duck GUI section in sampler panel (source dropdown + release + depth knobs)</name>
  <files>src/plugin/sampler/SamplerPanel.h, src/plugin/sampler/SamplerPanel.cpp</files>
  <read_first>
    - src/plugin/sampler/SamplerPanel.h (full file -- understand existing GUI component declarations)
    - src/plugin/sampler/SamplerPanel.cpp (the tremolo/auto-pan section for the pattern: combo box + sliders + label layout)
    - .planning/phases/45-auto-pan/45-02-SUMMARY.md (for how auto-pan GUI was structured)
  </read_first>
  <action>
  Add a "Sidechain Duck" section to SamplerPanel following the exact pattern used for tremolo and auto-pan GUI controls.

  HEADER (SamplerPanel.h):
  1. Add member declarations:
     - juce::ComboBox duckSourceBox -- dropdown: "None" + "Voice 0" through "Voice 23"
     - juce::Slider duckReleaseSlider -- rotary, 0.03 to 6.8 seconds, default 0.4
     - juce::Slider duckDepthSlider -- rotary, 0.0 to 1.0, default 1.0
     - juce::Label duckSourceLabel, duckReleaseLabel, duckDepthLabel

  IMPLEMENTATION (SamplerPanel.cpp):

  2. Constructor: initialize duck controls.
     - duckSourceBox: addItem("None", 1), then addItem("Voice 0", 2) through addItem("Voice 23", 25). Set selected to 1 (None).
     - duckReleaseSlider: setRange(0.03, 6.8, 0.01), setValue(0.4), suffix "s", rotary style
     - duckDepthSlider: setRange(0.0, 1.0, 0.01), setValue(1.0), suffix "%", rotary style
     - Labels: "Source", "Release", "Depth"
     - Add listeners (this) to combo box and both sliders.

  3. resized(): Position the duck section below the auto-pan section (or in the effects area). Use the same layout grid as tremolo/auto-pan: label above control, three controls in a row. Section labeled "Sidechain Duck".

  4. Listener callbacks:
     - comboBoxChanged: if duckSourceBox changed, write (selectedId - 2) to processor.getDuckSource(0) -- for voice 0 in the standalone testbed. Value of -1 (selectedId=1="None") means no duck.
     - sliderValueChanged: if duckReleaseSlider changed, write to processor.getDuckRelease(0). If duckDepthSlider changed, write to processor.getDuckDepth(0).

  5. timerCallback: read current duck state from processor and update GUI if not being edited:
     - duckSourceBox: read processor.getDuckSource(0), set selected ID to (value + 2)
     - duckReleaseSlider: read processor.getDuckRelease(0)
     - duckDepthSlider: read processor.getDuckDepth(0)

  6. Mutual exclusion visual feedback: When duck source is not "None", grey out the tremolo enable and auto-pan enable toggles (or add a note). When tremolo or auto-pan is enabled, grey out the duck source dropdown. Follow whatever pattern the existing mutual exclusion already uses visually.

  NOTE: Currently the GUI only controls voice 0 (the GUI-triggered voice in standalone mode). The duck source picker selects from voices 0-23 as the SOURCE. This means: "when voice X plays a note, duck voice 0." This is the most useful standalone configuration -- MIDI voices trigger ducks on the GUI voice.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb/build" && cmake --build . --target SPU94 -j$(nproc) 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Sampler panel shows "Sidechain Duck" section with Source dropdown, Release knob, Depth knob
  - Source dropdown lists "None" and "Voice 0" through "Voice 23"
  - Release knob range 0.03-6.8s, Depth knob range 0-100%
  - Controls wire to processor.getDuckSource(0) / getDuckRelease(0) / getDuckDepth(0)
  - Mutual exclusion: duck controls disabled when tremolo/auto-pan active, and vice versa
  - Standalone binary compiles and launches
  </done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <what-built>Sidechain duck controls added to sampler panel: source voice dropdown, release speed knob, depth knob. Wired to duck atomics from Plan 01.</what-built>
  <how-to-verify>
    1. Launch standalone: ./build/SPU94_artefacts/Release/Standalone/SPU94
    2. Load a sample in the sampler
    3. Verify "Sidechain Duck" section is visible with three controls
    4. Set Source to "Voice 1" (or any voice that MIDI triggers)
    5. Trigger voice 0 with the GUI play button (it should be playing a sustained note)
    6. Send a MIDI note (triggers voice 1 or whichever voice MIDI allocates)
    7. Listen: voice 0 should duck (volume drops) when the MIDI note triggers
    8. Listen: voice 0 should recover (volume returns) after the release time
    9. Adjust Depth to ~50% and retrigger -- duck should be shallower
    10. Adjust Release to maximum and retrigger -- recovery should be slower
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with duck behavior or GUI layout</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI thread -> audio thread | ComboBox/Slider values stored via atomics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Tampering | ComboBox index | mitigate | Clamp selectedId to valid range (1-25) before converting to voice index |
| T-46-05 | Tampering | Slider range | accept | JUCE Slider enforces min/max; no additional validation needed |
</threat_model>

<verification>
1. Build: `cmake --build . --target SPU94` compiles clean
2. Launch: standalone opens without crash, duck section visible
3. Functional: selecting a source voice and triggering produces audible duck + recovery
</verification>

<success_criteria>
- Duck source dropdown visible and functional per voice in sampler GUI
- Release and Depth knobs properly labeled and ranged
- Controls wire to processBlock duck logic from Plan 01
- Mutual exclusion enforced visually (duck vs tremolo/auto-pan)
- User can hear rhythmic pumping when source voice triggers repeatedly
</success_criteria>

<output>
Create `.planning/phases/46-sidechain-duck/46-02-SUMMARY.md` when done
</output>
