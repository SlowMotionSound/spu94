---
context: phase
phase: 06-python-binding-cli
task: 4
total_tasks: 4
status: paused
last_updated: 2026-04-22T18:59:17.454Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure during this session.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Tests verify machinery, not product claims** — the reverb-not-wired bug shipped through six phases because ~400 unit tests verified that functions ran and produced outputs of the expected type, but ZERO of them asserted "Hall produces audibly different output from Off." Before writing any new test, explicitly name the README/ROADMAP product claim it backs. If you can't, the test is noise.
- [ ] CONSTRAINT: **README is NOT a priority for this user** — skip any README findings, install-path issues, PyPI-package-existence checks, GitHub-URL fixes, and PEP 668 warnings when applying code-review fixes. Do not raise them in summaries or "here's what's broken" reports. Persisted to feedback memory as `feedback_readme_not_priority.md`.
- [ ] CONSTRAINT: **No ceremony on bug fixes** — do NOT propose new decimal phases, discuss cycles, plan cycles, or multi-step "first A, then B, then C" sequences for remediation work. Single lean commands (`/gsd-code-review-fix`, `/gsd-debug`, `/gsd-audit-fix`, direct inline fixes) are the preferred shape. Persisted to memory as `feedback_no_ceremony_on_bugs.md`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| `(void)` cast of computed outputs | Reverb's `LeftOutput`/`RightOutput` were computed at `spu94_reverb.c:613-614` and dropped via `(void)` cast with a "Phase 4 will consume this later" comment. Phase 4 didn't. Six phases shipped with the reverb output unwired. | blocking | Every `(void)` cast in `src/` must include a concrete reason in a one-line comment; any cast that defers consumption to "a later phase" must have a tracking issue in the roadmap that blocks the claiming milestone from closing. |
| Test files that document their own gaps | `tests/unit/preset/test_preset_nonzero_tail.c:25-65` explicitly acknowledged the reverb-wiring gap and deferred the fix to "M4 output-bus-through-FIR rewiring, out of M1 scope" — while M1 shipped claiming a working reverb. | blocking | Phase verification must grep tests for `out of M\d scope`, `deferred to`, `not yet`, `placeholder` and fail the phase if any such marker exists on test code that backs a claimed milestone deliverable. |
| Bouncing the user between multiple commands | When asked for "the ideal command," answering with two+ commands and a caveat lands as friction. User explicitly pushed back on this. | advisory | Default to one command. If the single command has limits, orchestrate around them internally; don't make the user coordinate. |
| Dragging README fixes into unrelated conversations | User has stated three+ times in this session that README is end-of-project polish, not current scope. Continuing to surface README findings reads as not listening. | advisory | See `feedback_readme_not_priority.md` — treat it as a hard skip filter on findings. |

## Current State

Phase 06 ran end-to-end: plans 06-01 through 06-05 are committed, Waves 1–3 produced clean SUMMARY artifacts, and Wave 4 (Plan 06-05) reached a human-verify checkpoint on SC-4 (README readability walkthrough).

The user did the walkthrough. It surfaced real bugs — broken install paths, missing test-WAV setup step, whole README too dense. User deferred all README fixes and asked for a lightweight drag-drop tool instead.

While building the launcher, smoke-testing revealed a much bigger problem: **the CLI produces audio that sounds identical to the input.** Tracing the code, reverb output `LeftOutput`/`RightOutput` was being `(void)`-cast in `spu94_reverb.c:613-614` and never reached the audio chain. `tests/unit/preset/test_preset_nonzero_tail.c:25-65` literally documented the gap as "out of M1 scope."

A `/gsd-debug` session (`.planning/debug/reverb-not-in-audio-path.md`) wired the reverb output to the FIR interpolator (ADR-Phase-6-G, committed as `d2c782a`). The wiring is correct — reverb now reaches the output chain — but the reverb network itself produces broken audio: output pinned at ~-5 dBFS regardless of input level, abrupt cliff at second 5. That's self-oscillating feedback or saturation-pinned, not Hall decay.

A whole-codebase code review (`.planning/REVIEW.md`) surfaced 22 findings: 3 CRITICAL, 6 HIGH, 7 MEDIUM, 4 LOW, 2 INFO. Unifying cause: tests verify machinery runs, not product claims.

User ended the session frustrated and invoked `/gsd-pause-work`.

## Completed Work

| Plan | Status | Commit |
|------|--------|--------|
| 06-01 ctypes binding scaffolding | done | ffb643a |
| 06-02 Python API layer | done | 7106169 |
| 06-03 Native CLI binary | done | 8f34457 |
| 06-04 Wheel packaging | done | a6a8588 |
| 06-05 Task 1 (README + verify script) | done | bb78953 |
| 06-05 Task 2 (fuzz migration) | done | 2f57ea2 |
| 06-05 Task 3 (ADRs) | done | b710ad0 |
| 06-05 SUMMARY | done | 01215e0 |
| Reverb wiring fix (partial — wiring only) | partial | d2c782a, 101aaa7 |
| Whole-codebase code review | done (advisory, not committed) | .planning/REVIEW.md |

## Remaining Work

1. **CR-01 — fix the Hall DSP bug.** Output pinned regardless of input is the top priority. Suspected: mix-bus aliasing, internal `sat_s16` saturation, possibly other DSP bugs. Resume `/gsd-debug` — session file at `.planning/debug/reverb-not-in-audio-path.md` has all the evidence.
2. **Apply fixes from REVIEW.md** (non-README findings only — skip CR-03 and any MEDIUM related to install paths). `/gsd-code-review-fix` is the single command.
3. **Commit the drag-drop launcher** at `scripts/desktop-launcher/` — currently uncommitted. Wait until CR-01 is fixed and the launcher produces real reverb; committing a launcher that produces broken audio is worse than not committing.
4. **Close Phase 6 Plan 5 Task 4** — the SC-4 checkpoint is still logically open. Persist as `.planning/phases/06-python-binding-cli/06-HUMAN-UAT.md` with the findings from today's walkthrough so they don't get forgotten. README revisions stay deferred per user instruction.
5. **Phase 6 verification** — run verification once CR-01 is fixed. NOT before.

## Decisions Made

- **Option A wet-only output (ADR-Phase-6-G)**: user chose PS1-authentic wet-only over "plug-in style dry+wet" after Claude initially leaned the wrong direction. The library's bit-faithfulness wins; engineers can mix dry+wet in a DAW if they want.
- **Drag-drop launcher** (Ubuntu Studio `.desktop` + wrapper script + installer) is the preferred UX surface for a recording engineer. CLI stays for power users / scripting. Installer is at `scripts/desktop-launcher/install-to-desktop.sh`.
- **README revisions deferred** — explicit user instruction, stated multiple times.
- **No new phases for remediation** — explicit user instruction.

## Blockers

- **CR-01 Hall DSP bug** — blocks phase 6 verification and the launcher commit. Evidence captured in `.planning/REVIEW.md` (under CR-01) and `.planning/debug/reverb-not-in-audio-path.md`.

## Required Reading (in order)

1. `.planning/REVIEW.md` — 22 findings from the whole-codebase review; skip README-related items (CR-03 and related MEDIUMs)
2. `.planning/debug/reverb-not-in-audio-path.md` — open debug session for CR-01; has the full context of the wiring fix and the evidence that the DSP network itself is broken
3. `docs/DECISIONS.md` § ADR-Phase-6-G — the wet-only architecture decision made this session
4. `docs/DECISIONS.md` § ADR-Phase-5-B — mix-bus mailbox (suspected contributor to the DSP bug — populated at 44.1 kHz without antialiasing, read at 22.05 kHz)
5. `tests/unit/process/test_process_reverb_audible.c` — end-to-end reverb-audible regression test added during the wiring fix; the FIRST test in the whole repo that asserts "Hall output materially differs from Off"
6. Memory files at `~/.claude/projects/-home-ubuntu-studio-Desktop-PSX-Reverb/memory/`: `feedback_readme_not_priority.md`, `feedback_no_ceremony_on_bugs.md`, `user_profile.md`

## Infrastructure State

- **Drag-drop launcher installed on user's Desktop** — `~/Desktop/spu94-render.desktop` and `~/.local/share/applications/spu94-render.desktop` are both present and trusted. They invoke `scripts/desktop-launcher/spu94-render.sh %F` with the absolute path to this repo. They currently produce broken audio (CR-01). Do not ask the user to re-test drag-drop until CR-01 is fixed.
- **build/** directory has a fresh `cmake --build build` of post-wiring-fix state. `build/src/cli/spu94` and `build/src/spu94/libspu94.so` reflect the current committed code (commit 101aaa7).
- **Test assets on disk** (not part of the repo): `/tmp/piano-hall-fixed.wav`, `/tmp/piano-hall-v2.wav`, `/tmp/piano-hall-v3.wav` are the output files the user listened to. Source is `/home/ubuntu-studio/Desktop/E-Keys_02_PSS470.wav` (user's input file — not owned by the repo).

## Context

The session started as a routine Phase 6 execute. Four plans landed cleanly (parallel Wave 1 + sequential Waves 2, 3, 4). Plan 06-05's human-verify checkpoint on SC-4 should have been a quick "looks good / approved" — but the user's walkthrough revealed the README install section was broken and, more importantly, that the CLI produced audio that sounded like the input. The CLI-produced-dry-audio finding was the entry point to discovering the reverb-not-wired bug (which had shipped undetected through Phases 3, 4, 5).

The debugger agent correctly wired reverb output to the interpolator and proved via test that the wiring works. But the reverb network itself has DSP correctness issues that only became visible once the output was actually audible. Measuring output vs input levels showed the network is pinning at saturation regardless of input — the classic self-oscillation / broken-feedback signature.

The code review escalated this from "one bug" to "systemic test-quality failure": tests verified machinery ran, not product claims. Six phases shipped with a test suite that let the reverb-not-wired bug pass unnoticed because no test actually compared reverb-active output to dry-signal output.

User's final request before pausing: `/gsd-code-review-fix` to fix all bugs, no new phases. Honor that literally on resume.

## Next Action

Run `/gsd-code-review-fix`. It will read `.planning/REVIEW.md` and apply fixes atomically. When it reaches CR-01 (flagged as "dedicated debug session" in the review), auto-resume `/gsd-debug` using the session file at `.planning/debug/reverb-not-in-audio-path.md` — do NOT ask the user to coordinate the handoff. Skip all README-related findings (CR-03 and any MEDIUM/LOW touching install paths / PyPI / PEP 668) per memory-persisted user preference.
