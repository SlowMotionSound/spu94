---
phase: 31-standalone-testbed-ux
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/plugin/PluginEditor.h
  - src/plugin/PluginEditor.cpp
autonomous: false
requirements: [TEST-01, TEST-02, TEST-04]

must_haves:
  truths:
    - "Load Sample button opens a file chooser and calls loadVoiceSample on selection"
    - "Status label displays loaded file name and encoded byte count"
    - "Trigger button keys on voice 0 at the pitch from the voice engine pitch knob"
    - "Stop Voice button keys off voice 0"
    - "Voice engine pitch knob covers MIDI range (0x0001 to 0x3FFF) with middle C at unity"
    - "All new GUI components are INVISIBLE in plugin wrapper types"
    - "Plugin GUI layout is byte-identical to v1.7 (TEST-04)"
  artifacts:
    - path: "src/plugin/PluginEditor.h"
      provides: "Voice panel component declarations"
      contains: "loadSampleButton"
    - path: "src/plugin/PluginEditor.cpp"
      provides: "Voice panel setup, layout, timer polling for status"
      contains: "loadVoiceSample"
  key_links:
    - from: "src/plugin/PluginEditor.cpp"
      to: "src/plugin/PluginProcessor.h"
      via: "processorRef.loadVoiceSample / triggerVoice / stopVoice calls"
      pattern: "processorRef\\.loadVoiceSample"
    - from: "src/plugin/PluginEditor.cpp"
      to: "src/plugin/PluginProcessor.h"
      via: "processorRef.getVoiceSampleLoaded / getName / getBytes"
      pattern: "getVoiceSampleLoaded"
---

<objective>
Add a standalone-only Voice Engine panel to the GUI: Load Sample button, pitch knob, Trigger/Stop buttons, and status label. All controls gated on wrapperType_Standalone so the plugin GUI remains unchanged (TEST-04).

Purpose: Provides the visual surface for loading samples (TEST-01), triggering playback (TEST-02), and confirming the load visually (file name + byte count). MIDI works without GUI (Plan 01 handles it), but the GUI completes the testbed user experience.

Output: PluginEditor.h/.cpp with voice panel controls, layout in standalone resized block, status label updated via timerCallback, pitch knob mapped to SPU pitch register range.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-standalone-testbed-ux/31-01-SUMMARY.md
@.planning/phases/31-standalone-testbed-ux/31-PATTERNS.md

<interfaces>
<!-- Processor API from Plan 01 that the editor will call -->

From src/plugin/PluginProcessor.h (Plan 01 output):
```cpp
// --- Voice engine control (message-thread callers) ---
void loadVoiceSample(const juce::File& file);
void triggerVoice(uint16_t pitch);
void stopVoice();

// --- Voice engine state (Phase 31: standalone testbed) ---
std::atomic<bool>& getVoiceSampleLoaded();
const juce::String& getVoiceSampleName() const;
uint32_t getVoiceSampleBytes() const;
```

Existing editor patterns (from PluginEditor.cpp):
```cpp
// Standalone gate (line 15):
if (processorRef.wrapperType == juce::AudioProcessor::wrapperType_Standalone)

// Button onClick pattern (line 19-37):
addAndMakeVisible(loadButton);
loadButton.onClick = [this]() { /* async file chooser */ };

// Resized standalone block (line 437-442):
if (processorRef.wrapperType == juce::AudioProcessor::wrapperType_Standalone)
{
    loadButton.setBounds(10, 10, 100, 30);
    playButton.setBounds(115, 10, 70, 30);
    stopButton.setBounds(190, 10, 70, 30);
}

// Timer callback (line 365): polls processor state, updates GUI
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare voice panel components in PluginEditor.h and wire in PluginEditor.cpp</name>
  <files>src/plugin/PluginEditor.h, src/plugin/PluginEditor.cpp</files>
  <read_first>
    - src/plugin/PluginEditor.h (full current state -- member declarations, where to add new ones)
    - src/plugin/PluginEditor.cpp (lines 1-55 constructor standalone block, lines 247-263 voicePitchKnob setup as analog for new knob, lines 365-413 timerCallback, lines 430-480 resized layout)
    - src/plugin/PluginProcessor.h (Plan 01 output -- the API being called)
    - .planning/phases/31-standalone-testbed-ux/31-PATTERNS.md (exact code patterns for button, knob, label, layout)
    - .planning/phases/31-standalone-testbed-ux/31-RESEARCH.md (State of the Art section: new pitch knob is SEPARATE from existing voicePitchKnob which controls the ADPCM coloration bus)
  </read_first>
  <action>
Modify PluginEditor.h -- add member declarations after the existing ADPCM voice path controls section (after line 83):

1. Voice engine panel declarations (standalone-only):
   - juce::TextButton loadSampleButton{"Load Sample"}
   - juce::TextButton triggerVoiceButton{"Trigger"}
   - juce::TextButton stopVoiceButton{"Stop Voice"}
   - juce::Label voiceSampleLabel (displays "filename NNNNb" after load)
   - juce::Slider voiceEnginePitchKnob (rotary, controls SPU pitch register)
   - juce::Label voiceEnginePitchLabel

Modify PluginEditor.cpp constructor -- extend the existing standalone-gated block (the `if (wrapperType_Standalone)` at line 15) by appending AFTER the stopButton onClick (before the closing brace at line 52):

2. Load Sample button: addAndMakeVisible, onClick launches async FileChooser (reuses the existing fileChooser member -- same pattern as loadButton). Filter: "*.wav;*.aiff;*.aif". On file selection: call processorRef.loadVoiceSample(result).

3. Trigger button: addAndMakeVisible, onClick reads pitch value from voiceEnginePitchKnob (which stores the raw SPU pitch register value), calls processorRef.triggerVoice(pitch).

4. Stop Voice button: addAndMakeVisible, onClick calls processorRef.stopVoice().

5. Voice engine pitch knob setup (inside the standalone block): setSliderStyle Rotary, setTextBoxStyle TextBoxBelow 50x18, setRange(1.0, 16383.0, 1.0) representing raw SPU pitch register 0x0001..0x3FFF, setValue(4096.0) for unity (0x1000 = middle C), setTextValueSuffix(" pitch"), label text "Voice Pitch". No onValueChange callback needed -- the button reads the knob value at trigger time.

6. Voice sample status label: addAndMakeVisible, initial text empty, left-justified.

Modify PluginEditor.cpp resized() -- inside the standalone block (after stopButton.setBounds at line 441), add bounds for new voice panel components. Place them in a second row below the existing Load/Play/Stop toolbar (y=45 or similar):
   - loadSampleButton.setBounds(10, 45, 110, 30)
   - triggerVoiceButton.setBounds(125, 45, 70, 30)
   - stopVoiceButton.setBounds(200, 45, 90, 30)
   - voiceEnginePitchLabel.setBounds(295, 37, 80, 16)
   - voiceEnginePitchKnob.setBounds(295, 51, 80, 54)
   - voiceSampleLabel.setBounds(380, 45, 200, 30)

Note: If the new row overlaps the register panel (Zone 2 starts at y=75), adjust Zone 2 top by increasing registerTop from 75 to 110 when in standalone mode. Check the resized function for the registerTop variable and conditionally adjust it.

Modify PluginEditor.cpp timerCallback() -- add a check at the end of the function: if processorRef.getVoiceSampleLoaded() is true, update voiceSampleLabel text to show the filename and byte count from processorRef.getVoiceSampleName() + " " + String(getVoiceSampleBytes()) + "B". Use a local bool to avoid unnecessary repaints (only update text if it changed, or simply let juce::Label's setText with dontSendNotification handle it).
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb/build" && cmake --build . --target spu94_plugin 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - PluginEditor.h contains declarations: loadSampleButton, triggerVoiceButton, stopVoiceButton, voiceSampleLabel, voiceEnginePitchKnob, voiceEnginePitchLabel
    - Constructor standalone block calls addAndMakeVisible for all 6 new components
    - loadSampleButton.onClick launches FileChooser and calls processorRef.loadVoiceSample
    - triggerVoiceButton.onClick reads voiceEnginePitchKnob.getValue() cast to uint16_t, calls processorRef.triggerVoice
    - stopVoiceButton.onClick calls processorRef.stopVoice()
    - voiceEnginePitchKnob range is 1.0 to 16383.0 with default 4096.0
    - resized() positions all new components inside the standalone gate
    - timerCallback() updates voiceSampleLabel text when voiceSampleLoaded is true
    - All new addAndMakeVisible calls are inside wrapperType_Standalone guard
    - Plugin builds clean, all 117 tests pass
  </acceptance_criteria>
  <done>Voice panel GUI complete: load, trigger, stop, pitch knob, status label. All standalone-gated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Standalone testbed voice engine UX: Load Sample encodes WAV to ADPCM into voice RAM, Trigger/Stop buttons play voice 0 at the selected pitch, MIDI note-on/off triggers polyphonic voices across all 24 slots. All controls are standalone-only (plugin GUI unchanged).
  </what-built>
  <how-to-verify>
    1. Build and launch the standalone: `cd build && cmake --build . --target spu94_standalone && ./spu94_standalone_artefacts/Release/SPU-94`
    2. Verify a "Load Sample" button, "Trigger" button, "Stop Voice" button, and a pitch knob labeled "Voice Pitch" appear below the existing Load WAV / Play / Stop row.
    3. Click "Load Sample", select any WAV file. Confirm the status label shows the filename and a byte count (e.g., "kick.wav 1024B").
    4. With pitch knob at default (4096 = unity), click "Trigger". Confirm audible output. Click "Stop Voice". Confirm silence.
    5. Adjust the pitch knob higher (e.g., 8192), click "Trigger". Confirm the sound plays at a higher pitch.
    6. Connect a MIDI controller (or use a virtual MIDI device). Play notes. Confirm polyphonic output (multiple notes ring simultaneously). Release notes. Confirm they fade/stop.
    7. (TEST-04) Build and validate the plugin: `pluginval --validate build/spu94_plugin_artefacts/VST3/SPU-94.vst3 --strictness-level 5` passes. (Or manually load in a DAW and confirm no voice panel appears.)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI input to processor | Button clicks translate to C API calls with user-controlled pitch values |
| File chooser to encoder | User-selected file loaded on message thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-05 | Tampering | pitch knob value | mitigate | Slider range constrained to 1..16383; voice engine clamps at 0x3FFF (VOICE-03) |
| T-31-06 | Denial of Service | Very large WAV file | accept | Testbed context; encode is CPU-bound but message-thread (no audio glitch). Long samples may lag UI briefly. |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- `cd build && cmake --build . --target spu94_plugin` compiles clean
- `cd build && ctest --output-on-failure` all tests pass
- Visual verification: standalone shows voice panel, plugin does not
- Audio verification: trigger produces sound, stop silences, MIDI triggers polyphony
</verification>

<success_criteria>
- Standalone shows Load Sample / Trigger / Stop Voice / pitch knob / status label
- Load Sample encodes WAV and displays file name + byte count
- Trigger plays voice 0 at knob pitch; Stop silences it
- MIDI note-on/off triggers polyphonic voices (Plan 01 backend)
- Plugin GUI is unchanged from v1.7
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/31-standalone-testbed-ux/31-02-SUMMARY.md` when done
</output>
