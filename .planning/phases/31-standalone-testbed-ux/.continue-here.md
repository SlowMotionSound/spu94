---
context: phase
phase: 31-standalone-testbed-ux
task: 0
total_tasks: 0
status: reopening
last_updated: 2026-05-17T18:14:20.472Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Never delete SPU-94.settings — JUCE standalone wrapper on KDE/Wayland saves window position as (0, 44) regardless of actual position. Deleting the file makes the window permanently invisible until user manually maximizes from taskbar. Fix must be a code-level window position override, not settings file manipulation.
- [ ] CONSTRAINT: Never guess at runtime behavior — write a test or observe actual state before editing code. This session spent over an hour guessing at window positioning fixes that all failed.
- [ ] CONSTRAINT: Apply GUI changes one at a time with a build+verify between each — batching the SamplerWindow, mixer reorganization, and Input Rate rename into one commit made it impossible to isolate what broke the window.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Settings file deletion | Deleted ~/.config/SPU-94.settings to "fix" window positioning. JUCE on Wayland cannot recover a valid position without a pre-existing file. Window became permanently offscreen. | blocking | Never delete or modify SPU-94.settings. Window positioning fix must be code-level (force visible position in editor constructor or paint callback). |
| Batch GUI changes | Combined SamplerWindow creation, mixer knob reorganization, label rename, and layout changes into one commit. When the window broke, couldn't tell which change caused it. | blocking | One GUI change per build+verify cycle. Commit after each verified change. |
| Guessing instead of observing | Spent 60+ minutes trying setCentreRelative, callAsync, paint-callback centering, settings patching — all without first verifying WHY the window was invisible (strace, window manager inspection, JUCE source reading). | blocking | Before any fix attempt: run the binary with output capture, check if the window exists via window manager tools, read the JUCE standalone wrapper source to understand how it places windows on Wayland. |

<current_state>
Phase 31 code is at commit ce23265 — the state where Anthony verified the GUI worked (load sample, trigger, pitch, stop all functional). The thread-safety fixes are in. All tests pass.

The session's GUI rework (SamplerWindow, mixer reorganization, Input Rate rename, sampler bus separation in C core) was committed then reverted via git reset --hard back to ce23265. The C core sampler bus separation (sampler_fader, sampler_send as independent mixer controls) was lost in the reset and needs to be re-applied.

The JUCE standalone window does not appear on launch due to the settings file saving (0, 44) as window position. User must manually maximize from taskbar each time. This needs a code-level fix.
</current_state>

<completed_work>

Phase 31 Plans 31-01 and 31-02 executed:
- 31-01: Voice engine processor API (sample load, MIDI dispatch, trigger/stop) — commit 136791b
- 31-02: Voice panel GUI (load sample, trigger, stop, pitch knob, status label) — commit ea24a98
- Thread-safety fix: GUI trigger/stop staged through atomics, voice-steal key-off — commit ce23265
- Code review completed, critical findings fixed

Signal flow decisions made (conversation only, not yet in code):
- Sampler output is independent from live input ADPCM bus — own fader and send
- Five signal paths: 3 from live input (dry→reverb, ADPCM→reverb, ADPCM→mixer, dry→mixer), 2 from sampler (sampler→reverb, sampler→mixer)
- Global sampler send (not per-voice EON) for now
- Mixer GUI sections: ADPCM (Input ADPCM Level + Sampler Level), Dry, Reverb
- Sampler gets its own window — separate instrument, connected to SPU-94 at audio level
- "Voice Rate" label renamed to "Input Rate" (live input sample rate, not sampler)
</completed_work>

<remaining_work>

1. JUCE window position fix — code-level forced centering on startup so the window appears without manual maximize. This is the immediate blocker.
2. C core sampler bus separation — re-apply sampler_fader and sampler_send fields to spu94_state, API, and process loop. Was working and tested before the reset.
3. "Voice Rate" → "Input Rate" label rename — one-line change in PluginEditor.cpp
4. Sampler window — separate JUCE DocumentWindow for the sampler instrument controls. Must be added carefully (one change at a time, verified between each).
5. Move sampler controls from main editor to sampler window
6. Add Sampler Level and Sampler Send knobs to main mixer section
7. Reorganize mixer GUI into ADPCM / Dry / Reverb sections
8. GUI polish — Anthony has not yet walked through the visual issues he spotted
9. MIDI verification — Anthony couldn't test MIDI (no controller available)
</remaining_work>

<decisions_made>

- Sampler and SPU-94 reverb are two separate products connected at audio level — separate windows, separate feel, coupled only by signal flow (saved to memory)
- Live input signal has 4 paths: ADPCM→Send→Reverb, ADPCM→Level→Mixer, Dry→Send→Reverb, Dry→Level→Mixer
- Sampler signal has 2 paths: Sampler→Send→Reverb, Sampler→Level→Mixer
- Global sampler send replaces per-voice EON flag for now — one knob controls all 24 voices' reverb contribution
- Mixer GUI organized by sound character: ADPCM section (crunchy PS1), Dry section (clean passthrough), Reverb section (wet)
- "Voice Rate" is wrong — it's the live input sample rate, call it "Input Rate"
- Scope from user, not spec — never extrapolate milestone scope from hardware spec without explicit user confirmation (saved to memory)
- Use precise signal flow language — always specify which path (live input vs sampler) when describing controls (saved to memory)
</decisions_made>

<blockers>
- JUCE standalone window invisible on KDE/Wayland without manual maximize from taskbar. Settings file always saves (0, 44) because Wayland doesn't let JUCE read real window position. Needs code-level fix before any further GUI work.
</blockers>

## Required Reading (in order)
1. This file — blocking constraints and anti-patterns
2. `.planning/phases/31-standalone-testbed-ux/31-RESEARCH.md` — voice engine architecture
3. `src/plugin/PluginEditor.cpp` — current GUI layout (at ce23265 state)
4. `src/spu94/spu94_process.c` — current signal flow (voice output still summed into patina bus)
5. Memory: `project_sampler_architecture.md` — sampler/reverb separation decision
6. Memory: `feedback_precise_signal_language.md` — naming discipline
7. Memory: `feedback_scope_from_user_not_spec.md` — never extrapolate scope

## Infrastructure State
- No background processes running
- Build system: CMake, build/ directory, Release configuration
- ~/.config/SPU-94.settings exists with filterState blob but window position (0, 44) is broken on Wayland
- All tests passing at ce23265

## Pre-Execution Critique Required
- Design artifact: Signal flow decisions are in conversation only, not yet documented
- Critique focus: Is the sampler bus separation correct? Does the global sampler send (vs per-voice EON) lose any PS1 faithfulness that matters?
- Gate: Document the agreed signal flow before re-implementing

<context>
Anthony and I agreed on a clear signal flow architecture and mixer layout, then I tried to implement everything at once, broke the window, spent an hour guessing at fixes, and had to reset. The signal flow decisions are solid — the implementation approach was the problem. Next session needs to re-apply changes one at a time with verification between each.

Anthony is frustrated with the session. Respect that by being precise, methodical, and never guessing.
</context>

<next_action>
Start with: Fix the JUCE standalone window positioning on Wayland/KDE. Read the JUCE StandaloneFilterWindow source (build/_deps/juce-src/modules/juce_audio_plugin_client/Standalone/juce_StandaloneFilterWindow.h) to understand how it saves/restores position, then implement a targeted fix. Verify the window appears before touching anything else.
</next_action>
