---
context: phase
phase: 31-standalone-testbed-ux
task: 0
total_tasks: 0
status: paused
last_updated: 2026-05-18T01:54:04.537Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Apply GUI changes one at a time with a build+verify between each — batching changes makes it impossible to isolate breakage on XWayland.
- [ ] CONSTRAINT: Never guess at runtime behavior — observe with xwininfo/wmctrl before editing code.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Batch GUI changes | Prior session combined SamplerWindow creation, mixer reorg, label rename into one commit. Broke window, couldn't tell which change caused it. | blocking | One GUI change per build+verify cycle. |
| Guessing instead of observing | Prior session spent 60+ minutes guessing at window fixes without checking actual window state. | advisory | Run binary, check xwininfo, then fix. |

<current_state>
Phase 31 has 6 uncommitted changes on top of ce23265, all verified individually:

1. XWayland window sizing fix (PluginEditor timer pokes editor bounds on first tick)
2. "Voice Rate" → "Input Rate" label rename
3. C core sampler bus separation (sampler_fader, sampler_send as independent mixer fields)
4. Sampler Level knob in bottom mixer row + Samp Send knob in top toolbar with other sends
5. Separate PSX Sampler window (SamplerWindow.h) — voice controls moved into it
6. Samp Send relocated from mixer row to toolbar (Anthony: sends go with sends)

All tests pass. Window appears on launch. Both windows functional.
</current_state>

<completed_work>

Phase 31 Plans 31-01 and 31-02 executed:
- 31-01: Voice engine processor API (sample load, MIDI dispatch, trigger/stop) — commit 136791b
- 31-02: Voice panel GUI (load sample, trigger, stop, pitch knob, status label) — commit ea24a98
- Thread-safety fix: GUI trigger/stop staged through atomics, voice-steal key-off — commit ce23265
- XWayland window fix: timer-based editor bounds poke — uncommitted
- Input Rate rename — uncommitted
- C core sampler bus: sampler_fader + sampler_send fields, getters/setters, process.c 4-bus mixer, preset serialization — uncommitted
- Sampler + Samp Send GUI knobs wired to processor atomics — uncommitted
- SamplerWindow.h: separate DocumentWindow, voice controls reparented into it — uncommitted
- Samp Send moved to toolbar with ADPCM Send and Dry Send — uncommitted
</completed_work>

<remaining_work>

1. Reorganize mixer section labels into ADPCM / Dry / Reverb groupings (cosmetic)
2. GUI polish — Anthony to walk through visual issues
3. MIDI input verification — needs controller plugged in
</remaining_work>

<decisions_made>

- Sampler and SPU-94 reverb are separate products connected at audio level — separate windows
- Global sampler send replaces per-voice EON — one knob for all 24 voices' reverb contribution
- Sampler bus is independent from ADPCM coloration bus — own fader and send
- Samp Send lives in toolbar with other sends, not mixer row (Anthony's correction)
- "Voice Rate" renamed to "Input Rate" — it controls live input sample rate
- XWayland fix uses 800px threshold — second window changes initial bogus size
- Signal flow: 6 paths total — 4 live-input (ADPCM fader/send, dry fader/send) + 2 sampler (sampler fader/send)
</decisions_made>

<blockers>
None. Window fix resolved. All structural work complete.
</blockers>

## Required Reading (in order)
1. This file — constraints and current state
2. `src/plugin/SamplerWindow.h` — the sampler window shell
3. `src/plugin/PluginEditor.cpp` — GUI layout with sampler window integration
4. `src/spu94/spu94_process.c` — 4-bus master mixer signal flow

## Infrastructure State
- No background processes running
- Build system: CMake, build/ directory, Release configuration
- All C core tests passing
- 9 modified files + 1 new file (SamplerWindow.h), all uncommitted

<context>
This session successfully re-applied all the changes that broke last session, but this time one at a time. The XWayland window bug was the real root cause — JUCE's StandaloneFilterWindow reads getWidth()/getHeight() before XWayland propagates the editor size. The fix pokes the editor bounds by ±1 pixel on the first timer tick to re-trigger the sizing chain. Threshold had to be raised to 800px because the second sampler window changed the initial bogus size from 2x58 to 128x128. Anthony verified each change visually. All remaining work is polish/verification, not structural.
</context>

<next_action>
Start with: Commit all session work as one commit. Then address remaining items (mixer label reorg, GUI polish, MIDI) based on Anthony's direction.
</next_action>
