---
context: phase
phase: 31-standalone-testbed-ux
task: 0
total_tasks: 0
status: paused
last_updated: 2026-05-20T03:05:48Z
---

<current_state>
All v1.8 phases (27-32) complete. Session focused on sampler UX polish:
ADSR display accuracy, decay timing, sampler drive stage, gate trigger.
Three commits this session: 5ea7425, 27eff9b, bfda726.
</current_state>

<completed_work>

This session:
- ADSR display overhaul: analytical rendering replaces tick-by-tick simulation. Smooth knob-proportional width allocation (linear weight, no floor). Rise/Fall center detent at ±3%. Power-curve sync between display and engine for all segments.
- Decay shift max raised from 15 to 20: mid-knob decay now audible (~600ms vs ~155ms). Engine handles shifts >15 fine; the PS1 4-bit limit is a hardware constraint, not an engine one.
- Sampler drive stage: Q12 int32 gain with sat_s16 hard clipping, applied after voice mixer tick before master mix buses. Independent +24dB overdrive for sampler voices. Knob in PSX Sampler window next to Voice Pitch.
- Gate-style trigger: mouseDown=key_on, mouseUp=key_off. Hold for sustain, release for release phase. Stop Voice remains as emergency kill.
- Default ADSR changed: Rel=0, all Exp off (flat rectangle at launch).
</completed_work>

<remaining_work>

- Loop scrub silence: some start-marker positions produce silence during real-time loop scrub. Likely ADPCM decoder state issue when jumping between blocks — prev1/prev2 history doesn't match the new block position.
- Min-loop test: verify 56-sample minimum-distance looping produces audible single-cycle tones.
- Waveform scroll/zoom: fixed 380x120 display feels clunky for long samples. No zoom or scroll implemented.
- Knob sizing: ADSR knobs (55x45) and marker knobs (60x50) feel small vs main knobs (65-80 wide).
- XWayland sampler window: occasionally launches tiny (intermittent).
- MIDI verification: needs a controller plugged in.
</remaining_work>

<decisions_made>

- ADSR display uses analytical rendering (not tick-by-tick) — extreme shift values (shift 20 = 2.4M ticks) made simulation impractical for display
- Display width allocation uses raw knob value (linear * 0.05, sustain weight 0.02, no floor) — smooth continuous growth, instant phases near-invisible
- Decay max shift 15→20 — PS1 hardware is 4-bit but the ADSR engine handles any shift; makes decay proportional to attack at same knob position
- Sampler drive uses Q12 int32 (not int16) to avoid overflow at 16x
- Trigger button acts as modular gate (mouseDown/mouseUp) instead of click-to-toggle
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `src/plugin/AdsrDisplay.h` — analytical envelope rendering, weight allocation formula
2. `src/spu94/spu94_process.c:103-115` — sampler drive application point in signal chain
3. `.planning/HANDOFF.json` — full structured state with all task history

## Infrastructure State
- No background processes running (SPU-94 killed at session end)

<context>
v1.8 milestone is functionally complete (all 6 phases done). This session was
post-milestone UX polish. The remaining items are all cosmetic/ergonomic —
none block milestone completion. Next session could either continue polish
or close out v1.8.
</context>

<next_action>
Start with: test loop scrub silence (ADPCM decoder state on block jumps),
or decide to complete v1.8 milestone and defer remaining polish to v1.9.
</next_action>
