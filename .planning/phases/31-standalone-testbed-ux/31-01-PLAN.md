---
phase: 31-standalone-testbed-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/PluginProcessor.h
  - src/plugin/PluginProcessor.cpp
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "loadVoiceSample encodes WAV to ADPCM and stores in voice_ram at offset 0"
    - "triggerVoice keys on voice 0 at a given pitch register value"
    - "stopVoice keys off voice 0"
    - "MIDI note-on in standalone processBlock triggers a voice at the correct pitch"
    - "MIDI note-off in standalone processBlock releases the matching voice"
    - "acceptsMidi returns true ONLY for standalone wrapper type"
    - "Plugin processBlock path ignores MIDI buffer (TEST-04 unchanged)"
  artifacts:
    - path: "src/plugin/PluginProcessor.h"
      provides: "Voice engine public API + state atomics"
      contains: "loadVoiceSample"
    - path: "src/plugin/PluginProcessor.cpp"
      provides: "Sample load, trigger, stop, MIDI dispatch, voice allocation"
      contains: "midiNoteToPitch"
  key_links:
    - from: "src/plugin/PluginProcessor.cpp"
      to: "include/spu94/spu94_voice.h"
      via: "spu94_voice_mixer_key_on/key_off calls"
      pattern: "spu94_voice_mixer_key_on"
    - from: "src/plugin/PluginProcessor.cpp"
      to: "include/spu94/spu94_sample_loader.h"
      via: "spu94_sample_encode_to_ram call"
      pattern: "spu94_sample_encode_to_ram"
---

<objective>
Wire the voice engine C core APIs into the JUCE processor layer: sample loading (WAV-to-ADPCM into voice RAM), GUI-triggered voice playback, and standalone-only MIDI note dispatch with round-robin polyphonic voice allocation.

Purpose: Provides the processor-level API that the GUI (Plan 02) will call, and enables standalone MIDI polyphony without touching the plugin audio path.

Output: PluginProcessor.h/.cpp with loadVoiceSample, triggerVoice, stopVoice methods; MIDI dispatch in standalone processBlock; conditional acceptsMidi; round-robin voice allocator with note tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-24-voice-polyphony-mixer/30-PLAN-SUMMARY.md
@.planning/phases/31-standalone-testbed-ux/31-RESEARCH.md
@.planning/phases/31-standalone-testbed-ux/31-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From include/spu94/spu94_voice.h:
```cpp
spu94_voice_mixer_t *spu94_get_voice_mixer(void);

spu94_result_t spu94_voice_mixer_key_on(spu94_voice_mixer_t *m, int voice_idx,
    uint32_t start_addr, uint16_t pitch, int16_t vol_l, int16_t vol_r,
    int reverb_on, const spu94_adsr_state_t *adsr_cfg);

spu94_result_t spu94_voice_mixer_key_off(spu94_voice_mixer_t *m, int voice_idx);

spu94_result_t spu94_voice_mixer_load_sample(spu94_voice_mixer_t *m,
    uint32_t addr, const uint8_t *source, uint32_t source_size);
```

From include/spu94/spu94_sample_loader.h:
```cpp
int32_t spu94_sample_encode_to_ram(
    const int16_t *pcm, uint32_t num_samples,
    uint8_t *voice_ram, uint32_t ram_offset, uint32_t ram_size,
    int loop_enable);
```

From include/spu94/spu94_spu_ram.h:
```cpp
#define SPU94_SPU_RAM_BYTES (512 * 1024)
```

From src/standalone/WavLoader.h:
```cpp
struct WavLoadResult { std::vector<int16_t> L, R; uint64_t numFrames; };
static std::optional<WavLoadResult> load(const juce::File& file);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add voice engine API and state to PluginProcessor.h</name>
  <files>src/plugin/PluginProcessor.h</files>
  <read_first>
    - src/plugin/PluginProcessor.h (full current state -- patterns for includes, public API style, private atomics)
    - include/spu94/spu94_voice.h (spu94_voice_mixer_key_on signature, spu94_adsr_state_t)
    - include/spu94/spu94_sample_loader.h (spu94_sample_encode_to_ram signature)
    - .planning/phases/31-standalone-testbed-ux/31-PATTERNS.md (exact code patterns to follow)
  </read_first>
  <action>
Modify PluginProcessor.h to support the voice engine testbed:

1. Add includes inside the existing extern "C" block: spu94_voice.h and spu94_sample_loader.h.

2. Change acceptsMidi() from hardcoded `return false` to `return wrapperType == wrapperType_Standalone`. This conditionally enables MIDI only for the standalone build (TEST-04 preservation).

3. Uncomment the midiMessages parameter: change the processBlock declaration from `juce::MidiBuffer& midiMessages` (it's already named in the header declaration at line 25-26, but confirmed with the cpp file where it's commented out as /*midiMessages*/ -- the header declaration stays as-is, the cpp change is in Task 2).

4. Add public methods section after the existing WAV playback control section (after line 55):
   - void loadVoiceSample(const juce::File& file) -- message-thread sample encoder
   - void triggerVoice(uint16_t pitch) -- keys on voice 0 at given pitch
   - void stopVoice() -- keys off voice 0

5. Add public accessors for GUI status display (after the existing ADPCM voice path controls section around line 93):
   - std::atomic<bool>& getVoiceSampleLoaded() returning voiceSampleLoaded
   - const juce::String& getVoiceSampleName() const returning voiceSampleName
   - uint32_t getVoiceSampleBytes() const returning voiceSampleBytes

6. Add private member declarations (after wavSource/pendingSlots block, before srcChain_):
   - std::atomic<bool> voiceSampleLoaded{false}
   - juce::String voiceSampleName
   - uint32_t voiceSampleBytes{0}
   - int8_t noteForVoice[24] array initialized to -1 (voice allocation tracking)
   - int nextVoice{0} (round-robin pointer)

7. Add private helper declarations:
   - static uint16_t midiNoteToPitch(int note, int baseNote = 60)
   - int allocateVoice(int note)
   - int findVoiceForNote(int note)
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb/build" && cmake --build . --target spu94_plugin 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Header contains `#include <spu94/spu94_voice.h>` and `#include <spu94/spu94_sample_loader.h>` inside extern "C" block
    - acceptsMidi() body is `return wrapperType == wrapperType_Standalone;`
    - Public section contains declarations for loadVoiceSample, triggerVoice, stopVoice
    - Public accessors getVoiceSampleLoaded, getVoiceSampleName, getVoiceSampleBytes exist
    - Private section contains voiceSampleLoaded, voiceSampleName, voiceSampleBytes, noteForVoice[24], nextVoice
    - Private section contains midiNoteToPitch, allocateVoice, findVoiceForNote declarations
    - Compiles without errors (linker errors expected until Task 2 provides definitions)
  </acceptance_criteria>
  <done>PluginProcessor.h declares the full voice engine API surface. acceptsMidi is standalone-conditional.</done>
</task>

<task type="auto">
  <name>Task 2: Implement voice engine methods and MIDI dispatch in PluginProcessor.cpp</name>
  <files>src/plugin/PluginProcessor.cpp</files>
  <read_first>
    - src/plugin/PluginProcessor.cpp (lines 254-256 for processBlock signature, lines 519-585 for standalone branch, lines 776-812 for loadWavFile/startPlayback/stopPlayback patterns)
    - src/plugin/PluginProcessor.h (Task 1 output -- new declarations)
    - include/spu94/spu94_voice.h (full API signatures, spu94_get_voice_mixer, spu94_voice_mixer_key_on params)
    - include/spu94/spu94_sample_loader.h (spu94_sample_encode_to_ram params and return value)
    - .planning/phases/31-standalone-testbed-ux/31-PATTERNS.md (exact MIDI dispatch pattern, loadVoiceSample pattern)
    - .planning/phases/31-standalone-testbed-ux/31-RESEARCH.md (Pitfall 1 mixer.enabled, Pitfall 2 acceptsMidi, Pitfall 7 encode on audio thread)
  </read_first>
  <action>
Implement all voice engine methods in PluginProcessor.cpp:

1. Change processBlock signature parameter from `juce::MidiBuffer& /*midiMessages*/` to `juce::MidiBuffer& midiMessages` (remove comment-out). This enables the buffer to be read in the standalone path.

2. Add MIDI dispatch block INSIDE the existing `if (isStandalone)` block at line 521, BEFORE the existing WAV-active check (before line 527). Only process MIDI when voiceSampleLoaded is true. Iterate midiMessages using `for (const auto metadata : midiMessages)`. On note-on: compute pitch via midiNoteToPitch(note), compute volume from velocity as `(vel * 0x7FFF) / 127`, call allocateVoice(note), call spu94_voice_mixer_key_on with mixer from spu94_get_voice_mixer(), start_addr=0, reverb_on=1, adsr_cfg=nullptr. On note-off: call findVoiceForNote, if >= 0 call spu94_voice_mixer_key_off.

3. Implement loadVoiceSample(const juce::File& file) after the existing stopPlayback/isPlaying/isLoaded methods (around line 812). Pattern: call WavLoader::load, if no value return. Get mixer via spu94_get_voice_mixer(). Call spu94_sample_encode_to_ram with result->L.data(), result->numFrames, mixer->voice_ram, ram_offset=0, SPU94_SPU_RAM_BYTES, loop_enable=1. If bytes > 0: set mixer->enabled = 1, set mixer->master_vol_l = 0x7FFF, set mixer->master_vol_r = 0x7FFF, store filename to voiceSampleName, store byte count to voiceSampleBytes, store true to voiceSampleLoaded with release ordering.

4. Implement triggerVoice(uint16_t pitch): get mixer, call spu94_voice_mixer_key_on(mixer, 0, 0, pitch, 0x7FFF, 0x7FFF, 1, nullptr).

5. Implement stopVoice(): get mixer, call spu94_voice_mixer_key_off(mixer, 0).

6. Implement midiNoteToPitch(int note, int baseNote): compute ratio = pow(2.0, (note - baseNote) / 12.0), pitch = (int)(0x1000 * ratio + 0.5), clamp to 1..0x3FFF, return uint16_t.

7. Implement allocateVoice(int note): use nextVoice as round-robin index into voices 0-23. If noteForVoice[nextVoice] != -1, that's a voice being stolen (acceptable). Set noteForVoice[nextVoice] = note. Capture current index, advance nextVoice = (nextVoice + 1) % 24, return captured index.

8. Implement findVoiceForNote(int note): linear scan noteForVoice[0..23] for matching note. On match, set noteForVoice[idx] = -1, return idx. If no match, return -1.

9. In the constructor (or prepareToPlay), initialize noteForVoice array: memset(noteForVoice, -1, sizeof(noteForVoice)). Also ensure nextVoice = 0. (Check constructor patterns -- if arrays are brace-initialized in the header, skip explicit init.)
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb/build" && cmake --build . --target spu94_plugin 2>&1 | tail -5 && ctest --output-on-failure 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - processBlock signature uses `juce::MidiBuffer& midiMessages` (no comment-out)
    - MIDI dispatch loop exists inside the `if (isStandalone)` block, before WAV-active check
    - MIDI dispatch calls spu94_voice_mixer_key_on on note-on with pitch from midiNoteToPitch
    - MIDI dispatch calls spu94_voice_mixer_key_off on note-off via findVoiceForNote
    - loadVoiceSample calls WavLoader::load then spu94_sample_encode_to_ram into mixer->voice_ram
    - loadVoiceSample sets mixer->enabled=1, master_vol_l=0x7FFF, master_vol_r=0x7FFF on success
    - loadVoiceSample stores filename, byte count, and sets voiceSampleLoaded=true
    - triggerVoice calls spu94_voice_mixer_key_on(mixer, 0, 0, pitch, 0x7FFF, 0x7FFF, 1, nullptr)
    - stopVoice calls spu94_voice_mixer_key_off(mixer, 0)
    - midiNoteToPitch(60, 60) returns 0x1000 (unity)
    - midiNoteToPitch(72, 60) returns 0x2000 (octave up)
    - midiNoteToPitch(96, 60) clamps at 0x3FFF (hardware max)
    - Plugin builds clean and all 117 existing tests pass
  </acceptance_criteria>
  <done>PluginProcessor.cpp provides working voice load, trigger, stop, and MIDI polyphonic dispatch. Plugin format path unchanged. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WAV file to encoder | Untrusted file data crosses into spu94_sample_encode_to_ram |
| MIDI input to voice allocation | External MIDI note numbers drive array indexing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | spu94_sample_encode_to_ram | mitigate | Existing bounds check in C API returns -1 on overflow (RAM-03); loadVoiceSample checks return > 0 before enabling |
| T-31-02 | Denial of Service | MIDI note flood | accept | 24-voice round-robin naturally limits; no amplification vector |
| T-31-03 | Tampering | MIDI note number out of range | mitigate | midiNoteToPitch clamps output to 0x0001..0x3FFF; JUCE MidiMessage validates 0-127 |
| T-31-04 | Information Disclosure | voice_ram contents | accept | Voice RAM contains user-loaded audio only; no secrets |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
- `cd build && cmake --build . --target spu94_plugin` compiles without warnings
- `cd build && ctest --output-on-failure` all tests pass (no regressions)
- grep confirms acceptsMidi contains wrapperType_Standalone
- grep confirms processBlock MIDI parameter is not commented out
- grep confirms loadVoiceSample, triggerVoice, stopVoice methods exist
</verification>

<success_criteria>
- PluginProcessor.h declares voice engine API with standalone-conditional MIDI
- PluginProcessor.cpp implements sample load, trigger/stop, MIDI dispatch
- Plugin builds cleanly on target platform
- All 117 existing tests pass (zero regressions)
- Plugin processBlock path untouched (TEST-04 guarantee)
</success_criteria>

<output>
Create `.planning/phases/31-standalone-testbed-ux/31-01-SUMMARY.md` when done
</output>
