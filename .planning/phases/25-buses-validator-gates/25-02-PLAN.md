---
phase: 25-buses-validator-gates
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - .github/workflows/plugins.yml
autonomous: true
requirements: [PLUG-37, PLUG-38, PLUG-39, PLUG-40, PLUG-41, PLUG-42]

must_haves:
  truths:
    - "pluginval strictness-7 runs on every format on every OS as a build-blocking gate"
    - "auval runs on every macOS build as a build-blocking gate"
    - "lv2lint + sord_validate run on every Linux LV2 build as a build-blocking gate"
    - "VST3 SDK validator runs on every VST3 build as a build-blocking gate"
    - "Any validator warning or error fails the CI build"
    - "The advisory pluginval-early-warning job is removed or replaced by the hard gate"
  artifacts:
    - path: ".github/workflows/plugins.yml"
      provides: "All four validator gates as hard CI steps"
      contains: "strictness-level 7"
  key_links:
    - from: ".github/workflows/plugins.yml"
      to: "pluginval"
      via: "strictness-7 invocation per format per OS"
      pattern: "strictness-level 7"
    - from: ".github/workflows/plugins.yml"
      to: "auval"
      via: "macOS-only CI step"
      pattern: "auval -v aufx Sp94 Spu9"
    - from: ".github/workflows/plugins.yml"
      to: "lv2lint"
      via: "Linux-only CI step, built from source"
      pattern: "lv2lint"
    - from: ".github/workflows/plugins.yml"
      to: "VST3 validator"
      via: "Built from SDK source, runs on all OSes"
      pattern: "validator"
---

<objective>
Promote pluginval from strictness-1 smoke to strictness-7 hard gate on every format on every OS. Add auval (macOS), lv2lint + sord_validate (Linux), and VST3 SDK validator as hard CI gates. Remove the advisory pluginval-early-warning job. Any warning or error from any validator fails the build.

Purpose: The bus layout changes from Plan 01 need to survive validator scrutiny. These gates catch RT-safety violations, parameter thread safety issues, AU bus-layout negotiation bugs, LV2 metadata errors, and VST3 interface contract violations before they reach beta testers. PLUG-37 through PLUG-42 make validation a non-negotiable CI gate.

Output: Updated plugins.yml with four validator categories as hard gates; advisory job removed.
</objective>

<execution_context>
@/home/ubuntu-studio/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu-studio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-buses-validator-gates/25-RESEARCH.md
@.planning/phases/25-buses-validator-gates/25-PATTERNS.md
@.planning/phases/25-buses-validator-gates/25-01-SUMMARY.md

<interfaces>
<!-- Key CI patterns the executor needs. Extracted from existing workflow. -->

From .github/workflows/plugins.yml (SHA-pinning convention):
```yaml
uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
uses: actions/cache@1bd1e32a3bdc45362d1e726936510720a7c30a57 # v4.2.0
uses: actions/upload-artifact@b4b15b8c7c6ac21ea08fcf65892d2ee8f75cf882 # v4.4.3
uses: actions/download-artifact@fa0a91b85d4f404e444e00e005971372dc801d16 # v4.1.8
```

From .github/workflows/plugins.yml (env vars, lines 31-33):
```yaml
env:
  PLUGINVAL_VERSION: "v1.0.4"
  PLUGIN_ARTEFACTS_DIR: "build/src/plugin/spu94_plugin_artefacts/Release"
```

From .github/workflows/plugins.yml (per-OS conditional pattern):
```yaml
if: matrix.name == 'linux'
if: matrix.name == 'macos'
if: matrix.name == 'windows'
```

From .github/workflows/plugins.yml (existing pluginval smoke, lines 162-193):
```yaml
- name: Smoke (Linux plugin formats)
  if: matrix.name == 'linux'
  run: |
    set -e
    pluginval --validate-in-process --strictness-level 1 \
      "${PLUGIN_ARTEFACTS_DIR}/VST3/SPU-94.vst3"
```

From .github/workflows/plugins.yml (advisory job, lines 252-285):
```yaml
pluginval-early-warning:
    name: pluginval-strictness-7 (advisory)
    needs: build-plugins
    runs-on: ubuntu-22.04
    continue-on-error: true
```

From 25-RESEARCH.md (auval command):
```
auval -v aufx Sp94 Spu9
```

From 25-RESEARCH.md (lv2lint build-from-source):
```bash
sudo apt-get install -y meson ninja-build liblilv-dev libcurl4-openssl-dev
git clone --depth 1 https://gitlab.com/drobilla/lv2lint.git
cd lv2lint
meson setup build -Donline-tests=disabled -Delf-tests=disabled -Dx11-tests=disabled
ninja -C build
sudo ninja -C build install
```

From 25-RESEARCH.md (VST3 validator — pluginval at strictness >= 5 already invokes VST3 validator logic internally, per Open Question 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote pluginval to strictness-7 hard gate + add auval/lv2lint/VST3 validator steps</name>
  <files>.github/workflows/plugins.yml</files>
  <read_first>
    .github/workflows/plugins.yml (full file — understand existing structure, smoke steps, advisory job),
    .planning/phases/25-buses-validator-gates/25-RESEARCH.md (Validator Details section — auval command format, lv2lint build steps, VST3 validator, pluginval strictness-7 test map),
    .planning/phases/25-buses-validator-gates/25-PATTERNS.md (CI SHA-pinning convention, per-OS conditional pattern, artefacts dir env var)
  </read_first>
  <action>
    Modify .github/workflows/plugins.yml with these changes:

    A) PROMOTE pluginval smoke steps to strictness-7 hard gates.
    Replace the three existing "Smoke (Linux/macOS/Windows plugin formats)" steps. Change `--strictness-level 1` to `--strictness-level 7` and add `--timeout-ms 120000` to each pluginval invocation. Keep the same per-format coverage: Linux runs VST3 + LV2 + CLAP; macOS runs VST3 + AU + CLAP; Windows runs VST3 + CLAP. Rename the steps from "Smoke (...)" to "Validate (..., pluginval strictness-7)" for clarity. These are NOT continue-on-error — they fail the build (PLUG-42). Keep `set -e` at the top of each Linux/macOS script block.

    For xvfb: the Linux pluginval steps need `xvfb-run -a` prefix because pluginval may create a GUI. Move the "Install xvfb" step (currently at line 220) to BEFORE the validation steps (currently it comes after the smoke steps, which is a bug in the existing workflow — it should be before). Actually, looking at the workflow, the xvfb install is for the standalone smoke. For pluginval, wrap each Linux pluginval call with `xvfb-run -a`.

    B) ADD auval step (macOS only, PLUG-38).
    Add a new step after the macOS pluginval validation:
    - Name: "Validate AU (auval)"
    - Condition: `if: matrix.name == 'macos'`
    - Command: `auval -v aufx Sp94 Spu9`
    - No continue-on-error (PLUG-42)
    auval is pre-installed on macOS runners; no installation step needed.

    C) ADD lv2lint + sord_validate step (Linux only, PLUG-39).
    Add two new steps for Linux:

    Step C1 — Install lv2lint + sordi:
    - Name: "Install lv2lint + sordi (Linux)"
    - Condition: `if: matrix.name == 'linux'`
    - Commands: install sordi from apt (`sudo apt-get install -y sordi`). For lv2lint: build from source — `git clone --depth 1 https://gitlab.com/drobilla/lv2lint.git /tmp/lv2lint`, then `cd /tmp/lv2lint && meson setup build -Donline-tests=disabled -Delf-tests=disabled -Dx11-tests=disabled && ninja -C build && sudo ninja -C build install`.
    - Add required build deps to the existing "Install Linux deps" step: append `meson liblilv-dev libcurl4-openssl-dev sordi` to the apt-get install list.

    Step C2 — Run lv2lint + sord_validate:
    - Name: "Validate LV2 (lv2lint + sord_validate)"
    - Condition: `if: matrix.name == 'linux'`
    - Commands:
      ```
      set -e
      sord_validate "${PLUGIN_ARTEFACTS_DIR}/LV2/SPU-94.lv2"/*.ttl
      lv2lint -I "${PLUGIN_ARTEFACTS_DIR}/LV2/SPU-94.lv2" \
        -M nopack \
        "https://spu94project.org/spu94"
      ```
    - No continue-on-error (PLUG-42)

    D) HANDLE VST3 SDK validator (PLUG-40).
    Per RESEARCH.md Open Question 1: pluginval at strictness >= 5 already invokes VST3 validator logic internally. Building the standalone Steinberg validator from source is expensive (full SDK compile) and may be redundant. The RESEARCH.md recommendation is to implement it but fall back to pluginval's built-in VST3 validation if too expensive.

    Pragmatic approach: pluginval strictness-7 (already promoted in step A) includes the "vst3 validator" test at strictness level 5+, which calls the Steinberg validation logic with the `-e` (extensive) flag. This satisfies the intent of PLUG-40 (VST3 conformance checking). Add a CI comment documenting this coverage: "# PLUG-40: VST3 conformance covered by pluginval strictness-7 (includes Steinberg validator tests at level 5+)."

    If the executor discovers that pluginval's built-in VST3 validation is insufficient (e.g., pluginval logs don't show VST3 validator tests running), THEN add a standalone Steinberg validator build step. But start with the pluginval coverage.

    E) REMOVE the advisory pluginval-early-warning job (lines 252-285).
    The entire `pluginval-early-warning` job block is now redundant — strictness-7 is the hard gate in the main build job. Delete it entirely.

    F) ENSURE no continue-on-error on any validator step (PLUG-42).
    Verify that none of the new or promoted steps have `continue-on-error: true`. The build must fail on any validator warning or error.

    G) KEEP the standalone smoke steps unchanged. They are not validator gates — they're a build-and-launch test for the dev testbed.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && grep -c "strictness-level 7" .github/workflows/plugins.yml && grep -c "auval" .github/workflows/plugins.yml && grep -c "lv2lint" .github/workflows/plugins.yml && grep -c "continue-on-error" .github/workflows/plugins.yml && echo "--- advisory job check ---" && grep -c "pluginval-early-warning" .github/workflows/plugins.yml || echo "advisory job removed (expected 0)"</automated>
  </verify>
  <acceptance_criteria>
    - plugins.yml contains `--strictness-level 7` in all pluginval invocations (no more `--strictness-level 1` except none remain)
    - plugins.yml contains zero occurrences of `--strictness-level 1` (all promoted to 7)
    - plugins.yml contains `auval -v aufx Sp94 Spu9` in a macOS-only step without continue-on-error
    - plugins.yml contains `lv2lint` and `sord_validate` in Linux-only steps without continue-on-error
    - plugins.yml contains a comment referencing PLUG-40 documenting VST3 validator coverage via pluginval
    - plugins.yml does NOT contain the `pluginval-early-warning` job (removed entirely)
    - plugins.yml contains zero occurrences of `continue-on-error: true`
    - The "Install Linux deps" step includes `meson liblilv-dev libcurl4-openssl-dev sordi` in the apt install list
    - plugins.yml contains a step that builds lv2lint from source on Linux
    - All third-party actions remain SHA-pinned (no tag-only references)
    - Standalone smoke steps are unchanged
  </acceptance_criteria>
  <done>pluginval promoted to strictness-7 hard gate on all formats on all OSes. auval gate added for macOS. lv2lint + sord_validate gate added for Linux LV2. VST3 conformance covered by pluginval's built-in strictness-5+ VST3 validator tests (documented). Advisory job removed. Zero continue-on-error on any validator step. Any warning or error fails the CI build.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Four categories of plugin validators wired as hard CI gates in plugins.yml:
    1. pluginval strictness-7 on every format on every OS (replaces strictness-1 smoke)
    2. auval on macOS AU builds
    3. lv2lint + sord_validate on Linux LV2 builds
    4. VST3 conformance via pluginval's built-in validator tests

    The advisory pluginval-early-warning job has been removed.
    Any validator warning or error now fails the CI build.
  </what-built>
  <how-to-verify>
    1. Push the branch to GitHub (or trigger a CI run manually)
    2. Watch the "plugins" workflow run on all three OS runners
    3. Verify these steps appear and pass:
       - "Validate (Linux, pluginval strictness-7)" with VST3, LV2, CLAP
       - "Validate (macOS, pluginval strictness-7)" with VST3, AU, CLAP
       - "Validate AU (auval)" on macOS
       - "Validate LV2 (lv2lint + sord_validate)" on Linux
       - "Validate (Windows, pluginval strictness-7)" with VST3, CLAP
    4. Verify the old "pluginval-strictness-7 (advisory)" job is gone
    5. Verify no steps have "continue-on-error" badges

    Note: If CI isn't set up yet, review the plugins.yml diff for correctness. The full CI validation happens when you push.
  </how-to-verify>
  <resume-signal>Type "approved" if CI passes or the workflow diff looks correct, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> plugin binary | Validators load and stress-test the plugin binary; any crash or violation is caught |
| Plugin -> host contract | Validators verify the plugin honors format-specific interface contracts (AU, LV2, VST3, CLAP) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Tampering | CI workflow | mitigate | All third-party actions SHA-pinned; pluginval downloaded from pinned release tag |
| T-25-05 | Denial of Service | pluginval strictness-7 | accept | Timeout set to 120000ms; if validator hangs, CI step times out and fails (which is the correct behavior) |
| T-25-06 | Repudiation | Validator results | mitigate | CI logs are preserved; any failure blocks merge to main |
</threat_model>

<verification>
1. plugins.yml syntax valid: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/plugins.yml'))"`
2. No strictness-1 remaining: `grep -c "strictness-level 1" .github/workflows/plugins.yml` returns 0
3. Strictness-7 present: `grep -c "strictness-level 7" .github/workflows/plugins.yml` returns >= 3 (one per OS)
4. auval present: `grep -c "auval" .github/workflows/plugins.yml` returns >= 1
5. lv2lint present: `grep -c "lv2lint" .github/workflows/plugins.yml` returns >= 1
6. Advisory job removed: `grep -c "pluginval-early-warning" .github/workflows/plugins.yml` returns 0
7. No continue-on-error: `grep -c "continue-on-error" .github/workflows/plugins.yml` returns 0
</verification>

<success_criteria>
- pluginval runs at strictness-7 as a hard gate on all formats on all 3 OSes
- auval runs as a hard gate on macOS
- lv2lint + sord_validate run as hard gates on Linux
- VST3 conformance verified (via pluginval's built-in tests or standalone validator)
- Advisory job removed
- Zero continue-on-error directives in the workflow
- CI would fail on any validator warning or error
</success_criteria>

<output>
After completion, create `.planning/phases/25-buses-validator-gates/25-02-SUMMARY.md`
</output>
