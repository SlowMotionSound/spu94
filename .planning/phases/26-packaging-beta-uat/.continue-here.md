---
context: phase
phase: 26-packaging-beta-uat
task: 0
total_tasks: 4
status: planned
last_updated: 2026-05-13T17:59:23Z
---

<current_state>
Phase 26 (Packaging & Beta UAT) is fully planned with 2 plans in 1 wave, ready to execute. Both plans run in parallel with zero file overlap. Phase 25 was closed out this session with 4 code review fixes and a CI config bugfix (pluginval format support).
</current_state>

<completed_work>

This session:
- Phase 25 close-out: approved CI checkpoint, fixed 4 code review findings (CR-01 NaN/Inf sanitization, CR-02 thread race, WR-01 LookAndFeel pointer, WR-03 dual source of truth), fixed CI pluginval format support bug (LV2/CLAP removed from pluginval steps)
- Verified pluginval strictness-7 passes locally on VST3
- Fixed stale Phase 22 roadmap tracking
- Phase 26 context gathered: 10 DAW-OS matrix, macOS .pkg+.dmg, load+audio+state UAT checklist
- Phase 26 planned: 2 plans, verified by plan checker (1 advisory warning accepted)
</completed_work>

<remaining_work>

- Plan 26-01 Task 1: Lock plugin window to fixed size (setResizable false)
- Plan 26-01 Task 2: Write BETA-README.md (cache-reset per-DAW, unsigned binary workarounds, install paths)
- Plan 26-02 Task 1: Create packaging scripts (macOS create_pkg.sh + create_dmg.sh, Windows spu94.iss, Linux install.sh)
- Plan 26-02 Task 2: Create release.yml CI workflow (tag-triggered GitHub Release with per-OS artifacts)
- After execution: manual UAT on Linux/macOS, then ship to beta testers
</remaining_work>

<decisions_made>

- 10 DAW-OS test combinations: Reaper (3), Ardour (Linux), Logic (macOS), Ableton (2), FL Studio (2), Bitwig (Linux)
- Pro Tools coverage via VST3 wrapper (tester-side, not a build target)
- macOS ships BOTH .pkg installer AND .dmg drag-install
- UAT scope: load + audio + state round-trip (not full feature sweep)
- Anthony tests Linux/macOS personally; beta testers cover macOS DAWs + Windows
- pluginval v1.0.4 only supports VST3+AU — CI config fixed accordingly
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/phases/26-packaging-beta-uat/26-CONTEXT.md` — decisions driving the packaging phase
2. `.planning/phases/26-packaging-beta-uat/26-01-PLAN.md` — fixed-size window + beta README plan
3. `.planning/phases/26-packaging-beta-uat/26-02-PLAN.md` — packaging scripts + release CI plan
4. `.planning/milestones/v1.7-REQUIREMENTS.md` §"Packaging & Distribution" — PLUG-43..48

## Infrastructure State
- Build is Release, all plugin formats compile (VST3, AU, LV2, CLAP, Standalone)
- pluginval installed at ~/bin/pluginval (v1.0.4)
- All 5 plugin tests green (bus_layout, mono_sum, state_serializer, state_roundtrip, null_passthrough)
- 7 pre-existing test failures unrelated to plugin work (CLI, packaging, golden, RT bench)

<context>
Phase 26 is the last required phase before v1.7 ships (Phase 27 Code Signing is conditional). After Phase 26 execution, Anthony will test on Linux and macOS personally, then ship to beta testers. The packaging work is straightforward scripting — the CI already builds all 11 binaries and uploads artifacts.
</context>

<next_action>
Resume with `/gsd-resume-work`. Execute Phase 26: `/gsd-execute-phase 26`
</next_action>
