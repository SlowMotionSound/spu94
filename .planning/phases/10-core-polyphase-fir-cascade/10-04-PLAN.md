---
phase: 10-core-polyphase-fir-cascade
plan: 04
type: execute
wave: 3
depends_on: [10-01, 10-03]
files_modified:
  - tests/golden/
autonomous: true
requirements: [INT-04]

must_haves:
  truths:
    - "DAC golden files are regenerated with the 8x cascade output"
    - "Conformance test passes with 135 .wav + 135 .sha256 files present"
    - "Audio-band frequency response of 8x output matches v1.2 within 0.01dB (INT-04)"
  artifacts:
    - path: "tests/golden/"
      provides: "Regenerated DAC golden files reflecting 8x cascade"
  key_links:
    - from: "scripts/regenerate_goldens.py"
      to: "spu94 CLI binary"
      via: "subprocess call with --dac flag"
      pattern: "spu94.*reverb.*--dac"
    - from: "tests/conformance/test_goldens_present.py"
      to: "tests/golden/"
      via: "SHA-256 sidecar validation"
      pattern: "sha256"
---

<objective>
Regenerate DAC golden files with the 8x cascade and verify passband conformance (INT-04).

Purpose: With the 8x cascade wired in and non-DAC identity proven, the DAC golden files must be regenerated to reflect the new algorithm's output. INT-04 requires the audio-band frequency response to match v1.2 within 0.01dB -- the scipy prototype from Plan 01 provides the passband comparison tool.

Output: 55 regenerated DAC golden .wav + .sha256 files in `tests/golden/`; passing conformance test; verified passband match.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-polyphase-fir-cascade/10-CONTEXT.md
@.planning/phases/10-core-polyphase-fir-cascade/10-RESEARCH.md

@.planning/phases/10-core-polyphase-fir-cascade/10-01-SUMMARY.md
@.planning/phases/10-core-polyphase-fir-cascade/10-03-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs. -->

From scripts/regenerate_goldens.py (golden regeneration commands):
```
python3 scripts/regenerate_goldens.py --dac            # 50 full-pipeline DAC goldens
python3 scripts/regenerate_goldens.py --dac-isolated   # 5 isolated DAC-only goldens
```

From tests/conformance/test_goldens_present.py (conformance gate):
```
pytest tests/conformance/test_goldens_present.py -x
# Validates: 135 .wav + 135 .sha256 present, spot-checks SHA-256 matches
```

From tools/dac_filter_design.py (passband verification from Plan 01):
```
python3 tools/dac_filter_design.py --verify-8x
# Validates: passband deviation < 0.01dB, impulse response checks, DC gain
```

Golden file inventory (from 10-RESEARCH.md):
- 50 full-pipeline DAC: tests/golden/<preset>/dac/<input>.wav (10 presets x 5 inputs)
- 5 isolated DAC: tests/golden/dac_isolated/<input>.wav
- Total DAC: 55 .wav + 55 .sha256 = 110 files to regenerate
- Non-DAC: 80 .wav + 80 .sha256 (already verified bit-identical in Plan 03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate DAC golden files</name>
  <files>tests/golden/</files>
  <read_first>
    - scripts/regenerate_goldens.py (understand the --dac and --dac-isolated flags, output paths, SHA-256 sidecar generation)
    - tests/golden/ (directory listing to understand current structure)
  </read_first>
  <action>
Regenerate all 55 DAC golden files with the new 8x cascade:

1. Build the CLI binary to ensure it includes the 8x wiring:
   ```bash
   cd "/home/ubuntu-studio/Desktop/PSX Reverb/build" && cmake --build .
   ```

2. Regenerate 50 full-pipeline DAC goldens (10 presets x 5 inputs):
   ```bash
   cd "/home/ubuntu-studio/Desktop/PSX Reverb"
   python3 scripts/regenerate_goldens.py --dac
   ```
   This produces `tests/golden/<preset>/dac/<input>.wav` + `.sha256` for each combination.

3. Regenerate 5 isolated DAC-only goldens:
   ```bash
   python3 scripts/regenerate_goldens.py --dac-isolated
   ```
   This produces `tests/golden/dac_isolated/<input>.wav` + `.sha256`.

4. Verify structural completeness: the golden directory should contain exactly 135 .wav files and 135 .sha256 sidecars (50 reverb + 30 ADPCM + 50 DAC + 5 DAC-isolated = 135).

5. Run the conformance test:
   ```bash
   cd "/home/ubuntu-studio/Desktop/PSX Reverb"
   pytest tests/conformance/test_goldens_present.py -x
   ```
   This validates file presence and SHA-256 sidecar correctness.

The DAC goldens WILL have different SHA-256 hashes compared to v1.2 -- this is expected because the 8x cascade produces mathematically different output than the v1.2 single-rate approximation. The v1.2 originals are safely archived in `tests/golden_v1.2/` (Plan 01).
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && python3 scripts/regenerate_goldens.py --dac 2>&1 | tail -3 && python3 scripts/regenerate_goldens.py --dac-isolated 2>&1 | tail -3 && find tests/golden -name "*.wav" | wc -l | grep -q "135" && find tests/golden -name "*.sha256" | wc -l | grep -q "135" && pytest tests/conformance/test_goldens_present.py -x 2>&1 | tail -5 && echo "GOLDEN REGEN PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `find tests/golden -name "*.wav" | wc -l` outputs `135`
    - `find tests/golden -name "*.sha256" | wc -l` outputs at least `135`
    - `pytest tests/conformance/test_goldens_present.py -x` exits with code 0
    - DAC golden .sha256 sidecars differ from archived v1.2 versions in `tests/golden_v1.2/` (confirming the 8x cascade produces different output)
  </acceptance_criteria>
  <done>55 DAC golden files regenerated with 8x cascade output. Conformance test passes with 135 .wav + sidecars. DAC goldens reflect the true oversampled interpolation.</done>
</task>

<task type="auto">
  <name>Task 2: Verify passband conformance (INT-04)</name>
  <files></files>
  <read_first>
    - tools/dac_filter_design.py (understand --verify-8x output format and what it checks)
  </read_first>
  <action>
This is a verification-only task. Run the scipy passband comparison to prove INT-04: audio-band frequency response matches v1.2 within 0.01dB across 20Hz-20kHz.

1. Run the 8x verification:
   ```bash
   cd "/home/ubuntu-studio/Desktop/PSX Reverb"
   python3 tools/dac_filter_design.py --verify-8x
   ```
   This runs the scipy 8x cascade simulation and checks passband deviation against the analytical composite.

2. Also run the original verification to confirm it still passes:
   ```bash
   python3 tools/dac_filter_design.py --verify
   ```

3. Run the full C test suite to confirm everything passes end-to-end:
   ```bash
   cd build && ctest --output-on-failure
   ```

INT-04 is satisfied when `--verify-8x` reports passband deviation less than 0.01dB across 20Hz-20kHz. The same coefficients produce the same passband behavior whether run at 44.1kHz (v1.2) or at the true rates (v1.3) -- this is guaranteed by DSP theory and confirmed empirically.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && python3 tools/dac_filter_design.py --verify-8x 2>&1 && python3 tools/dac_filter_design.py --verify 2>&1 | tail -5 && cd build && ctest --output-on-failure 2>&1 | tail -10 && echo "INT-04 + FULL SUITE PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/dac_filter_design.py --verify-8x` exits with code 0
    - Output from `--verify-8x` shows passband deviation less than 0.01 dB
    - `python3 tools/dac_filter_design.py --verify` exits with code 0 (v1.2 verification unchanged)
    - `ctest --output-on-failure` exits with code 0 (full C test suite passes)
  </acceptance_criteria>
  <done>INT-04 verified: audio-band frequency response of the 8x cascade matches v1.2 within 0.01dB. Full C test suite passes. Both scipy verification modes pass. Phase 10 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Golden file regeneration | CLI binary processes deterministic input; no external untrusted data |
| Scipy verification | Runs on locally-generated signals; no network access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | Golden file integrity during regeneration | mitigate | SHA-256 sidecars generated atomically alongside .wav files; conformance test validates pair integrity |
| T-10-08 | Repudiation | INT-04 passband claim | mitigate | Scipy --verify-8x produces numerical measurement, not subjective assessment; automated exit code gates the claim |
</threat_model>

<verification>
1. 135 golden .wav + sidecars present and conformance test passes
2. `--verify-8x` reports passband within 0.01dB (INT-04)
3. `--verify` original mode still passes
4. Full `ctest --output-on-failure` passes
</verification>

<success_criteria>
- DAC golden files reflect true 8x oversampled cascade output
- Conformance gate passes (135 .wav + 135 .sha256)
- INT-04 passband conformance verified within 0.01dB
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-polyphase-fir-cascade/10-04-SUMMARY.md`
</output>
