---
phase: 44-tremolo
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - src/plugin/PluginProcessor.h
  - src/plugin/PluginProcessor.cpp
  - src/plugin/PluginEditor.h
  - src/plugin/PluginEditor.cpp
  - src/plugin/SamplerWindow.h
autonomous: false
requirements: [TREM-01, TREM-02, TREM-03, TREM-04, TREM-05, TREM-06]

must_haves:
  truths:
    - "Tremolo Enable toggle in the sampler window activates periodic volume pulsing on voice 0 (TREM-01, TREM-06)"
    - "Speed knob labeled in Hz controls the oscillation rate from 0.5 to 19 Hz (TREM-02, TREM-06)"
    - "Depth knob 0-100% controls how much the volume dips on each cycle (TREM-03, TREM-06)"
    - "Curve toggle switches between Linear (triangle) and Exponential (Uni-Vibe) character (TREM-04, TREM-06)"
    - "Ratio control allows L/R rate divergence for polyrhythmic stereo drift (TREM-05, TREM-06)"
  artifacts:
    - path: "src/plugin/PluginProcessor.h"
      provides: "Tremolo atomic control variables and accessor methods"
      contains: "tremoloEnabled"
    - path: "src/plugin/PluginProcessor.cpp"
      provides: "Audio-thread tremolo activation using spu94_tremolo_configure and spu94_tremolo_apply_depth"
      contains: "spu94_tremolo_configure"
    - path: "src/plugin/PluginEditor.h"
      provides: "Tremolo GUI widget declarations"
      contains: "tremoloSpeedKnob"
    - path: "src/plugin/PluginEditor.cpp"
      provides: "Tremolo section layout and callbacks in sampler window panel"
      contains: "Tremolo"
    - path: "src/plugin/SamplerWindow.h"
      provides: "Window height increased to accommodate tremolo section"
      contains: "870"
  key_links:
    - from: "src/plugin/PluginProcessor.cpp"
      to: "spu94_tremolo_configure"
      via: "include spu94/spu94_tremolo.h and call in processBlock"
      pattern: "spu94_tremolo_configure"
    - from: "src/plugin/PluginProcessor.cpp"
      to: "spu94_tremolo_apply_depth"
      via: "per-tick depth application reading sweep_l/r levels"
      pattern: "spu94_tremolo_apply_depth"
    - from: "src/plugin/PluginEditor.cpp"
      to: "src/plugin/PluginProcessor.h"
      via: "accessor methods for tremolo atomics"
      pattern: "getTremoloSpeed\\|getTremoloDepth"
---

<objective>
Wire tremolo controls into the sampler GUI and audio thread: Speed knob (Hz), Depth knob (0-100%), Curve toggle (Linear/Exponential), L/R Ratio control, and Enable toggle. The audio thread uses Plan 01's spu94_tremolo_configure and spu94_tremolo_apply_depth to produce audible periodic volume pulsing.

Purpose: Makes tremolo audible and controllable by the user (TREM-06), completing all 6 tremolo requirements. The user sees labeled controls in the sampler window and hears the effect immediately.

Output: Functional tremolo effect with 5 GUI controls in a dedicated "Tremolo" section of the sampler window.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-tremolo/44-01-SUMMARY.md

<interfaces>
From include/spu94/spu94_tremolo.h (created in Plan 01):
- spu94_tremolo_shift_t spu94_tremolo_hz_to_shift(float hz)
- void spu94_tremolo_configure(spu94_voice_mixer_t *m, int voice_idx, uint8_t mode, uint8_t shift, uint8_t step, uint8_t ratio_num, uint8_t ratio_den)
- int16_t spu94_tremolo_apply_depth(int16_t sweep_level, int16_t base_vol, uint8_t depth)

From src/plugin/PluginProcessor.h (existing pattern for atomics):
- std::atomic<int> rampDirection{0}; std::atomic<float> rampSpeed{1.7f}; etc.
- Accessor pattern: std::atomic<T>& getXxx() { return xxx; }
- Audio thread reads atomics with memory_order_relaxed in processBlock

From src/plugin/PluginEditor.h (existing widget pattern):
- juce::Slider rampSpeedKnob; juce::TextButton rampCurveButton{"Linear"}; etc.
- Layout in resized() with constexpr int offsets

From src/plugin/PluginEditor.cpp (VCA ramp section as template, lines 430-489):
- Section label with bold font, teal/coral colored buttons, rotary knobs with text below
- Callbacks store to processor atomics with memory_order_relaxed
- Layout uses absolute positioning within the sampler panel

From src/plugin/SamplerWindow.h:
- Current size: 400x810. contentPanel.setSize(400, 810); centreWithSize(400, 810)
- ensureMinimumSize checks 400x800

From src/plugin/PluginProcessor.cpp (audio thread pattern, lines 661-679):
- VCA ramp uses atomic exchange (rampArm) as one-shot trigger
- Tremolo differs: it is continuous, enabled/disabled via a boolean atomic
- When tremoloEnabled is true, every processBlock iteration applies depth to vol_l/vol_r
- The tremolo reconfigures sweeps only when parameters change (not every block)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tremolo atomics to PluginProcessor and audio-thread logic</name>
  <files>src/plugin/PluginProcessor.h, src/plugin/PluginProcessor.cpp</files>
  <read_first>
    - src/plugin/PluginProcessor.h (lines 75-110 for accessor pattern; lines 390-407 for atomic declarations)
    - src/plugin/PluginProcessor.cpp (lines 0-49 for speedToShift and includes; lines 660-680 for VCA ramp audio-thread pattern)
    - include/spu94/spu94_tremolo.h (the API from Plan 01)
    - include/spu94/spu94_voice.h (mixer struct, voice access pattern)
  </read_first>
  <action>
    In PluginProcessor.h, add tremolo control atomics after the VCA ramp section (after line 406):
    - std::atomic bool tremoloEnabled (default false)
    - std::atomic float tremoloSpeed (default 4.0f, in Hz)
    - std::atomic int tremoloDepth (default 128, range 0-255, maps to 0-100% in GUI)
    - std::atomic int tremoloCurve (default 0, 0=Linear 1=Exponential)
    - std::atomic int tremoloRatioNum (default 1)
    - std::atomic int tremoloRatioDen (default 1)
    - std::atomic bool tremoloParamsChanged (default false, flags reconfigure)

    Add accessor methods in the public section (after getRampArm, around line 86):
    - getTremoloEnabled, getTremoloSpeed, getTremoloDepth, getTremoloCurve, getTremoloRatioNum, getTremoloRatioDen, getTremoloParamsChanged

    In PluginProcessor.cpp, add include for spu94/spu94_tremolo.h at the top.

    In processBlock (after the VCA ramp arm block, after line 680), add tremolo logic:
    1. Check tremoloParamsChanged.exchange(false). If true OR if tremoloEnabled just turned on:
       - Read speed, curve, ratioNum, ratioDen atomics
       - Call spu94_tremolo_hz_to_shift(speed) to get shift/step
       - Call spu94_tremolo_configure(mixer, 0, curve, shift, step, ratioNum, ratioDen)
       - Store a local bool indicating tremolo is active
    2. If tremoloEnabled is false and was previously true: deactivate sweeps by setting sweep_l.active=0 and sweep_r.active=0 on voice 0, restore vol_l/vol_r to their base values.
    3. The depth application happens per-sample in the voice tick output. Since the sweep modifies vol_l/vol_r directly (sweep.level IS the volume), we need a different approach: store the "base volume" (the volume the user set) separately, and after each mixer tick, compute the effective volume using spu94_tremolo_apply_depth.

    Implementation detail for depth: The sweep oscillates the sweep_l.level and sweep_r.level fields between 0 and 0x7FFF via retrigger. The depth scaling reads these levels each audio callback and computes effective volume for the voice output. Since the mixer already applies vol_l/vol_r to the voice output, the cleanest path is:
    - When tremolo is enabled, set sweep_l/r level to 0x7FFF initially (start at peak, direction=decrease means it ramps down first)
    - Store base_vol_l and base_vol_r as the user's intended volume
    - Each processBlock, read sweep_l.level and sweep_r.level, apply depth, write result to voices[0].vol_l and voices[0].vol_r
    - This means tremolo overrides the per-sample volume with depth-modulated volume

    Add two new atomic int16_t members: tremoloBaseVolL and tremoloBaseVolR (snapshot of user volume when tremolo enables). When tremolo enables, snapshot current vol_l/vol_r. Each block: voices[0].vol_l = spu94_tremolo_apply_depth(sweep_l.level, baseVolL, depth).

    The per-block application is acceptable because at 44100/buffer_size iterations per second, the modulation updates at control rate which is fine for tremolo speeds up to 19 Hz (audio-rate AM is Phase 48's concern and will need per-sample in the mixer tick itself).

    Actually, the sweep ticks happen per-sample inside spu94_voice_mixer_tick. The sweep modifies sweep_l.level every sample. But vol_l/vol_r are what get applied to the voice output in spu94_voice_tick. So the approach is: after mixer_tick returns, update vol from sweep levels. But wait -- the sweep level IS separate from vol_l in the current architecture. Let me re-read...

    Looking at spu94_voice.h: vol_l and vol_r are separate fields from sweep_l and sweep_r. The sweep tick modifies sweep_l.level, and spu94_voice_tick uses vol_l for output scaling. The existing Phase 41 VCA ramp code in PluginProcessor.cpp configures the sweeps but the voice tick already reads sweep.level and applies it -- actually checking spu94_voice.c is needed.

    Given this uncertainty: the executor must read src/spu94/spu94_voice.c to confirm how sweep.level relates to vol_l in the voice tick. If sweep.level replaces vol_l during tick (as the sweep header says "sweep modifies the volume register directly"), then tremolo via sweep works automatically -- the sweep IS the volume. In that case, depth must be implemented differently: instead of full-range oscillation + post-multiply, we set the sweep boundaries to achieve the depth effect.

    Simplest approach matching the architecture: When tremolo enabled, configure sweeps to oscillate. The sweep directly IS the volume. For depth: set initial level to base_vol (user volume), configure sweep direction=decrease with retrigger. The sweep will oscillate between 0 and whatever ceiling it reverses at (0x7FFF). For depth < 100%, limit the floor: pre-processBlock, if sweep_l.level drops below (base_vol * (255-depth) / 255), write that floor value back. This is hacky.

    Better: use the post-multiply approach. Each processBlock (control rate), read sweep_l.level (which oscillates 0 to 0x7FFF independently). Compute modulated_vol = spu94_tremolo_apply_depth(sweep_l.level, base_vol_l, depth). Write to voices[0].vol_l. Same for R. The sweep runs in "shadow" mode -- its level oscillates but it does NOT directly control the voice volume (we override vol every block). The sweep struct's level field acts as an LFO.

    This requires: on tremolo enable, configure sweep_l/r with level starting at 0x7FFF (will ramp down), direction=1 (decrease), retrigger_enable=1. Do NOT let the existing voice_tick path apply sweep to vol (check if it does). If voice_tick applies sweep.level to vol automatically, we need a way to prevent that (set sweep.active=0 after configure? No, that stops ticking). OR use a separate spu94_sweep_t instance not part of the voice struct. The cleanest solution: declare two spu94_sweep_t instances in PluginProcessor as shadow LFOs, tick them manually per processBlock sample.

    Final decision for executor: Declare two file-scope spu94_sweep_t instances (tremolo_lfo_l, tremolo_lfo_r) in PluginProcessor.cpp. Tick them manually (spu94_sweep_tick) once per sample in the processBlock loop. They oscillate independently of the voice's own sweep_l/sweep_r. Each block, apply depth from their levels to voices[0].vol_l/vol_r. This completely decouples tremolo from the voice's built-in sweep, avoiding conflicts with the VCA ramp controls.

    The approach:
    - Two static spu94_sweep_t members in the processor (tremLfoL, tremLfoR)
    - On tremolo param change: spu94_sweep_configure on tremLfoL and tremLfoR with retrigger_enable=1, direction=1, phase=0, the computed shift/step, level set to 0x7FFF
    - In processBlock per-sample loop (or once per buffer for control-rate): tick tremLfoL and tremLfoR via spu94_sweep_tick
    - After ticking: voices[0].vol_l = spu94_tremolo_apply_depth(tremLfoL.level, baseVolL, depth)
    - Since processBlock processes bufferSize samples at once, tick the LFOs bufferSize times per call, applying depth at the end of the buffer (control-rate update). For sub-20Hz tremolo this is fine.

    Actually for proper tremolo, per-sample volume modulation sounds better. But the voice_mixer_tick already processes all 24 voices per sample. We would need to inject volume modification INSIDE the per-sample loop. The processBlock loop calls spu94_voice_mixer_tick once per output sample. So: in the per-sample loop, BEFORE calling mixer_tick, tick tremolo LFOs and write modulated vol to voice 0. This gives sample-accurate tremolo.

    Summary for executor:
    1. Add two spu94_sweep_t members to SPU94AudioProcessor (tremLfoL, tremLfoR)
    2. Add tremoloBaseVolL/R (int16_t atomics, snapshot user vol on enable)
    3. On param change: configure tremLfoL/R with spu94_sweep_configure
    4. In per-sample loop in processBlock, when tremolo enabled: spu94_sweep_tick both LFOs, then write depth-modulated vol to mixer voices[0].vol_l/vol_r
    5. On tremolo disable: restore voices[0].vol_l/vol_r from base snapshot
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb && cmake --build build --target SPU94 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PluginProcessor.h declares tremoloEnabled, tremoloSpeed, tremoloDepth, tremoloCurve, tremoloRatioNum, tremoloRatioDen atomics
    - PluginProcessor.h declares getTremoloEnabled/Speed/Depth/Curve/RatioNum/RatioDen accessor methods
    - PluginProcessor.cpp includes spu94/spu94_tremolo.h
    - PluginProcessor.cpp has tremolo LFO tick in the per-sample loop (spu94_sweep_tick called on tremLfoL/R)
    - PluginProcessor.cpp applies spu94_tremolo_apply_depth to voices[0].vol_l/vol_r when tremolo enabled
    - Full project builds with zero errors
  </acceptance_criteria>
  <done>Audio thread produces tremolo modulation when tremoloEnabled=true; builds clean</done>
</task>

<task type="auto">
  <name>Task 2: Add tremolo GUI section to sampler window</name>
  <files>src/plugin/PluginEditor.h, src/plugin/PluginEditor.cpp, src/plugin/SamplerWindow.h</files>
  <read_first>
    - src/plugin/PluginEditor.h (lines 130-170 for widget declarations pattern)
    - src/plugin/PluginEditor.cpp (lines 430-489 for VCA ramp section setup as template; lines 1159-1167 for VCA ramp layout)
    - src/plugin/SamplerWindow.h (current 400x810 size)
    - src/plugin/PluginProcessor.h (the tremolo accessors added in Task 1)
  </read_first>
  <action>
    In SamplerWindow.h: increase window height from 810 to 880 (60px for tremolo section). Update contentPanel.setSize(400, 880), centreWithSize(400, 880), and ensureMinimumSize threshold to 870.

    In PluginEditor.h: add tremolo widget member declarations after the VCA ramp widgets (after rampArmButton line 149):
    - juce::Label tremoloSectionLabel
    - juce::ToggleButton tremoloEnableToggle with text "Tremolo"
    - juce::Slider tremoloSpeedKnob (rotary, 0.5-19.0 Hz range)
    - juce::Label tremoloSpeedLabel
    - juce::Slider tremoloDepthKnob (rotary, 0-100 range representing percent)
    - juce::Label tremoloDepthLabel
    - juce::TextButton tremoloCurveButton with text "Linear"
    - juce::Slider tremoloRatioKnob (rotary, 0.5-2.0 range for L:R ratio)
    - juce::Label tremoloRatioLabel

    In PluginEditor.cpp constructor (after VCA ramp setup, after line 489 area):
    Add tremolo section setup following the exact same pattern as VCA ramp:
    - tremoloSectionLabel: "Tremolo" text, same font/color as rampSectionLabel (0xFFD0D0D0, 11pt bold)
    - tremoloEnableToggle: onClick stores to processorRef.getTremoloEnabled() and sets tremoloParamsChanged
    - tremoloSpeedKnob: Rotary, TextBoxBelow, range 0.5-19.0 Hz, step 0.1, default 4.0, suffix " Hz", skew from midpoint 4.0. onValueChange stores to processorRef.getTremoloSpeed() and sets paramsChanged
    - tremoloDepthKnob: Rotary, TextBoxBelow, range 0-100, step 1, default 50, suffix "%". onValueChange stores (value * 255 / 100) to processorRef.getTremoloDepth() and sets paramsChanged
    - tremoloCurveButton: toggles "Linear"/"Exponential" like rampCurveButton. onClick stores 0/1 to processorRef.getTremoloCurve() and sets paramsChanged
    - tremoloRatioKnob: Rotary, TextBoxBelow, range 0.5-2.0, step 0.1, default 1.0, suffix " L:R". onValueChange computes ratio_num/ratio_den (ratio >= 1: num=round(ratio*10), den=10; ratio < 1: num=10, den=round(10/ratio)) stores to processorRef.getTremoloRatioNum/Den and sets paramsChanged
    - All widgets added to panel via panel.addAndMakeVisible

    In PluginEditor.cpp resized() (after VCA ramp layout, after line 1167):
    Add tremolo section layout below VCA ramp. Use constexpr int tremY = rampy + 100 (below VCA ramp section). Layout:
    - tremoloSectionLabel at (20, tremY, 80, 16)
    - tremoloEnableToggle at (100, tremY, 80, 20)
    - tremoloSpeedLabel at (20, tremY+20, 60, 16)
    - tremoloSpeedKnob at (20, tremY+36, 80, 70)
    - tremoloDepthLabel at (110, tremY+20, 60, 16)
    - tremoloDepthKnob at (110, tremY+36, 80, 70)
    - tremoloCurveButton at (200, tremY+36, 80, 28)
    - tremoloRatioLabel at (290, tremY+20, 60, 16)
    - tremoloRatioKnob at (290, tremY+36, 80, 70)

    Color scheme: use teal (0xFF3CBBB1) for the enable toggle when active, follow PS1 color palette.
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb && cmake --build build --target SPU94 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SamplerWindow.h has height 880 (not 810)
    - PluginEditor.h declares tremoloEnableToggle, tremoloSpeedKnob, tremoloDepthKnob, tremoloCurveButton, tremoloRatioKnob
    - PluginEditor.cpp has "Tremolo" section label text
    - Speed knob range is 0.5-19.0 with " Hz" suffix
    - Depth knob range is 0-100 with "%" suffix
    - Ratio knob range is 0.5-2.0
    - All tremolo widgets are addAndMakeVisible on the panel
    - All callbacks store to processor tremolo atomics
    - Full project builds with zero errors
  </acceptance_criteria>
  <done>Tremolo section visible in sampler window with Speed, Depth, Curve, Ratio, and Enable controls; builds clean</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify tremolo is audible and controls work</name>
  <what-built>
    Tremolo effect: periodic volume pulsing on the sampler voice, controlled by Speed (Hz), Depth (%), Curve (Linear/Exponential), and L/R Ratio knobs in a new "Tremolo" section of the sampler window.
  </what-built>
  <how-to-verify>
    1. Launch the standalone SPU-94 application (Release build)
    2. Load a sustained WAV sample and play it (hold a note or enable loop)
    3. In the sampler window, find the new "Tremolo" section below the VCA Ramp section
    4. Enable the "Tremolo" toggle -- you should hear periodic volume pulsing
    5. Turn the Speed knob: at 0.5 Hz the pulsing is slow (one dip every 2 seconds); at 10+ Hz it becomes a fast flutter
    6. Turn the Depth knob: at 0% there should be no audible modulation; at 100% the volume should dip to silence on each cycle
    7. Toggle the Curve button between Linear and Exponential: Linear should sound like symmetric triangle-wave tremolo; Exponential should have a faster attack and slower recovery (Uni-Vibe character)
    8. Turn the Ratio knob away from 1.0 (e.g., 2.0): you should hear the left and right channels pulsing at different rates, creating stereo drift/movement
    9. Disable the "Tremolo" toggle -- volume should return to normal immediately
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the tremolo behavior</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI knobs -> processor atomics | User-facing float/int values cross into audio thread |
| Processor atomics -> C core | Tremolo params forwarded to spu94_tremolo_configure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | Tampering | GUI knob ranges | mitigate | JUCE slider ranges enforce valid bounds (0.5-19 Hz, 0-100%, 0.5-2.0 ratio) |
| T-44-05 | DoS | Per-sample LFO tick | accept | Two sweep_tick calls per sample is negligible CPU (identical to existing sweep infrastructure, already proven RT-safe) |
| T-44-06 | Information Disclosure | N/A | accept | No user data involved; audio DSP only |
</threat_model>

<verification>
Build succeeds. Tremolo controls visible in sampler window. Enabling tremolo produces audible volume modulation. All 5 knobs/toggles function as labeled. Disabling tremolo restores original volume.
</verification>

<success_criteria>
- User can hear periodic volume pulsing when tremolo is enabled (TREM-01)
- Speed knob labeled in Hz controls rate from 0.5-19 Hz (TREM-02)
- Depth at 0% = no modulation, at 100% = full pulsing (TREM-03)
- Linear/Exponential toggle produces audibly different character (TREM-04)
- Ratio away from 1:1 produces stereo drift (TREM-05)
- All controls visible and labeled in sampler GUI (TREM-06)
</success_criteria>

<output>
Create `.planning/phases/44-tremolo/44-02-SUMMARY.md` when done
</output>
