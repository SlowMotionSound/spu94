---
phase: 44-tremolo
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - src/plugin/PluginEditor.h
  - src/plugin/PluginEditor.cpp
autonomous: false
requirements: [TREM-06]

must_haves:
  truths:
    - "Tremolo section is visible in the sampler window with labeled controls"
    - "Enable toggle starts/stops the volume pulsing"
    - "Speed knob is labeled in Hz and covers 0.5-19 Hz"
    - "Depth knob covers 0-100% with visible percentage"
    - "Curve toggle switches between Linear and Exponential"
    - "L/R Ratio selector offers 1:1 plus other ratios"
  artifacts:
    - path: "src/plugin/PluginEditor.h"
      provides: "Tremolo GUI component declarations (toggle, knobs, buttons, labels)"
    - path: "src/plugin/PluginEditor.cpp"
      provides: "Tremolo section layout, control wiring to PluginProcessor atomics"
  key_links:
    - from: "src/plugin/PluginEditor.cpp"
      to: "PluginProcessor tremolo atomics"
      via: "processorRef.getTremoloEnabled() etc"
      pattern: "getTremolo"
---

<objective>
Add the tremolo GUI controls to the sampler window.

Purpose: Users need visible, labeled controls to enable tremolo and adjust speed/depth/curve/ratio. This wires the GUI layer to the atomic state variables created in Plan 01.

Output: A "Tremolo" section in the sampler panel with enable toggle, speed knob (Hz), depth knob (%), curve toggle (Linear/Exponential), and L/R ratio control.
</objective>

<execution_context>
@.planning/phases/44-tremolo/44-01-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/plugin/PluginEditor.h
@src/plugin/PluginEditor.cpp
@src/plugin/PluginProcessor.h
@src/plugin/SamplerWindow.h

<interfaces>
From src/plugin/PluginProcessor.h (added by Plan 01):
  std::atomic<bool>&  getTremoloEnabled();
  std::atomic<float>& getTremoloSpeedHz();
  std::atomic<float>& getTremoloDepth();
  std::atomic<int>&   getTremoloCurve();
  std::atomic<float>& getTremoloRatio();

From src/plugin/SamplerWindow.h:
  - contentPanel is a juce::Component sized 400x810
  - Sampler window uses setContentNonOwned(&contentPanel, true)

From src/plugin/PluginEditor.cpp layout section (around line 1159-1167):
  - VCA Ramp section starts at constexpr int rampy = voly + 218
  - rampSectionLabel at (20, rampy)
  - rampArmButton at (300, rampy+20, 80, 36)
  - Total ramp section height is about 90px (label + buttons + speed knob)
  - Window height is 810px, so there is room below the ramp section
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tremolo GUI controls in sampler panel</name>
  <files>src/plugin/PluginEditor.h, src/plugin/PluginEditor.cpp</files>
  <read_first>
    - src/plugin/PluginEditor.h (full file -- understand member declarations pattern)
    - src/plugin/PluginEditor.cpp lines 420-500 (VCA Ramp section setup for style reference)
    - src/plugin/PluginEditor.cpp lines 1150-1170 (VCA Ramp layout bounds for positioning)
    - src/plugin/PluginProcessor.h lines 76-100 (getter pattern for atomics)
  </read_first>
  <action>
    In PluginEditor.h, add member declarations for the tremolo section controls:
    - juce::Label tremoloSectionLabel
    - juce::ToggleButton tremoloEnableToggle
    - juce::Slider tremoloSpeedKnob
    - juce::Label tremoloSpeedLabel
    - juce::Slider tremoloDepthKnob
    - juce::Label tremoloDepthLabel
    - juce::TextButton tremoloCurveButton
    - juce::Slider tremoloRatioKnob
    - juce::Label tremoloRatioLabel

    In PluginEditor.cpp constructor (the sampler panel setup block, same section as VCA Ramp):
    
    Section header label:
    - tremoloSectionLabel text "Tremolo", same style as rampSectionLabel (11pt bold, color 0xFFD0D0D0)
    - addAndMakeVisible on panel

    Enable toggle:
    - tremoloEnableToggle.setButtonText("Enable")
    - tremoloEnableToggle.setColour(juce::ToggleButton::tickColourId, juce::Colour(0xFF3CBBB1)) (teal tick)
    - onValueChange: store to processorRef.getTremoloEnabled()
    - addAndMakeVisible on panel

    Speed knob:
    - Rotary style, TextBoxBelow, range 0.5 to 19.0, step 0.1, default 5.0
    - Suffix " Hz", skew from midpoint 4.0 (cluster detail in musical 1-8 Hz range)
    - Double-click returns to 5.0
    - onValueChange: store float to processorRef.getTremoloSpeedHz()
    - tremoloSpeedLabel text "Speed", centred

    Depth knob:
    - Rotary style, TextBoxBelow, range 0.0 to 100.0, step 1.0, default 100.0
    - Suffix "%"
    - Double-click returns to 100.0
    - onValueChange: store value/100.0f to processorRef.getTremoloDepth()
    - tremoloDepthLabel text "Depth", centred

    Curve button:
    - Same toggle pattern as rampCurveButton: text starts "Linear", click toggles to "Exponential" and back
    - onValueChange: store 0 or 1 to processorRef.getTremoloCurve()

    Ratio knob:
    - Rotary style, TextBoxBelow, range 0.5 to 4.0, step 0.1, default 1.0
    - Custom text display showing "1:X" format (use textFromValueFunction lambda: format as "1:" + String(value, 1))
    - Double-click returns to 1.0
    - onValueChange: store float to processorRef.getTremoloRatio()
    - tremoloRatioLabel text "L/R Ratio", centred

    In the resized() function, sampler panel layout block:
    - Position tremolo section BELOW the VCA Ramp section
    - constexpr int tremy = rampy + 100 (100px below ramp section start gives clearance)
    - Layout similar to VCA Ramp pattern:
      - tremoloSectionLabel at (20, tremy, 120, 16)
      - tremoloEnableToggle at (140, tremy, 80, 20)
      - tremoloSpeedLabel at (20, tremy + 20, 80, 16)
      - tremoloSpeedKnob at (20, tremy + 36, 80, 70)
      - tremoloDepthLabel at (110, tremy + 20, 80, 16)
      - tremoloDepthKnob at (110, tremy + 36, 80, 70)
      - tremoloCurveButton at (200, tremy + 40, 90, 28)
      - tremoloRatioLabel at (300, tremy + 20, 80, 16)
      - tremoloRatioKnob at (300, tremy + 36, 80, 70)

    If the total sampler panel height needs to grow beyond 810 to accommodate the new section, increase the contentPanel size in SamplerWindow.h and the centreWithSize call accordingly. The tremolo section bottom edge is at approximately tremy + 106. Check whether this exceeds the 810 height (voly is around 360-400, rampy is voly+218 = ~578-618, tremy is rampy+100 = ~678-718, bottom of tremolo section is tremy+106 = ~784-824). If it exceeds 810, increase to 880.

    Use the PS1 color scheme: teal (0xFF3CBBB1) for active states, dark gray background inherits from panel.

    When the enable toggle is OFF, visually dim the other controls by setting their enabled state to false. Add logic in the onValueChange of the enable toggle: tremoloSpeedKnob.setEnabled(enabled), tremoloDepthKnob.setEnabled(enabled), tremoloCurveButton.setEnabled(enabled), tremoloRatioKnob.setEnabled(enabled).
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb && cmake --build build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Project builds clean with zero errors
    - grep -c "tremoloSectionLabel\|tremoloEnableToggle\|tremoloSpeedKnob\|tremoloDepthKnob\|tremoloCurveButton\|tremoloRatioKnob" src/plugin/PluginEditor.h returns 6 or more
    - grep -c "getTremoloEnabled\|getTremoloSpeedHz\|getTremoloDepth\|getTremoloCurve\|getTremoloRatio" src/plugin/PluginEditor.cpp returns 5 or more
    - grep "tremy" src/plugin/PluginEditor.cpp confirms layout positions exist
  </acceptance_criteria>
  <done>Tremolo section visible in sampler window with enable toggle, speed knob (Hz), depth knob (%), curve toggle, and L/R ratio control. All controls wired to processor atomics.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification</name>
  <what-built>Tremolo GUI section in the sampler window with all five controls (enable, speed, depth, curve, ratio), wired to the audio processor's tremolo logic from Plan 01.</what-built>
  <how-to-verify>
    1. Launch the standalone app: ./build/SPU94_artefacts/Release/Standalone/SPU94
    2. Load a sample in the sampler window (drag a WAV or use File menu)
    3. Look below the VCA Ramp section -- a "Tremolo" section should be visible with:
       - "Enable" toggle (teal tick color when on)
       - "Speed" rotary knob showing Hz value (default 5.0 Hz)
       - "Depth" rotary knob showing percentage (default 100%)
       - "Linear" button (toggles to "Exponential" on click)
       - "L/R Ratio" rotary knob (default 1.0, shows "1:1.0" format)
    4. Play a sustained note. Toggle "Enable" ON.
    5. Verify: you hear periodic volume pulsing (tremolo effect)
    6. Turn Depth to 0% -- pulsing should disappear (constant volume)
    7. Turn Depth back to 100% -- full pulsing returns
    8. Change Speed from 5 Hz to 1 Hz -- pulsing should slow noticeably
    9. Change Speed to 15 Hz -- fast flutter
    10. Click curve button to "Exponential" -- character changes (asymmetric: fast dip, slow recovery)
    11. Change L/R Ratio to 2.0 -- stereo drift becomes audible (channels pulse at different rates)
    12. Toggle Enable OFF -- pulsing stops, controls dim
  </how-to-verify>
  <resume-signal>Type "approved" if tremolo sounds and looks correct, or describe any issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI thread -> audio thread | Slider/button callbacks write atomics read by processBlock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | Tampering | GUI slider ranges | mitigate | All sliders have explicit min/max ranges set; no unbounded input reaches processor |
| T-44-05 | Information Disclosure | N/A | accept | No sensitive data in this component |
</threat_model>

<verification>
1. Project builds: cmake --build build (zero errors)
2. Visual: tremolo section is visible below VCA Ramp in sampler window
3. Functional: enabling tremolo produces audible volume pulsing
4. Controls respond: speed/depth/curve/ratio all affect the sound
</verification>

<success_criteria>
- Tremolo section visible and labeled in sampler GUI (TREM-06)
- All five controls (enable, speed, depth, curve, ratio) are functional
- Enabling/disabling tremolo starts/stops volume pulsing immediately
- Depth at 0% produces no audible modulation
- Visual feedback: controls dim when tremolo is disabled
</success_criteria>

<output>
Create `.planning/phases/44-tremolo/44-02-SUMMARY.md` when done
</output>
