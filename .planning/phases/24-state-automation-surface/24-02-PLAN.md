---
phase: 24-state-automation-surface
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/plugin/PluginProcessor.h
  - src/plugin/PluginProcessor.cpp
  - src/plugin/PluginEditor.cpp
  - src/plugin/MorphPanel.h
  - src/plugin/MorphPanel.cpp
autonomous: true
requirements: [PLUG-28, PLUG-29, PLUG-30, PLUG-31]

must_haves:
  truths:
    - "Host automation lanes show exactly 9 parameters with correct display units"
    - "Moving a GUI knob records automation in the host (beginChangeGesture/setValueNotifyingHost/endChangeGesture)"
    - "Host-written automation values reach the audio thread through the existing atomic bridge"
    - "Parameter IDs are frozen snake_case strings with versionHint=1 and will never change"
    - "Input Gain displays as dB (-inf to +24 dB); mixer/send/morph params display as percent 0-100"
    - "The REVERT comment at PluginEditor.cpp is removed; 0..16 Input Gain range is permanent"
  artifacts:
    - path: "src/plugin/PluginProcessor.h"
      provides: "9 AudioParameterFloat* member pointers plus public accessors"
      contains: "paramInputGain"
    - path: "src/plugin/PluginProcessor.cpp"
      provides: "addParameter calls in constructor for all 9 params"
      contains: "addParameter"
    - path: "src/plugin/PluginEditor.cpp"
      provides: "GUI knobs rewired through AudioParameterFloat gesture API"
      contains: "setValueNotifyingHost"
    - path: "src/plugin/MorphPanel.cpp"
      provides: "Morph Speed and Morph Grit wired through AudioParameterFloat gesture API"
      contains: "setValueNotifyingHost"
  key_links:
    - from: "PluginEditor slider.onValueChange"
      to: "AudioParameterFloat::setValueNotifyingHost"
      via: "normalized value conversion then host notification"
      pattern: "setValueNotifyingHost"
    - from: "AudioParameterFloat (host automation write)"
      to: "std::atomic<float>"
      via: "param listener or processBlock reads param->get()"
      pattern: "paramInputGain->get()"
    - from: "PluginProcessor::processBlock"
      to: "spu94_set_* engine calls"
      via: "existing per-block atomic -> engine register sync"
      pattern: "spu94_set_dry_fader"
---

<objective>
Register 9 host-automatable AudioParameterFloat instances and rewire all GUI controls to flow through the parameter system so DAW automation lanes work bidirectionally.

Purpose: Without registered parameters, DAW automation is impossible. This is the bridge between the host's automation engine and SPU-94's atomic-scalar GUI bridge.

Output: 9 parameters registered with frozen IDs, GUI knobs rewired with gesture brackets, REVERT comment cleaned up.
</objective>

<execution_context>
@/home/ubuntu-studio/.claude/get-shit-done/workflows/execute-plan.md
@/home/ubuntu-studio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-state-automation-surface/24-CONTEXT.md
@.planning/phases/24-state-automation-surface/24-RESEARCH.md
@.planning/phases/24-state-automation-surface/24-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 and existing code. -->

From src/plugin/PluginProcessor.h (existing atomics that params must mirror):
  std::atomic<float> inputLevel{0.50f};       // range 0.0..16.0
  std::atomic<float> dryLevel{0.0f};          // range 0.0..1.0
  std::atomic<float> patinaLevel{0.0f};       // range 0.0..1.0 (displayed as "ADPCM Level")
  std::atomic<float> reverbLevel{1.0f};       // range 0.0..1.0
  std::atomic<float> adpcmSend{1.0f};         // range 0.0..1.0
  std::atomic<float> drySend{0.0f};           // range 0.0..1.0
  std::atomic<float> morphPosition{0.625f};   // range 0.0..1.0
  std::atomic<float> morphSpeed{0.5f};        // range 0.0..1.0
  std::atomic<int>   morphGrit{0};            // 0 or 1

  static constexpr float kInputGainDefault = 0.5f;
  static constexpr float kInputGainMax     = 16.0f;

From src/plugin/PluginProcessor.cpp:280-307 (processBlock reads atomics per block):
  spu94_set_input_gain(engines[0], 0x7FFF);  // pinned at unity
  spu94_set_dry_fader(engines[0], dryLevel.load() * 0x7FFF);
  spu94_set_patina_fader(engines[0], patinaLevel.load() * 0x7FFF);
  // ... etc for all mixer/morph atomics

From src/plugin/PluginEditor.cpp:100-103 (current direct-atomic pattern to replace):
  inputLevelKnob.onValueChange = [this] {
      processorRef.getInputLevel().store(static_cast<float>(inputLevelKnob.getValue()), ...);
  };

From src/plugin/MorphPanel.cpp:130-134 (current direct-atomic pattern to replace):
  speedKnob.onValueChange = [this]() {
      processorRef.getMorphSpeed().store(static_cast<float>(speedKnob.getValue()), ...);
  };

FROZEN PARAMETER ID TABLE (PLUG-30 -- these IDs never change once published):
  1. "input_gain"      versionHint=1  Input Gain       0..16  skew@1.0  default=0.5   dB display
  2. "adpcm_send"      versionHint=1  ADPCM Send       0..1   linear    default=1.0   % display
  3. "dry_send"        versionHint=1  Dry Send         0..1   linear    default=0.0   % display
  4. "morph_position"  versionHint=1  Morph Position   0..1   linear    default=0.625 % display
  5. "morph_speed"     versionHint=1  Morph Speed      0..1   linear    default=0.5   % display
  6. "morph_grit"      versionHint=1  Morph Grit       0..1   step=1.0  default=0.0   Int/Fract.
  7. "dry_level"       versionHint=1  Dry Level        0..1   linear    default=0.0   % display
  8. "adpcm_level"     versionHint=1  ADPCM Level      0..1   linear    default=0.0   % display
  9. "reverb_level"    versionHint=1  Reverb Level     0..1   linear    default=1.0   % display
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register 9 AudioParameterFloat instances and wire processBlock reads</name>
  <files>src/plugin/PluginProcessor.h, src/plugin/PluginProcessor.cpp</files>
  <read_first>
    src/plugin/PluginProcessor.h
    src/plugin/PluginProcessor.cpp
    .planning/phases/24-state-automation-surface/24-RESEARCH.md
  </read_first>
  <action>
    In PluginProcessor.h, add 9 non-owning raw pointer members (JUCE owns the params after addParameter):
      juce::AudioParameterFloat* paramInputGain    = nullptr;
      juce::AudioParameterFloat* paramAdpcmSend    = nullptr;
      juce::AudioParameterFloat* paramDrySend      = nullptr;
      juce::AudioParameterFloat* paramMorphPosition = nullptr;
      juce::AudioParameterFloat* paramMorphSpeed   = nullptr;
      juce::AudioParameterFloat* paramMorphGrit    = nullptr;
      juce::AudioParameterFloat* paramDryLevel     = nullptr;
      juce::AudioParameterFloat* paramAdpcmLevel   = nullptr;
      juce::AudioParameterFloat* paramReverbLevel  = nullptr;
    Place them in the private section near the existing atomic declarations. Add public accessor methods: getParamInputGain(), getParamAdpcmSend(), etc., each returning the raw pointer.

    In PluginProcessor.cpp constructor, AFTER the existing body but BEFORE any other init, register all 9 parameters via addParameter. The registration ORDER is the AU parameter index order and MUST NEVER CHANGE (PLUG-30, Pitfall 1). Use exactly this order:
      1. input_gain -- NormalisableRange(0.0f, 16.0f, 0.01f) with setSkewForCentre(1.0f). Default 0.5f. Label "dB". stringFromValue: if v < 0.0001f return "-inf dB", else return String(20*log10(v), 1) + " dB". valueFromString: if contains "inf" return 0.0f, else pow(10, getFloatValue()/20).
      2. adpcm_send -- NormalisableRange(0.0f, 1.0f, 0.01f). Default 1.0f. Label "%". Percent display: String(int(v*100+0.5f)) + "%". valueFromString: getFloatValue()/100.
      3. dry_send -- same range/display as adpcm_send. Default 0.0f.
      4. morph_position -- same range/display as adpcm_send. Default 0.625f.
      5. morph_speed -- same range/display as adpcm_send. Default 0.5f.
      6. morph_grit -- NormalisableRange(0.0f, 1.0f, 1.0f) (step=1: two positions). Default 0.0f. Label "". stringFromValue: v < 0.5f returns "Int", else "Fract.". valueFromString: containsIgnoreCase("fract") returns 1.0f, else 0.0f.
      7. dry_level -- same as adpcm_send. Default 0.0f.
      8. adpcm_level -- same as adpcm_send. Default 0.0f.
      9. reverb_level -- same as adpcm_send. Default 1.0f.

    All ParameterID constructors use versionHint=1.

    Per D-01: mixer levels (dry_level, adpcm_level, reverb_level) display as percent 0-100.
    Per D-02: sends (adpcm_send, dry_send) display as percent 0-100.
    Per D-03: input_gain displays as dB.
    Per D-04: morph_speed and morph_grit display as percent / Int|Fract.
    Per D-05: morph_position displays as percent 0-100.

    PARAMETER -> ATOMIC WIRING: Each AudioParameterFloat stores its own internal value. The audio thread must read from the params, not the old atomics, for the 9 automated values. In processBlock, change the reads for the 9 params from atomic.load() to paramXxx->get(). Specifically:
      - Replace dryLevel.load(memory_order_relaxed) with paramDryLevel->get() at both occurrences (around lines 287 and 556)
      - Replace patinaLevel.load() with paramAdpcmLevel->get() at both occurrences (around lines 265, 289, 558)
      - Replace reverbLevel.load() with paramReverbLevel->get() (around lines 291, 560)
      - Replace drySend.load() with paramDrySend->get() (around lines 293, 562)
      - Replace adpcmSend.load() with paramAdpcmSend->get() (around lines 265, 295, 564)
      - Replace morphSpeed.load() with paramMorphSpeed->get() (around line 315)
      - Replace morphGrit.load() with static_cast<int>(paramMorphGrit->get() + 0.5f) (around lines 307, 576)
      - Replace morphPosition.load() with paramMorphPosition->get() (around line 318)
      - Replace inputLevel.load() with paramInputGain->get() (around lines 404, 467)

    Do NOT remove the atomic member declarations yet -- they remain as the internal storage. The AudioParameterFloat's get() method returns the denormalized real value, which is what processBlock needs.

    ALSO update the state-load path in setStateInformation (written in Plan 01): after writing the appendix values to atomics, ALSO update the corresponding AudioParameterFloat using setValueNotifyingHost with the normalized value. This ensures the host knows about restored values. For each of the 4 appendix params:
      paramInputGain->setValueNotifyingHost(paramInputGain->getNormalisableRange().convertTo0to1(result.inputGain));
      paramMorphPosition->setValueNotifyingHost(result.morphPosition);  // 0..1 range, normalization is identity
      paramMorphSpeed->setValueNotifyingHost(result.morphSpeed);
      paramMorphGrit->setValueNotifyingHost(result.morphGrit >= 0.5f ? 1.0f : 0.0f);

    For mixer atomics (dryLevel, patinaLevel, reverbLevel, adpcmSend, drySend): these are loaded from the .spu94 text body via the pending preset mechanism (audio thread calls spu94_preset_load which sets engine registers; then processBlock syncs engine registers back to atomics at lines 225-230). After the preset is applied on the audio thread, the atomics reflect the loaded values. The AudioParameterFloat values need updating too. Add a post-preset-apply sync in processBlock where filePresetAppliedCount is incremented (around line 237): after syncing atomics from engine registers, also call setValueNotifyingHost on the 5 mixer params using the freshly-loaded atomic values. WAIT -- setValueNotifyingHost must NOT be called from the audio thread (it triggers host callbacks on the message thread). Instead, use the raw setValue method or store into the param's internal value. Actually, for non-APVTS, the simplest safe pattern: after the audio thread syncs atomics from the engine, the EDITOR's timer callback already reads atomics to refresh sliders. The host reads getValueForHost() which calls getValue() which returns the internal param value. So we need the param's internal value to match. Use the audio-thread-safe pattern: paramDryLevel->operator=(dryLevel.load()), but that does not exist. The correct approach: make processBlock write the param value directly via the param's internal mechanism. AudioParameterFloat stores a std::atomic<float> internally. Calling param->setValue(normalized) from ANY thread is documented safe in JUCE because it just writes an atomic. So in processBlock, at the existing sync point (lines 225-230), after updating the wrapper atomics, also call:
      paramDryLevel->setValue(dryLevel.load(std::memory_order_relaxed));
      paramAdpcmLevel->setValue(patinaLevel.load(std::memory_order_relaxed));
      paramReverbLevel->setValue(reverbLevel.load(std::memory_order_relaxed));
      paramDrySend->setValue(drySend.load(std::memory_order_relaxed));
      paramAdpcmSend->setValue(adpcmSend.load(std::memory_order_relaxed));
    Note: setValue takes the NORMALIZED 0..1 value. For mixer params where the real range is 0..1, normalized == real. So this is correct.

    Similarly sync inputLevel and morph params at the same point:
      paramInputGain->setValue(paramInputGain->getNormalisableRange().convertTo0to1(inputLevel.load(std::memory_order_relaxed)));
      paramMorphPosition->setValue(morphPosition.load(std::memory_order_relaxed));
      paramMorphSpeed->setValue(morphSpeed.load(std::memory_order_relaxed));
      paramMorphGrit->setValue(morphGrit.load(std::memory_order_relaxed) >= 1 ? 1.0f : 0.0f);

    This sync only runs inside the if(filePresetReady) block, not every block -- it is triggered only on preset load, which is correct.

    PLUG-31: Block-granularity automation is the design. Each param is read once per processBlock via get(). No per-sample interpolation.

    PLUG-29: No APVTS anywhere. All 9 params use raw addParameter. Confirm no #include of AudioProcessorValueTreeState.
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb/build && cmake --build . --config Release --target SPU94_VST3 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - PluginProcessor.h contains 9 juce::AudioParameterFloat* members named paramInputGain through paramReverbLevel
    - PluginProcessor.h contains 9 public getParamXxx() accessor methods
    - PluginProcessor.cpp constructor contains exactly 9 addParameter calls in the frozen order
    - All 9 ParameterID constructors use versionHint=1
    - Parameter IDs match exactly: "input_gain", "adpcm_send", "dry_send", "morph_position", "morph_speed", "morph_grit", "dry_level", "adpcm_level", "reverb_level"
    - processBlock reads paramXxx->get() instead of atomic.load() for all 9 automated params
    - The preset-load sync block in processBlock calls setValue on all 9 params to sync after state restore
    - setStateInformation calls setValueNotifyingHost for the 4 float-appendix params
    - No AudioProcessorValueTreeState usage anywhere
    - input_gain stringFromValue returns "-inf dB" when v < 0.0001f and "X.X dB" otherwise
    - morph_grit stringFromValue returns "Int" for v < 0.5f and "Fract." otherwise
    - Build succeeds (VST3 target compiles clean)
  </acceptance_criteria>
  <done>9 AudioParameterFloat instances registered with frozen IDs and correct display units. processBlock reads from params. State restore syncs params. No APVTS. Build green.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire GUI controls through AudioParameterFloat gesture API and strip REVERT comment</name>
  <files>src/plugin/PluginEditor.cpp, src/plugin/MorphPanel.h, src/plugin/MorphPanel.cpp</files>
  <read_first>
    src/plugin/PluginEditor.cpp
    src/plugin/PluginEditor.h
    src/plugin/MorphPanel.cpp
    src/plugin/MorphPanel.h
    src/plugin/PluginProcessor.h
  </read_first>
  <action>
    PART A -- Strip REVERT comment (Phase 23 carryover):
    In PluginEditor.cpp around lines 92-95, delete the 3-line comment block that starts with "// UAT-ONLY (Phase 23):" and ends with "// approval -- slider exposure is Phase 24/PLUG-28 work." The setRange(0.0, 16.0, 0.01) line and setSkewFactorFromMidPoint(1.0) line STAY -- they are permanent per Phase 23 UAT sign-off.

    PART B -- Rewire PluginEditor knobs through parameter gesture API:
    For each of the 6 knobs in PluginEditor (inputLevelKnob, adpcmSendKnob, drySendKnob, dryKnob, patinaKnob, reverbKnob), replace the direct atomic store in onValueChange with the gesture-aware parameter notification pattern.

    For inputLevelKnob (lines 100-104), replace the lambda body with:
      auto* p = processorRef.getParamInputGain();
      p->beginChangeGesture();
      p->setValueNotifyingHost(p->getNormalisableRange().convertTo0to1(static_cast<float>(inputLevelKnob.getValue())));
      p->endChangeGesture();

    For percent-range knobs (adpcmSendKnob, drySendKnob, dryKnob, patinaKnob, reverbKnob), the normalized range IS the real range (0..1), so normalized == real value. Replace each lambda body with:
      auto* p = processorRef.getParamXxx();  // matching param accessor
      p->beginChangeGesture();
      p->setValueNotifyingHost(static_cast<float>(knob.getValue()));
      p->endChangeGesture();

    Mapping: adpcmSendKnob -> getParamAdpcmSend(), drySendKnob -> getParamDrySend(), dryKnob -> getParamDryLevel(), patinaKnob -> getParamAdpcmLevel(), reverbKnob -> getParamReverbLevel().

    NOTE ON GESTURE BRACKETING: The begin/end gesture calls inside onValueChange work for continuous drag because JUCE fires onValueChange on every drag step. For truly correct automation recording, begin should fire on drag start and end on drag end. Check if juce::Slider has onDragStart/onDragEnd members. If available (JUCE 8.0.12), wire them:
      knob.onDragStart = [this] { processorRef.getParamXxx()->beginChangeGesture(); };
      knob.onDragEnd   = [this] { processorRef.getParamXxx()->endChangeGesture(); };
    And remove begin/end from onValueChange, leaving only setValueNotifyingHost.

    If onDragStart/onDragEnd are NOT available on juce::Slider in JUCE 8.0.12, fall back to the Slider::Listener approach: have the editor implement juce::Slider::Listener, override sliderDragStarted/sliderDragEnded for gesture brackets, and use onValueChange for the setValueNotifyingHost call only. OR keep the simpler pattern with begin+end in every onValueChange -- this works but produces many short gestures instead of one per drag. Hosts handle this acceptably. Use whichever approach compiles cleanly.

    ALSO add host->GUI sync in the timer callback (timerCallback). After the existing preset-load sync block, add a param->GUI sync for automation playback: read each param's current value and update the slider if the value differs from the slider's current position by more than 0.001. This makes the GUI knobs follow automation playback. Use paramXxx->get() (denormalized real value) and compare to knob.getValue(). If different, set knob value with dontSendNotification to avoid feedback loops.

    PART C -- Rewire MorphPanel controls:
    MorphPanel needs access to the AudioParameterFloat pointers. The cleanest approach: MorphPanel already holds a reference to SPU94AudioProcessor. Add the same gesture-aware pattern to speedKnob.onValueChange (currently at MorphPanel.cpp:130-134) and gritIntButton/gritFractButton onClick handlers (currently at MorphPanel.cpp:167-168 and the setMorphGrit method at line 253).

    For speedKnob.onValueChange: replace the direct atomic store with:
      auto* p = processorRef.getParamMorphSpeed();
      p->beginChangeGesture();
      p->setValueNotifyingHost(static_cast<float>(speedKnob.getValue()));
      p->endChangeGesture();

    For morphKnob.onValueChange (MorphPanel.cpp:97-105): add parameter notification alongside the existing atomic store:
      auto* p = processorRef.getParamMorphPosition();
      p->beginChangeGesture();
      p->setValueNotifyingHost(static_cast<float>(morphKnob.getValue()));
      p->endChangeGesture();
    Remove the direct atomic store -- the param system handles value propagation.

    For setMorphGrit (MorphPanel.cpp:253): replace the direct atomic store with:
      auto* p = processorRef.getParamMorphGrit();
      p->beginChangeGesture();
      p->setValueNotifyingHost(grit >= 1 ? 1.0f : 0.0f);
      p->endChangeGesture();
    Remove the direct morphGrit.store() call.

    In MorphPanel::updateKnobPosition (called from editor timer): add param->GUI sync for morphPosition, morphSpeed, and morphGrit. Read param value, compare to knob position, update if different with dontSendNotification.
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb/build && cmake --build . --config Release --target SPU94_VST3 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The 3-line REVERT comment at PluginEditor.cpp (previously lines 92-95) is gone; setRange(0.0, 16.0) and setSkewFactorFromMidPoint(1.0) remain
    - inputLevelKnob.onValueChange calls setValueNotifyingHost instead of direct atomic store
    - adpcmSendKnob, drySendKnob, dryKnob, patinaKnob, reverbKnob all call setValueNotifyingHost
    - Every onValueChange that calls setValueNotifyingHost also has beginChangeGesture/endChangeGesture (either in onValueChange or split across onDragStart/onDragEnd)
    - MorphPanel speedKnob.onValueChange calls setValueNotifyingHost on paramMorphSpeed
    - MorphPanel morphKnob.onValueChange calls setValueNotifyingHost on paramMorphPosition
    - MorphPanel setMorphGrit calls setValueNotifyingHost on paramMorphGrit
    - No direct atomic .store() calls remain for the 9 automated params in PluginEditor.cpp or MorphPanel.cpp
    - Timer callback syncs param values to GUI knobs for automation playback
    - Build succeeds (VST3 target compiles clean)
  </acceptance_criteria>
  <done>All 9 GUI controls rewired through AudioParameterFloat gesture API. REVERT comment stripped. Timer syncs host automation to GUI. No direct atomic stores for automated params. Build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host -> AudioParameterFloat::setValue | Host writes normalized 0..1 values into params during automation playback |
| GUI -> AudioParameterFloat::setValueNotifyingHost | User knob drags flow through the param system to the host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05 | Tampering | Host writes out-of-range param value | accept | JUCE's NormalisableRange clamps input to declared range; no additional validation needed |
| T-24-06 | Denial of Service | Rapid automation lane changes | accept | Block-granularity reads (one get() per processBlock); no per-sample overhead; CPU impact negligible |
| T-24-07 | Repudiation | Parameter ID change breaks saved sessions | mitigate | IDs frozen with versionHint=1; registration order locked; documented in PLAN and code comments; future params added at END only |
</threat_model>

<verification>
1. Build: VST3 target compiles clean
2. Code review: exactly 9 addParameter calls, all with versionHint=1
3. Code review: no direct atomic .store() for the 9 automated params in editor or MorphPanel
4. Code review: every GUI control that was rewired has gesture brackets
5. Code review: REVERT comment is gone; slider range 0..16 and skew remain
6. Code review: no APVTS usage anywhere in the codebase
</verification>

<success_criteria>
- 9 parameters visible in host automation lanes
- GUI knob movements record automation (gesture brackets present)
- Host automation playback moves GUI knobs (timer sync present)
- Input Gain shows dB in automation lane; all others show percent
- REVERT comment removed; Input Gain range permanent
- Build green
</success_criteria>

<output>
After completion, create `.planning/phases/24-state-automation-surface/24-02-SUMMARY.md`
</output>
