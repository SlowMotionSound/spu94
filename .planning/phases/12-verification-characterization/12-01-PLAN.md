---
phase: 12-verification-characterization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/cmd_reverb.c
  - python/spu94/_binding.py
  - tests/golden/off/dac/impulse.wav
  - tests/golden/off/dac/impulse.wav.sha256
  - tests/conformance/test_goldens_present.py
autonomous: true
requirements: [INT-02]

must_haves:
  truths:
    - "CLI --no-dac-true-oversample flag forces v1.2 DAC path when used with --dac"
    - "Python binding exposes set/get_dac_true_oversample for characterization scripts"
    - "New v1.3 DAC golden files exist under tests/golden/ with SHA-256 sidecars"
    - "Golden regression gate passes (--check-dac and --check-dac-isolated exit 0)"
    - "v1.2 golden archive is preserved untouched in tests/golden_v1.2/"
  artifacts:
    - path: "src/cli/cmd_reverb.c"
      provides: "--no-dac-true-oversample CLI flag"
      contains: "no-dac-true-oversample"
    - path: "python/spu94/_binding.py"
      provides: "dac_true_oversample ctypes binding"
      contains: "spu94_set_dac_true_oversample"
    - path: "tests/golden/dac_isolated/impulse.wav.sha256"
      provides: "v1.3 golden SHA-256 sidecar"
  key_links:
    - from: "src/cli/cmd_reverb.c"
      to: "spu94_set_dac_true_oversample"
      via: "CLI flag dispatch"
      pattern: "spu94_set_dac_true_oversample.*state.*0"
    - from: "python/spu94/_binding.py"
      to: "libspu94.so"
      via: "ctypes argtypes/restype"
      pattern: "spu94_set_dac_true_oversample"
---

<objective>
Expose the v1.2/v1.3 DAC mode toggle to CLI and Python surfaces, then regenerate all DAC golden files under the v1.3 (true oversampled) path with SHA-256 sidecars. This establishes the regression baseline for v1.3 and provides the surface access needed for the Phase 12 characterization script.

Purpose: INT-02 requires new v1.3 golden files. The characterization script (CMP-02, Plan 02) needs CLI/Python access to toggle between v1.2 and v1.3 modes for comparison measurements.
Output: CLI flag, Python binding, regenerated golden files with sidecars, passing regression gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-noise-recalibration-integration/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --no-dac-true-oversample CLI flag + Python binding for dac_true_oversample</name>
  <files>src/cli/cmd_reverb.c, python/spu94/_binding.py</files>
  <read_first>
    - src/cli/cmd_reverb.c (current long_opts table, DAC section wiring at ~line 393-399)
    - python/spu94/_binding.py (existing DAC binding declarations at ~line 279-295)
    - include/spu94/spu94.h (public API declarations for set/get_dac_true_oversample at ~line 308-309)
  </read_first>
  <action>
In src/cli/cmd_reverb.c:
1. Add to the help string (after --no-dac-noise line ~114):
   "  --no-dac-true-oversample Use v1.2 approximate DAC path (use with --dac)\n"
2. Add to long_opts array (after {"no-dac-noise", ...} at line 143):
   {"no-dac-true-oversample", no_argument, NULL, 1011},
3. Add bool declaration (after `bool no_dac_noise = false;` ~line 163):
   bool no_dac_true_oversample = false;
4. Add case in switch (after case 1002 at ~line 203):
   case 1011:
       no_dac_true_oversample = true;
       break;
5. Add to DAC section (after line 398 `spu94_set_dac_noise_enabled`):
   if (no_dac_true_oversample)
       spu94_set_dac_true_oversample(state, 0);

In python/spu94/_binding.py:
After the dac_noise_enabled block (line ~295), add:
   _lib.spu94_set_dac_true_oversample.restype = None
   _lib.spu94_set_dac_true_oversample.argtypes = [ctypes.c_void_p, ctypes.c_int]

   _lib.spu94_get_dac_true_oversample.restype = ctypes.c_int
   _lib.spu94_get_dac_true_oversample.argtypes = [ctypes.c_void_p]
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb && grep -c "no-dac-true-oversample" src/cli/cmd_reverb.c && grep -c "spu94_set_dac_true_oversample" python/spu94/_binding.py</automated>
  </verify>
  <acceptance_criteria>
    - src/cli/cmd_reverb.c contains string "no-dac-true-oversample" at least 3 times (help, long_opts, case)
    - src/cli/cmd_reverb.c contains "spu94_set_dac_true_oversample(state, 0)" 
    - python/spu94/_binding.py contains "spu94_set_dac_true_oversample.restype = None"
    - python/spu94/_binding.py contains "spu94_get_dac_true_oversample.restype"
    - Build succeeds: cd build && cmake --build . --target spu94 exits 0
  </acceptance_criteria>
  <done>CLI --no-dac-true-oversample flag wired, Python binding declares set/get_dac_true_oversample, project builds cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate v1.3 DAC golden files + verify regression gate</name>
  <files>tests/golden/*/dac/*.wav, tests/golden/*/dac/*.wav.sha256, tests/golden/dac_isolated/*.wav, tests/golden/dac_isolated/*.wav.sha256</files>
  <read_first>
    - scripts/regenerate_goldens.py (understand --dac and --dac-isolated modes, lines 1-100 for invocation patterns)
    - tests/conformance/test_goldens_present.py (golden count assertion at line 211-214: expects 135 .wav + 135 .sha256)
  </read_first>
  <action>
1. Build the project fresh to pick up Phase 11 changes:
   cd /home/ubuntu-studio/Desktop/PSX\ Reverb/build && cmake --build . --target spu94

2. Regenerate all 50 full-pipeline DAC goldens (v1.3 path is default since dac_true_oversample=1):
   cd /home/ubuntu-studio/Desktop/PSX\ Reverb && python3 scripts/regenerate_goldens.py --dac

3. Regenerate all 5 isolated DAC goldens:
   python3 scripts/regenerate_goldens.py --dac-isolated

4. Verify the new goldens pass the SHA-256 regression gate:
   python3 scripts/regenerate_goldens.py --check-dac
   python3 scripts/regenerate_goldens.py --check-dac-isolated

5. Verify the conformance test still passes (golden count = 135):
   cd build && ctest -R goldens_present --output-on-failure

6. Verify v1.2 archive is untouched:
   ls tests/golden_v1.2/dac_isolated/impulse.wav (must exist)

Note: The test_goldens_present.py expects exactly 135 .wav files. Since we are REPLACING 55 existing DAC goldens (50 full-pipeline + 5 isolated) with new v1.3 versions, the count stays at 135. No test modification needed.
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb && python3 scripts/regenerate_goldens.py --check-dac && python3 scripts/regenerate_goldens.py --check-dac-isolated && cd build && ctest -R goldens_present --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - python3 scripts/regenerate_goldens.py --check-dac exits 0 and prints "PASS: 50/50 DAC goldens match"
    - python3 scripts/regenerate_goldens.py --check-dac-isolated exits 0 and prints "PASS: 5/5 DAC isolated goldens match"
    - ctest -R goldens_present exits 0 (135 .wav + 135 .sha256 count passes)
    - tests/golden_v1.2/dac_isolated/impulse.wav exists (archive preserved)
    - Each .wav.sha256 sidecar in tests/golden/dac_isolated/ contains a 64-char hex digest
  </acceptance_criteria>
  <done>55 v1.3 DAC golden files regenerated with SHA-256 sidecars, regression gate passes, v1.2 archive untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI input -> C core | --no-dac-true-oversample flag parsed from user input |
| Python ctypes -> .so | Binding calls into native library |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | T (Tampering) | Golden file regeneration | accept | Determinism env (LC_ALL=C, TZ=UTC, SOURCE_DATE_EPOCH) already enforced in regenerate_goldens.py; SHA-256 sidecars detect tampering |
| T-12-02 | I (Info Disclosure) | CLI flag | accept | No sensitive data exposed; toggle is a boolean mode selector |
</threat_model>

<verification>
1. Build succeeds with new CLI flag
2. CLI --help shows --no-dac-true-oversample
3. Python binding loads without ImportError
4. All 55 DAC goldens regenerated and pass --check
5. Golden count remains 135 (conformance test)
6. v1.2 archive directory untouched
</verification>

<success_criteria>
- INT-02 satisfied: v1.3 golden files exist with SHA-256 sidecars, v1.2 archived
- CLI and Python surfaces can toggle between v1.2 and v1.3 DAC modes
- Full test suite passes (ctest --output-on-failure)
</success_criteria>

<output>
After completion, create `.planning/phases/12-verification-characterization/12-01-SUMMARY.md`
</output>
