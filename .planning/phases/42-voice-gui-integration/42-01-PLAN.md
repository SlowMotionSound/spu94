---
phase: 42-voice-gui-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VGUI-01, VGUI-02, VGUI-03]

must_haves:
  truths:
    - "All 57 voice_tick unit tests pass (covering PMON, NON, sweep, signed volume, ADSR, loop, EON, Gauss, anti-aliasing)"
    - "All 6 noise_gen unit tests pass"
    - "All 12 ADSR unit tests pass"
    - "All 12 sweep unit tests pass"
    - "All 11 sample_loader unit tests pass"
    - "rt_no_heap gate passes (no malloc/calloc/realloc/free in link closure)"
    - "rt_no_locks gate passes (no pthread/sem/futex in link closure)"
    - "rt_no_syscalls gate passes (zero syscalls in hot path)"
    - "hotpath_alloc_gate passes (zero heap syscalls in steady state)"
    - "hotpath_alloc_gate_negative passes (expected failure confirms gate integrity)"
    - "rt_bench_latency passes (bounded latency)"
    - "Existing features unbroken: ADSR, loop mechanics, EON reverb send, Gaussian interpolation, anti-aliasing toggle, MIDI dispatch"
  artifacts: []
  key_links:
    - from: "tests/unit/voice/test_voice_tick.c"
      to: "src/spu94/spu94_voice.c"
      via: "spu94_voice_mixer_tick + spu94_voice_tick"
      pattern: "spu94_voice_mixer_tick"
    - from: "tests/rt_safety/hotpath_alloc_gate.sh"
      to: "src/spu94/spu94_process.c"
      via: "strace syscall filter on spu94_process hot path"
      pattern: "spu94_process"
---

<objective>
Run the complete voice engine test suite and all rt_safety gates to verify that every
v1.9 feature coexists correctly after the GUI integration phases (39-41). This is the
final gate before v1.9 can ship.

Purpose: Phases 39-41 added Pan/Level controls, NON/PMON toggles, and VCA ramp controls
to the sampler GUI. While each phase was tested individually, this phase confirms that
the full feature set works together: no regressions in the C core, no rt_safety violations
from the combined feature surface, and all voice engine behaviors remain intact.

Output: Verification results confirming all 98+ unit tests and 6 rt_safety gates pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/unit/voice/CMakeLists.txt
@tests/rt_safety/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full voice engine regression suite (VGUI-01, VGUI-02)</name>
  <files></files>
  <read_first>
    tests/unit/voice/CMakeLists.txt (to identify all test targets and labels)
  </read_first>
  <action>
    Build the test-only configuration from build-test (SPU94_BUILD_GUI=OFF,
    SPU94_BUILD_PLUGIN=OFF). Run every voice engine test target individually and
    record pass/fail counts:

    1. test_voice_tick — expect 57 tests, 0 failures. This target covers the full
       feature matrix: ADSR envelope shaping, loop mechanics with flag side effects,
       EON reverb send gating, Gaussian interpolation, anti-aliasing toggle, signed
       volume with phase inversion, PMON pitch modulation (single + chain + silent
       modulator), NON noise substitution (ADPCM still decodes), sweep volume ramp
       (linear/exp, increase/decrease, anti-stall), and cross-feature combinations
       (PMON+NON, sweep+ADSR concurrent).

    2. test_noise_gen — expect 6 tests, 0 failures. Covers LFSR polynomial, seed,
       timer mechanism, NoiseShift/NoiseStep frequency control.

    3. test_adsr — expect 12 tests, 0 failures. Covers all four phases (attack,
       decay, sustain, release), step formulas with corrected base-8, exponential
       modes, counter-accumulate mechanism.

    4. test_sweep — expect 12 tests, 0 failures. Covers linear/exp increase/decrease,
       anti-stall guard, negative-phase sweep, KON reset, independent L/R.

    5. test_sample_loader — expect 11 tests, 0 failures. Covers WAV loading, ADPCM
       block parsing, loop point extraction.

    Per VGUI-01: the voice_tick tests exercise multiple features simultaneously
    (the Phase 38 integration tests specifically combine PMON+NON, sweep+ADSR).
    Per VGUI-02: existing features are covered by their original test suites from
    Phases 28-32 which are included in the voice_tick binary.

    If any test fails, read the failing test function and the relevant source to
    diagnose whether the failure is a genuine regression from Phases 39-41 or a
    pre-existing issue.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && cmake --build build-test 2>&1 | tail -3 && echo "--- voice_tick ---" && build-test/tests/unit/voice/test_voice_tick 2>&1 | tail -3 && echo "--- noise_gen ---" && build-test/tests/unit/voice/test_noise_gen 2>&1 | tail -3 && echo "--- adsr ---" && build-test/tests/unit/voice/test_adsr 2>&1 | tail -3 && echo "--- sweep ---" && build-test/tests/unit/voice/test_sweep 2>&1 | tail -3 && echo "--- sample_loader ---" && build-test/tests/unit/voice/test_sample_loader 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_voice_tick: 57 Tests 0 Failures
    - test_noise_gen: 6 Tests 0 Failures
    - test_adsr: 12 Tests 0 Failures
    - test_sweep: 12 Tests 0 Failures
    - test_sample_loader: 11 Tests 0 Failures
    - Zero compiler warnings from voice engine source files
  </acceptance_criteria>
  <done>All 98 voice engine unit tests pass with zero failures, confirming VGUI-01 (multi-feature coexistence) and VGUI-02 (no regressions in waveform, ADSR, pitch, latch/lock, drive, MIDI dispatch)</done>
</task>

<task type="auto">
  <name>Task 2: rt_safety gates with full v1.9 feature set (VGUI-03)</name>
  <files></files>
  <read_first>
    tests/rt_safety/CMakeLists.txt (to understand gate targets, labels, and WILL_FAIL properties)
  </read_first>
  <action>
    Run all 6 rt_safety gates via ctest from build-test to verify VGUI-03: no heap
    allocation, no locks, no syscalls, and bounded latency on the audio hot path with
    every v1.9 feature compiled in.

    The gates:
    1. rt_no_heap — nm -u on static-linked binary, zero heap symbols (malloc, calloc,
       realloc, free). Verifies sweep module, noise module, envelope_step helper
       introduce no heap calls.
    2. rt_no_locks — nm -u, zero lock symbols (pthread_mutex_*, sem_*, futex).
    3. rt_no_syscalls — strace on spu94_process loop, zero syscalls between SIGUSR1
       markers.
    4. hotpath_alloc_gate — strace with brk/mmap filter, zero heap syscalls in
       steady state.
    5. hotpath_alloc_gate_negative — poisoned target MUST fail (WILL_FAIL property).
       Confirms the gate mechanism itself works.
    6. rt_bench_latency — bounded latency benchmark.

    Run: ctest -L rt_safety --output-on-failure from build-test.

    All 6 gates must pass. The Pan/Level, NON/PMON toggle, and VCA ramp GUI code
    lives in the JUCE layer and does not affect the C core link closure, but this
    confirms the full C core (with all feature modules) remains rt-safe.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && cmake --build build-test 2>&1 | tail -3 && ctest --test-dir build-test -L rt_safety --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ctest -L rt_safety: 6 tests passed, 0 failures, 0 errors
    - rt_no_heap: PASS
    - rt_no_locks: PASS
    - rt_no_syscalls: PASS (or SKIP if strace unavailable — note in summary)
    - hotpath_alloc_gate: PASS
    - hotpath_alloc_gate_negative: PASS (expected failure = gate integrity confirmed)
    - rt_bench_latency: PASS
  </acceptance_criteria>
  <done>All 6 rt_safety gates pass with the complete v1.9 feature set, confirming VGUI-03: no heap allocation, no locks, no syscalls, and bounded latency on the audio hot path with all GUI-driven features enabled</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strace -> spu94_process | syscall monitoring of hot path |
| nm -> link closure | symbol analysis of static binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Denial of Service | audio hot path | mitigate | rt_no_heap + rt_no_locks + rt_no_syscalls + hotpath_alloc_gate verify no unbounded operations |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
cd "/home/ubuntu-studio/Desktop/PSX Reverb" && cmake --build build-test && ctest --test-dir build-test -L rt_safety --output-on-failure && build-test/tests/unit/voice/test_voice_tick && build-test/tests/unit/voice/test_noise_gen && build-test/tests/unit/voice/test_adsr && build-test/tests/unit/voice/test_sweep && build-test/tests/unit/voice/test_sample_loader
Expected: All 98 unit tests pass, all 6 rt_safety gates pass
</verification>

<success_criteria>
- 98 voice engine unit tests pass across 5 targets (57 + 6 + 12 + 12 + 11)
- 6 rt_safety gates pass (no heap, no locks, no syscalls, alloc gate, negative gate, latency bench)
- Zero regressions in any existing feature
- No new forbidden operations on the audio hot path
</success_criteria>

<output>
Create `.planning/phases/42-voice-gui-integration/42-01-SUMMARY.md` when done
</output>
