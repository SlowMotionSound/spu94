---
context: phase
phase: 14-i-o-surfaces
task: complete
total_tasks: complete
status: complete
last_updated: 2026-05-02T18:30:00Z
---

<current_state>
Phase 14 (I/O Surfaces) is fully complete and verified. Phase 15 (Verification) is next — not yet planned.

All code is committed, both targets build clean, and the user has manually verified the JUCE GUI.
</current_state>

<completed_work>

Plan 14-01: CLI Preset I/O (2 tasks)
- Task 1: Created cmd_preset_dump.c — preset-dump subcommand with --preset/--name/-o/--list-presets flags
- Task 2: Added --load-preset flag to reverb with three-way mutual exclusion (--preset/--config/--load-preset)
- Commits: 027c5d3, 462ab07, 4ca28d2

Plan 14-02: JUCE Save/Load GUI (2 auto tasks + 1 human checkpoint)
- Task 1: Save/Load buttons, processor file-preset API (pendingPresetBuf + audio-thread drain), syncMixerKnobsFromProcessor
- Task 2: Custom preset dropdown entry (diamond prefix), modified-state asterisk indicator (30Hz diff of 46 fields)
- Task 3: Human verification — user approved after 3 post-merge fixes
- Commits: 1230a86, e3cf09b, 427175d

Post-merge fixes (applied after executor agents, before verification):
- 4817816: Save dialog simplified — removed AlertWindow name prompt, goes straight to system file dialog with preset name pre-filled via touch-create workaround for kdialog. Preset name metadata derived from filename.
- 4817816: Volume jump on preset load fixed — savePresetToString now syncs all mixer/DAC atomics to SPU before serializing (processBlock only pushes while playing, so saving while stopped captured stale prepareToPlay defaults)
- 2e9443b: Asterisk sticking after preset switch fixed — dropdown list now always rebuilds clean names on any factory preset switch

Verification: 059d17d — 4/4 success criteria passed
</completed_work>

<remaining_work>

Phase 15: Verification — round-trip golden test proving save/load/process produces bit-identical output.
Not yet planned (needs /gsd-discuss-phase 15 or /gsd-plan-phase 15).
</remaining_work>

<decisions_made>

- Save dialog simplified to single step: removed JUCE AlertWindow name+description prompt, go straight to system file dialog. Preset name comes from filename. Rationale: user found two-step flow redundant, and kdialog on KDE doesn't pre-fill filenames for non-existent files.
- Touch-create workaround for kdialog: create empty file before dialog so kdialog sees it and pre-fills the filename field. Cleaned up on cancel.
- File-preset audio-thread handoff uses pendingPresetBuf + acquire/release atomics (same pattern as WAV double-buffer), not the PresetCommandQueue (which is factory-preset-only).
- loaded_pid = -1 for file presets ensures default-faders block doesn't overwrite file's mixer values (matches JSON config behavior).
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/ROADMAP.md` — Phase 15 scope and success criteria
2. `.planning/phases/14-i-o-surfaces/14-VERIFICATION.md` — Phase 14 verification report
3. `.planning/phases/13-core-preset-api/13-01-SUMMARY.md` — C core save API (Phase 15 tests exercise this)

## Infrastructure State
- Build system: CMake, both spu94_cli and spu94_standalone_Standalone targets build clean
- No running services or background processes

<context>
Phase 14 shipped the last user-facing surfaces for the preset system. Phase 15 is a pure verification phase — a ctest target that proves save/load/process round-trips produce bit-identical WAV output. This is the final phase of v1.4 Preset System milestone.
</context>

<next_action>
Start with: `/gsd-discuss-phase 15` or `/gsd-plan-phase 15` to plan the verification golden test
</next_action>
