---
phase: 07-verification-golden-files-witness-diff-modulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/install-phase7-deps.sh
  - scripts/ci/check_coverage.py
  - scripts/ci/test_check_coverage.py
  - docs/COVERAGE.md
  - tests/conformance/CMakeLists.txt
  - tests/conformance/test_coverage_map_integrity.py
  - tests/CMakeLists.txt
  - .github/workflows/ci.yml
autonomous: true
requirements: [TEST-01, BUILD-08]
must_haves:
  truths:
    - "Host dev workstation has lilv-utils, python3-scipy, pytest-benchmark, strace, and lv2-dev installed and importable"
    - "`docs/COVERAGE.md` exists with three sections (per-register 35 rows, per-behavior, per-spec-paragraph) each citing existing tests"
    - "`scripts/ci/check_coverage.py` exits 0 when every COVERAGE.md row's named test exists and passes"
    - "`scripts/ci/check_coverage.py` exits 1 when injected with a row naming a missing file (negative meta-test)"
    - "`scripts/ci/check_coverage.py` exits 1 when injected with a row naming a fake test name (negative meta-test)"
    - "CI runs `check_coverage.py` as a dedicated `coverage-map-check` job on every push and fails the build on any gap"
    - "All spec citations in per-spec-paragraph section point to the pinned wayback snapshot `https://web.archive.org/web/20260114082525/https://psx-spx.consoledev.net/soundprocessingunitspu/`"
  artifacts:
    - path: "scripts/ci/install-phase7-deps.sh"
      provides: "Idempotent install of lilv-utils + lv2-dev + python3-scipy + python3-pytest-benchmark + strace"
      min_lines: 20
    - path: "scripts/ci/check_coverage.py"
      provides: "Parses COVERAGE.md three sections; verifies every `test:` entry exists and passes"
      min_lines: 60
    - path: "scripts/ci/test_check_coverage.py"
      provides: "Positive + negative meta-tests for check_coverage.py"
      min_lines: 40
    - path: "docs/COVERAGE.md"
      provides: "Three-section spec-conformance map with pinned wayback snapshot URL at top"
      min_lines: 80
    - path: "tests/conformance/test_coverage_map_integrity.py"
      provides: "Structural meta-test: COVERAGE.md parses; all three sections present; ≥35 per-register rows; every row has non-empty `test:` column"
      min_lines: 30
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/check_coverage.py"
      via: "new `coverage-map-check` job invoking the script"
      pattern: "check_coverage\\.py"
    - from: "docs/COVERAGE.md"
      to: "tests/unit/**/*.c + tests/python/**/*.py"
      via: "every row contains a `test:` field naming an existing test function"
      pattern: "tests/(unit|python|conformance)/"
    - from: "tests/conformance/CMakeLists.txt"
      to: "pytest tests/conformance/"
      via: "add_test wiring into ctest"
      pattern: "add_test.*conformance"
---

<objective>
Install Phase 7 host toolchain, scaffold `docs/COVERAGE.md` with three sections (per-register, per-behavior, per-spec-paragraph) citing the pinned wayback snapshot, ship the CI-enforced coverage validator and its meta-tests, and wire the new `coverage-map-check` CI job.

Purpose: Lock D-01..D-04 into executable form. Every other Phase 7 plan depends on (a) scipy + pytest-benchmark + lv2apply being importable on the host and (b) COVERAGE.md already existing with a parser so later plans can append rows for new tests.

Output: Installed toolchain, COVERAGE.md file with initial row population from the existing ~40 tests audit, working validator, negative meta-tests, new CI job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-verification-golden-files-witness-diff-modulation/07-CONTEXT.md
@.planning/phases/07-verification-golden-files-witness-diff-modulation/07-RESEARCH.md
@.planning/phases/07-verification-golden-files-witness-diff-modulation/07-VALIDATION.md
@include/spu94/spu94.h
@include/spu94/spu94_registers.h
@.github/workflows/ci.yml
@scripts/ci/verify-flags.sh
@scripts/ci/verify-readme-sections.sh

<interfaces>
<!-- The 35-register enum order (spu94_reg_t) that per-register COVERAGE rows must enumerate -->
<!-- From include/spu94/spu94_registers.h: -->
<!-- vLOUT, vROUT, mBASE, dAPF1, dAPF2, vIIR, vCOMB1..4, vWALL, vAPF1, vAPF2, -->
<!-- mLSAME, mRSAME, mLCOMB1, mRCOMB1, mLCOMB2, mRCOMB2, dLSAME, dRSAME, -->
<!-- mLDIFF, mRDIFF, mLCOMB3, mRCOMB3, mLCOMB4, mRCOMB4, dLDIFF, dRDIFF, -->
<!-- mLAPF1, mRAPF1, mLAPF2, mRAPF2, vLIN, vRIN (35 total, SPU94_REG__COUNT) -->

<!-- Existing test tree to audit for COVERAGE rows: -->
<!-- tests/unit/q15/ - Q15 truncation -->
<!-- tests/unit/buffer/ - BufferAddress wrap + mBASE snap-on-write -->
<!-- tests/unit/registers/ - per-register I/O -->
<!-- tests/unit/reverb/ - test_reverb_{apf1,apf2,body,comb,diff_iir,edges,hard_clip,input_scale,output_scale,same_iir}.c -->
<!-- tests/unit/fir/ - 11 FIR tests -->
<!-- tests/unit/preset/ - 3 preset tests -->
<!-- tests/unit/process/ - 7 process tests -->
<!-- tests/python/fuzz_*.py - 4 fuzz harnesses -->
<!-- tests/python/binding/ - 5 binding tests -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 7 host toolchain + create install script</name>
  <files>scripts/ci/install-phase7-deps.sh</files>
  <read_first>
    - .github/workflows/ci.yml (existing CI job structure; toolchain install pattern)
    - .planning/phases/07-verification-golden-files-witness-diff-modulation/07-RESEARCH.md § Installation (canonical apt package list per D-14 + researcher verification)
  </read_first>
  <action>
    Create `scripts/ci/install-phase7-deps.sh` as an idempotent bash script that:
    1. Sets `set -euo pipefail`.
    2. Runs `sudo apt-get update` followed by `sudo apt-get install -y lilv-utils lv2-dev python3-scipy python3-pytest-benchmark strace coreutils git`.
    3. Verifies each tool is available post-install with five explicit checks: `command -v lv2apply`, `python3 -c "import scipy.signal; import scipy; print(scipy.__version__)"`, `python3 -c "import pytest_benchmark; print(pytest_benchmark.__version__)"`, `command -v strace`, `command -v sha256sum`. Each failure prints `FAIL: <tool> not available after install` to stderr and exits 1.
    4. Prints `PASS: Phase 7 host toolchain installed` on success.

    Then execute the script on the current host via `bash scripts/ci/install-phase7-deps.sh`. Confirm the five verify lines print and the final PASS appears. If any `apt-get install` fails due to package-name drift (e.g., `lilv-utils` renamed on a future distro), fall back per RESEARCH.md's alt: `pip install --user scipy>=1.11 pytest-benchmark>=5.2` — document the fallback used in the script's header comment. Implements D-13 (host dev is the first environment) + Wave 0 install gap from VALIDATION.md.
  </action>
  <verify>
    <automated>bash scripts/ci/install-phase7-deps.sh</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/ci/install-phase7-deps.sh` exists and is executable (`test -x scripts/ci/install-phase7-deps.sh`).
    - `bash scripts/ci/install-phase7-deps.sh` exits 0 and the final line of stdout matches the literal string `PASS: Phase 7 host toolchain installed`.
    - After script run: `command -v lv2apply` prints a non-empty path; `python3 -c "import scipy.signal, pytest_benchmark"` exits 0; `command -v strace` prints a non-empty path.
    - `grep -c "set -euo pipefail" scripts/ci/install-phase7-deps.sh` returns 1.
    - `grep -cE "lilv-utils|lv2-dev|python3-scipy|python3-pytest-benchmark|strace" scripts/ci/install-phase7-deps.sh` returns at least 5.
  </acceptance_criteria>
  <done>Host toolchain installed and verified; idempotent install script committed so CI and future Phase 7 execution have a single reproducible entry point.</done>
</task>

<task type="auto">
  <name>Task 2: Create COVERAGE.md scaffold with all three sections + audit existing tests into rows</name>
  <files>docs/COVERAGE.md</files>
  <read_first>
    - docs/DECISIONS.md (to cite ADR-0001 for Q15, ADR-0005 for split write policy, ADR-0006 for mBASE snap, ADR-Phase-4-* for FIR)
    - include/spu94/spu94_registers.h (35-register order — per-register rows MUST appear in spu94_reg_t enum order)
    - .planning/phases/07-verification-golden-files-witness-diff-modulation/07-CONTEXT.md § Rough shape of docs/COVERAGE.md (row-format template)
    - tests/unit/reverb/ (via `ls tests/unit/reverb/`) — name every existing test file for per-behavior rows
    - tests/unit/fir/, tests/unit/buffer/, tests/unit/registers/, tests/unit/q15/, tests/unit/process/, tests/unit/preset/ (name every existing test file)
  </read_first>
  <action>
    Create `docs/COVERAGE.md` with this exact structure (D-01 single-file three-section):

    ```markdown
    # SPU-94 — Spec Conformance Coverage

    Pinned spec reference: https://web.archive.org/web/20260114082525/https://psx-spx.consoledev.net/soundprocessingunitspu/

    This file is CI-enforced by `scripts/ci/check_coverage.py` (D-02). Every row's
    `test:` field names a test that must exist and pass. Adding a new row without
    a corresponding test fails the build.

    ## Per-Register Coverage

    | Register | Type | test: | Notes |
    |----------|------|-------|-------|
    | `vLOUT`  | I16 | `tests/unit/registers/test_register_roundtrip.c::test_roundtrip_vLOUT` | |
    | `vROUT`  | I16 | `tests/unit/registers/test_register_roundtrip.c::test_roundtrip_vROUT` | |
    | `mBASE`  | U16 | `tests/unit/buffer/test_buffer_mbase.c::test_mbase_snap_on_write` | ADR-0006 |
    ...(all 35 registers in spu94_reg_t enum order)...

    ## Per-Behavior Coverage

    | Behavior | Spec reference | test: | Notes |
    |----------|----------------|-------|-------|
    | SAME IIR stage | #reverb-processing | `tests/unit/reverb/test_reverb_same_iir.c::test_same_iir_basic` | |
    | DIFF IIR stage | #reverb-processing | `tests/unit/reverb/test_reverb_diff_iir.c::test_diff_iir_basic` | |
    | 4-tap comb (D-07 cascading sat) | #reverb-processing | `tests/unit/reverb/test_reverb_comb.c::test_comb_cascading_saturation` | |
    | APF1 | #reverb-processing | `tests/unit/reverb/test_reverb_apf1.c::test_apf1_basic` | |
    | APF2 | #reverb-processing | `tests/unit/reverb/test_reverb_apf2.c::test_apf2_basic` | |
    | Input scale | #reverb-processing | `tests/unit/reverb/test_reverb_input_scale.c::test_input_scale_basic` | |
    | Output scale | #reverb-processing | `tests/unit/reverb/test_reverb_output_scale.c::test_output_scale_basic` | |
    | Hard clip on mix bus | #reverb-processing | `tests/unit/reverb/test_reverb_hard_clip.c::test_hard_clip_saturates` | ADR-0007 |
    | vIIR=0x8000 anomaly | #reverb-processing | `tests/unit/reverb/test_reverb_same_iir.c::test_same_iir_anomaly` | ADR-0002 |
    | BufferAddress wrap | #reverb-buffer | `tests/unit/buffer/test_buffer_wrap.c::test_wrap_formula` | ADR-0006 |
    | mBASE snap-on-write | #reverb-buffer | `tests/unit/buffer/test_buffer_mbase.c::test_mbase_snap_on_write` | ADR-0006 |
    | 39-tap FIR decimate | #reverb-buffer-resampling | `tests/unit/fir/test_fir_decimate.c::test_fir_decimate_impulse` | |
    | 39-tap FIR interpolate | #reverb-buffer-resampling | `tests/unit/fir/test_fir_interpolate.c::test_fir_interpolate_impulse` | |
    | 39-tap FIR bit identity | #reverb-buffer-resampling | `tests/unit/fir/test_fir_bit_identity.c::test_fir_bit_identity` | |
    | FIR chain latency | #reverb-buffer-resampling | `tests/unit/fir/test_fir_chain_latency.c::test_chain_latency_58_samples` | ADR-Phase-4-H |
    | Split write-timing policy | #spureverbregisters | `tests/unit/registers/test_write_policy.c::test_immediate_vs_tick_latched` | ADR-0005 |
    | Q15 truncation-not-rounding | #spu-fixed-point | `tests/unit/q15/test_q15_truncate.c::test_truncate_toward_negative_infinity` | ADR-0001 |
    | L/R tick alternation | #reverb-processing | `tests/unit/reverb/test_reverb_body.c::test_lr_tick_alternation` | |
    | Mid-stream write determinism | N/A (project invariant) | `tests/python/fuzz_process.py::main` | 10^6 steps |

    ## Per-Spec-Paragraph Coverage

    | Snapshot anchor | test: | Notes |
    |-----------------|-------|-------|
    | `#spureverbregisters` | `tests/unit/registers/test_register_roundtrip.c::test_all_35_registers_round_trip` | All 35 registers round-trip |
    | `#reverb-processing` | `tests/unit/reverb/test_reverb_body.c::test_reverb_body_composes_all_stages` | All stages composed |
    | `#reverb-buffer` | `tests/unit/buffer/test_buffer_wrap.c::test_wrap_formula` | Wrap + mBASE policy |
    | `#reverb-buffer-resampling` | `tests/unit/fir/test_fir_round_trip_transparency.c::test_round_trip_transparency` | 39-tap decimate + interpolate round-trip |
    | `#spu-fixed-point` | `tests/unit/q15/test_q15_truncate.c::test_truncate_toward_negative_infinity` | |
    ```

    CRITICAL: Audit the ACTUAL test tree first (`ls tests/unit/*/` and `grep -r "TEST(" tests/unit/`) to confirm each named test function exists. If the exact test name in this scaffold does not exist, substitute the closest existing test name from the audit — DO NOT invent names. If a COVERAGE row has no existing test (genuine gap per D-03), leave `test:` empty AND add the row to a `## Known Gaps` section at the bottom of the file for later plans to fill. Every populated `test:` cell MUST reference a real existing test.

    Per-register section MUST list all 35 registers in spu94_reg_t enum order (vLOUT, vROUT, mBASE, dAPF1, dAPF2, vIIR, vCOMB1, vCOMB2, vCOMB3, vCOMB4, vWALL, vAPF1, vAPF2, mLSAME, mRSAME, mLCOMB1, mRCOMB1, mLCOMB2, mRCOMB2, dLSAME, dRSAME, mLDIFF, mRDIFF, mLCOMB3, mRCOMB3, mLCOMB4, mRCOMB4, dLDIFF, dRDIFF, mLAPF1, mRAPF1, mLAPF2, mRAPF2, vLIN, vRIN). For registers whose direct test isn't obvious, point to `tests/unit/registers/test_register_roundtrip.c::test_roundtrip_<name>` (the existing per-register battery from Plan 02-05) — if that test-name pattern isn't present, use `tests/python/fuzz_process.py::main` which exercises all 35 registers via the 10^6-step fuzz.

    Implements D-01 (three sections), D-04 (pinned wayback URL at top).
  </action>
  <verify>
    <automated>python3 -c "import re; t = open('docs/COVERAGE.md').read(); assert '## Per-Register Coverage' in t; assert '## Per-Behavior Coverage' in t; assert '## Per-Spec-Paragraph Coverage' in t; assert 'web.archive.org/web/20260114082525' in t; rows = re.findall(r'^\\|\\s*\\`?[^|]*\\`?\\s*\\|[^|]*\\|\\s*\\`([^\\`]+)\\`', t, re.M); print(f'{len(rows)} test: rows found'); assert len(rows) >= 50, f'expected >=50 test: rows, got {len(rows)}'"</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/COVERAGE.md` exists.
    - `grep -c "^## Per-Register Coverage$" docs/COVERAGE.md` returns 1.
    - `grep -c "^## Per-Behavior Coverage$" docs/COVERAGE.md` returns 1.
    - `grep -c "^## Per-Spec-Paragraph Coverage$" docs/COVERAGE.md` returns 1.
    - `grep -c "web.archive.org/web/20260114082525/https://psx-spx.consoledev.net/soundprocessingunitspu/" docs/COVERAGE.md` returns at least 1.
    - `grep -cE "\\| \`v(LOUT\|ROUT\|IIR\|WALL\|APF1\|APF2\|COMB1\|COMB2\|COMB3\|COMB4\|LIN\|RIN)\`" docs/COVERAGE.md` returns at least 12 (all 12 v-prefix registers present).
    - `grep -cE "\\| \`m(BASE\|LSAME\|RSAME\|LDIFF\|RDIFF\|LCOMB[1-4]\|RCOMB[1-4]\|LAPF[12]\|RAPF[12])\`" docs/COVERAGE.md` returns at least 13 (all 13 m-prefix registers present).
    - `grep -cE "\\| \`d(APF[12]\|LSAME\|RSAME\|LDIFF\|RDIFF)\`" docs/COVERAGE.md` returns at least 6 (all 6 d-prefix registers for cross-check; dAPF1/2 + dL/R{SAME,DIFF}).
    - `python3 -c "import re; t=open('docs/COVERAGE.md').read(); rows=re.findall(r'\`(tests/[^\`]+)\`', t); assert len(rows)>=35, f'need >=35 test references, got {len(rows)}'"` exits 0.
  </acceptance_criteria>
  <done>COVERAGE.md scaffold exists with three sections, 35+ per-register rows in enum order, ~15-20 per-behavior rows, ~5+ per-spec-paragraph rows, pinned wayback URL at top, every populated `test:` cell references an existing test file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build check_coverage.py validator + positive/negative meta-tests + wire CI job</name>
  <files>scripts/ci/check_coverage.py, scripts/ci/test_check_coverage.py, tests/conformance/CMakeLists.txt, tests/conformance/test_coverage_map_integrity.py, tests/CMakeLists.txt, .github/workflows/ci.yml</files>
  <read_first>
    - scripts/ci/verify-flags.sh (existing validator pattern — regex + shell-out + per-row pass/fail)
    - scripts/ci/verify-readme-sections.sh (similar structural validator shape from Phase 6)
    - .planning/phases/07-verification-golden-files-witness-diff-modulation/07-RESEARCH.md § Code Examples — Example 1 (check_coverage.py skeleton)
    - docs/COVERAGE.md (created in Task 2 — the file the validator parses)
    - .github/workflows/ci.yml (CI job structure — add new job after existing ones, same SHA-pinned action pattern)
  </read_first>
  <behavior>
    - Test 1 (positive): Run `check_coverage.py` against the committed `docs/COVERAGE.md` → exits 0, stdout contains `PASS: <N> COVERAGE.md rows all green` where N >= 50.
    - Test 2 (negative - missing file): Copy COVERAGE.md to a tmp file, inject a row `| bogus | I16 | \`tests/unit/nonexistent_file.c::test_fake\` | |`, run the validator with `--file <tmp>` → exits 1, stderr contains `file not found`.
    - Test 3 (negative - missing test function): Inject a row referencing a real test file but a non-existent test name, run validator → exits 1, stderr contains `tests/unit/` and either `not found` or `returned non-zero`.
    - Test 4 (edge - empty test field): Inject a row with empty `test:` cell → exits 1, stderr contains `empty test: field` or similar gap marker.
  </behavior>
  <action>
    Step 1 — Build `scripts/ci/check_coverage.py` per RESEARCH.md Example 1, with these exact semantics:
    - Accepts optional `--file <path>` arg (defaults to `docs/COVERAGE.md`) so the negative meta-tests can point at tmp copies.
    - Parses every `test:` column value. Regex matches row cells of the form `` | [^|]* | [^|]* | `<path::testname>` | `` where the backticked cell in column 3 is the test reference.
    - For each `<path>::<test_name>`:
      - If `<path>` does not exist → print `FAIL: <spec>: file not found` to stderr; record failure.
      - If `<path>` ends with `.c` → run `ctest --test-dir build -R "^<test_name>$" --output-on-failure` with `subprocess.run([...], shell=False)` (D-02 + threat model: no shell injection from test-name field; reject rows whose `test_name` contains any of `;&|><$\`\n`).
      - If `<path>` ends with `.py` → run `pytest <path>::<test_name> -q --no-header` via `subprocess.run([...], shell=False)`.
      - Non-zero exit → record failure.
    - Handle rows with empty `test:` cell (which belong under `## Known Gaps` only) — if an empty `test:` appears outside `## Known Gaps`, emit `FAIL: empty test: field outside Known Gaps`.
    - Exit 0 if zero failures, else exit 1 printing `FAIL: ...` summary lines.
    - File MUST start with `#!/usr/bin/env python3` shebang and a module docstring naming the D-02 requirement.

    Step 2 — Build `scripts/ci/test_check_coverage.py` pytest module implementing Tests 1-4 above. Each negative test creates a tmp COVERAGE-style file in pytest's `tmp_path` fixture, injects the bad row, invokes `check_coverage.py --file <tmp>` via `subprocess.run`, asserts non-zero exit + expected stderr substring.

    Step 3 — Build `tests/conformance/CMakeLists.txt` that adds a pytest `add_test` entry invoking `pytest ${CMAKE_SOURCE_DIR}/tests/conformance/ ${CMAKE_SOURCE_DIR}/scripts/ci/test_check_coverage.py -q`. Wire into `tests/CMakeLists.txt` with `add_subdirectory(conformance)`.

    Step 4 — Build `tests/conformance/test_coverage_map_integrity.py`: structural meta-test asserting COVERAGE.md parses (`## Per-Register Coverage` / `## Per-Behavior Coverage` / `## Per-Spec-Paragraph Coverage` headers all present); per-register section has ≥35 rows; every non-Known-Gaps row has non-empty `test:` cell; pinned wayback URL line present.

    Step 5 — Append to `.github/workflows/ci.yml` a new job `coverage-map-check`:

    ```yaml
      coverage-map-check:
        name: coverage-map-check (TEST-01)
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
            uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
          - name: Install Phase 7 deps
            run: bash scripts/ci/install-phase7-deps.sh
          - name: Configure + build (need ctest for C tests referenced in COVERAGE)
            run: |
              cmake -S . -B build -G Ninja -DCMAKE_BUILD_TYPE=Debug
              cmake --build build
          - name: Run coverage-map check
            run: python3 scripts/ci/check_coverage.py
          - name: Run coverage-check meta-tests
            run: pytest scripts/ci/test_check_coverage.py -q
    ```

    Reuse the existing actions/checkout SHA `11bd71901bbe5b1630ceea73d27597364c9af683` (v4.2.2) already used elsewhere in ci.yml per Phase 1's SHA-pinning discipline.

    Threat mitigation (documented in `check_coverage.py` header comment): `subprocess.run([...], shell=False)` with argv-split; reject any row whose `test:` field contains shell metacharacters from the set `;&|><$\`\n` — mitigates T-07-A (CI script injection via test names).
  </action>
  <verify>
    <automated>python3 scripts/ci/check_coverage.py && pytest scripts/ci/test_check_coverage.py -q && pytest tests/conformance/test_coverage_map_integrity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/ci/check_coverage.py && head -1 scripts/ci/check_coverage.py | grep -q "python3"` succeeds.
    - `python3 scripts/ci/check_coverage.py` exits 0 and stdout final line matches `PASS: .* rows all green`.
    - `pytest scripts/ci/test_check_coverage.py -q` passes at least 4 test cases (positive + 3 negative).
    - `grep -q "shell=False" scripts/ci/check_coverage.py` (threat mitigation present).
    - `grep -qE "[\\\\\\\\\\\\\\$;&|><\\\\n\\\\\\`]" scripts/ci/check_coverage.py` shows rejection of shell metacharacters is coded.
    - `grep -c "coverage-map-check" .github/workflows/ci.yml` returns at least 2 (job name + one reference).
    - `grep -q "11bd71901bbe5b1630ceea73d27597364c9af683" .github/workflows/ci.yml` (SHA-pinned checkout action preserved).
    - `pytest tests/conformance/test_coverage_map_integrity.py -q` exits 0.
    - `ctest --test-dir build -R "^coverage_map_integrity$|^test_check_coverage$" --output-on-failure` exits 0 (tests discoverable via ctest).
  </acceptance_criteria>
  <done>Validator works on committed COVERAGE.md; positive + 3 negative meta-tests pass; integrity meta-test green; new CI job defined; threat T-07-A mitigated at source-code level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → `docs/COVERAGE.md` content | A malicious PR could inject a `test:` row whose backticked cell contains shell metacharacters, in the hope that the validator shells out unsafely |
| apt-get transport | Package manager fetches over HTTPS; signature-verified by apt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-A | Tampering + Elevation | `scripts/ci/check_coverage.py` parsing `docs/COVERAGE.md` test: field | mitigate | Use `subprocess.run([argv...], shell=False)` with argv-split; reject any `test:` value containing `;&|><$` backtick or newline (test name shell metacharacter allowlist); negative meta-test injects a metacharacter and asserts rejection |
| T-07-01-B | Tampering (supply-chain) | apt packages installed by `install-phase7-deps.sh` | accept | Apt's gpg-signed repositories provide the baseline trust; this is Debian/Ubuntu's normal supply-chain surface; a per-package pin is explicitly ruled out by D-14 (pin lives at the Docker base-image digest level, not at apt package level) |
| T-07-01-C | Information Disclosure | `check_coverage.py` stderr on failed test lookup could leak absolute filesystem paths from CI runner | accept | Leaked info is CI-runner ephemeral path (e.g., `/home/runner/work/...`) already visible in other CI log lines; not sensitive |
</threat_model>

<verification>
Run the full phase-plan suite after task 3:
- `bash scripts/ci/install-phase7-deps.sh` → PASS
- `python3 scripts/ci/check_coverage.py` → PASS with ≥50 rows green
- `pytest scripts/ci/test_check_coverage.py -q` → 4+ passed
- `pytest tests/conformance/test_coverage_map_integrity.py -q` → passed
- `ctest --test-dir build --output-on-failure` → all existing tests + new conformance tests green
</verification>

<success_criteria>
- [ ] Host toolchain installed; install script idempotent and committed
- [ ] `docs/COVERAGE.md` exists with three sections citing the pinned wayback URL
- [ ] All 35 registers appear in per-register section in spu94_reg_t enum order
- [ ] `check_coverage.py` validator ships + passes on committed COVERAGE.md
- [ ] 4 meta-tests (1 positive + 3 negative) all pass
- [ ] `coverage-map-check` CI job added with SHA-pinned checkout action
- [ ] Shell-injection threat T-07-01-A mitigated at source level
</success_criteria>

<output>
After completion, create `.planning/phases/07-verification-golden-files-witness-diff-modulation/07-01-SUMMARY.md` documenting: install script behaviour, COVERAGE.md audit decisions (which existing tests were mapped to which rows; which rows ended up in Known Gaps), validator regex shape, threat-mitigation language added, CI job wiring.
</output>
