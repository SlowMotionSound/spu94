---
context: phase
phase: 07-verification-golden-files-witness-diff-modulation
plan: 07-02
task: 3
total_tasks: 3
status: blocked_on_human_action
wave: 2
total_waves: 4
last_updated: 2026-04-23T21:42:51.148Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Running docker without group membership | Plan 07-02 Task 3 smoke test failed to execute because `ubuntu-studio` is not in the `docker` group; docker socket permission denied. Previously Plan 07-01 Task 1 also blocked on passwordless sudo. | advisory | Any future plan that shells out to `docker`, `sudo`, `apt-get`, or other auth-gated binaries must either (a) require the privilege is already granted (document in plan pre-reqs), or (b) emit a structured human-action checkpoint with the exact command to run. Do not try to silently escalate privileges. |

<current_state>
Phase 7 Wave 2, mid-plan 07-02. All file work is committed (regenerate_goldens.py, 50 goldens, meta-tests, Dockerfile.repro, reproducibility CI job). The final acceptance gate is a local docker smoke test which requires either `sudo docker` invocations or `ubuntu-studio` being added to the `docker` group. The executor emitted a clean checkpoint and paused. SUMMARY.md has not yet been written for 07-02.
</current_state>

<completed_work>

### Wave 1 (complete)
- Plan 07-01 — toolchain + COVERAGE.md scaffold + check_coverage.py validator + CI job. 5 commits, Self-Check PASSED. See `07-01-SUMMARY.md`.

### Wave 2 (plan 1 of 2 — 07-02 — 95% done)
- Task 1 (commit `a1b585a`): `scripts/regenerate_goldens.py` + 50 × `.wav` + 50 × `.sha256` under `tests/golden/` + `.gitignore` update
- Task 2 (commit `a6d0e87`): `scripts/test_regenerate_goldens.py` + `tests/conformance/test_goldens_present.py` + ctest registration
- Task 3 partial (commit `81d4bb6`): `Dockerfile.repro` pinned to `debian:bookworm-slim@sha256:5a2a80d11944804c01b8619bc967e31801ec39bf3257ab80b91070eb23625644` + `reproducibility` job added to `.github/workflows/ci.yml`
</completed_work>

<remaining_work>

Immediate (to finish 07-02):
- Run local docker smoke test: `sudo docker build -f Dockerfile.repro -t spu94-repro .` then `sudo docker run --rm spu94-repro`. Expected final stdout: `PASS: 50/50 goldens match`, exit code 0.
- Spawn continuation gsd-executor to write `07-02-SUMMARY.md` and commit as `docs(07-02): complete reproducibility plan`.

Then proceed through remaining phase 7 work:
- Wave 2 plan 2: **07-05** (hot-path allocation gate + pytest-benchmark harness). Shares `.github/workflows/ci.yml` and `.gitignore` with 07-02 — must run strictly sequentially after 07-02.
- Wave 3: **07-03** (witness-diff harness vs lv2-psx-reverb) + **07-04** (35-register × 3-mode modulation harness + LEVERS-CATALOG writer). Both touch `tests/conformance/CMakeLists.txt` — run sequentially.
- Wave 4: **07-06** (BIBLIOGRAPHY.md additive entries + DECISIONS.md ADR batch).
- Post-execution: code review gate, regression gate, schema-drift gate, gsd-verifier, ROADMAP update.
</remaining_work>

<decisions_made>

- Dockerfile.repro base image pinned by SHA digest (debian:bookworm-slim@sha256:5a2a80d11944804c01b8619bc967e31801ec39bf3257ab80b91070eb23625644), not tag, for true reproducibility.
- 50 golden .wav files committed in-tree (not .gitignored) — conformance CI requires them present.
- Orchestrator (not executor agents) owns STATE.md and ROADMAP.md writes. Executors explicitly skip those files.
- Sequential (not parallel) execution within waves where `files_modified` overlaps. Wave 2 (07-02 → 07-05) and Wave 3 (07-03 → 07-04) both have this constraint.
- 07-01 installed `lilv-utils` and `lv2-dev` for 07-03's witness-diff harness. Host Phase 7 toolchain is fully provisioned (scipy 1.17.1, pytest-benchmark 5.2.3, strace, sha256sum, lv2apply all present).
</decisions_made>

<blockers>

- **07-02 Task 3 docker smoke test**: user `ubuntu-studio` not in `docker` group. Workaround: run the build and run commands under `sudo`. Permanent fix: `sudo usermod -aG docker $USER`, then logout/reboot and re-login. Either unblocks the checkpoint.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured machine-readable state (plan/task numbers, commit hashes, remaining work)
2. `.planning/phases/07-verification-golden-files-witness-diff-modulation/07-02-PLAN.md` — the plan being executed; treat as authoritative for acceptance criteria
3. `.planning/phases/07-verification-golden-files-witness-diff-modulation/07-01-SUMMARY.md` — prior wave context (coverage-map validator contract that 07-02's goldens-presence test plugs into)
4. `.planning/phases/07-verification-golden-files-witness-diff-modulation/07-RESEARCH.md` (if present) — pitfalls catalogue

## Infrastructure State

- Git: branch `master`, HEAD `81d4bb6`. Orchestrator-owned `.planning/ROADMAP.md` and `.planning/STATE.md` modified but intentionally uncommitted (will be committed when orchestrator resumes).
- Phase 7 toolchain: installed and verified (`bash scripts/ci/install-phase7-deps.sh` → `PASS: Phase 7 host toolchain installed`).
- Docker: CLI 29.1.3 installed; daemon running; `ubuntu-studio` not in `docker` group (socket auth blocker).
- Python deps: scipy 1.17.1, pytest-benchmark 5.2.3, pytest available via user-site install.
- APT deps: `lilv-utils`, `lv2-dev` installed.
- Pending worktrees: none (the 07-01 checkpoint worktree was cleaned up when that plan completed on the main tree).
- CI: `.github/workflows/ci.yml` now has `coverage-map-check` (from 07-01) and `reproducibility` (from 07-02) jobs added. Not yet pushed.

<context>
Phase 7 is the verification phase — the goal is to ship the coverage map, 50-golden corpus with bit-reproducible Docker builds, witness-diff measurement, modulation harness, RT-safety gates, bibliography and ADRs. Wave 1 went cleanly. Wave 2 is 95% through plan 1 of 2; the final 5% is just running docker locally to verify the image actually reproduces. Two sudo/auth checkpoints in one phase suggests the plans should list required host-level privileges up front. Anthony is a recording/broadcast engineer (not a coder); checkpoints need plain-language exact-command instructions.
</context>

<next_action>
Resume steps (in order):

1. Ask Anthony for the last few lines of `sudo docker run --rm spu94-repro` output, OR ask him to run the two `sudo docker` commands from repo root.
2. On `PASS: 50/50 goldens match`: spawn a continuation gsd-executor with prompt "Write 07-02-SUMMARY.md for the completed plan (tasks 1-3 all green, smoke test manually verified by human on 2026-04-23, exit 0, expected stdout matched) and commit as `docs(07-02): complete reproducibility plan`. Do NOT update STATE.md or ROADMAP.md."
3. After SUMMARY commits: update task #2 status, call `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap update-plan-progress 07 07-02 completed`, then spawn Wave 2 plan 2 executor for 07-05 (sequential, no worktree, shares ci.yml + .gitignore).
4. Proceed with Wave 3 (07-03 then 07-04 sequentially), Wave 4 (07-06), then post-execution gates (code review, regression, schema drift, verifier, roadmap close).
5. If docker smoke test FAILs: paste full output; treat as a real reproducibility gap in the Docker image. Likely culprits per the 07-02 agent: locale leakage, dr_wav metadata deltas, gcc version divergence.
</next_action>
