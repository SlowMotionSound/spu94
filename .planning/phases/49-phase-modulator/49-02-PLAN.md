---
phase: 49-phase-modulator
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - src/plugin/PluginEditor.h
  - src/plugin/PluginEditor.cpp
autonomous: true
requirements: [PMOD-05]

must_haves:
  truths:
    - "Phase modulator controls (Rate, Depth, Enable) are visible in the sampler GUI"
    - "Enabling phase mod via the toggle produces audible polarity cycling on a playing voice"
    - "Phase mod enable toggle disables when tremolo, auto-pan, or AM is active (mutual exclusion visual)"
  artifacts:
    - path: "src/plugin/PluginEditor.h"
      provides: "Phase mod section GUI components (label, toggle, rate knob, depth knob)"
      contains: "phaseModSectionLabel"
    - path: "src/plugin/PluginEditor.cpp"
      provides: "Phase mod section layout, wiring to processor atomics, mutual exclusion visual feedback"
      contains: "phaseModEnableToggle"
  key_links:
    - from: "src/plugin/PluginEditor.cpp"
      to: "processor.getPhaseModEnabled()"
      via: "toggle onChange writes atomic"
      pattern: "getPhaseModEnabled"
    - from: "src/plugin/PluginEditor.cpp"
      to: "processor.getPhaseModSpeedHz()"
      via: "rotary knob onChange writes atomic"
      pattern: "getPhaseModSpeedHz"
---

<objective>
Phase modulator GUI controls in the sampler window

Purpose: Give the user visible, labeled controls to engage the phase modulator (Rate/Depth/Enable) following the same section layout as Tremolo, Auto-Pan, and AM sections. The phase modulator does NOT have a curve toggle (linear-only per ADR-0060).

Output: Phase Mod section with enable toggle, rate knob (Hz, 0.5-19), depth knob (0-100%) in PluginEditor.h/.cpp
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-phase-modulator/49-01-SUMMARY.md

<interfaces>
<!-- From Plan 49-01 (will exist when this plan executes) -->

From src/plugin/PluginProcessor.h (phase mod accessors created in 49-01):
```cpp
// Phase modulator controls (Phase 49: polarity oscillation through zero crossing)
std::atomic<bool>&  getPhaseModEnabled()  { return phaseModEnabled; }
std::atomic<float>& getPhaseModSpeedHz()  { return phaseModSpeedHz; }
std::atomic<float>& getPhaseModDepth()    { return phaseModDepth; }
```

From src/plugin/PluginEditor.h (existing GUI pattern -- AM section):
```cpp
// AM synthesis controls (Phase 48)
juce::Label amSectionLabel;
juce::ToggleButton amEnableToggle{"Enable"};
juce::Slider amRateKnob;
juce::Label amRateLabel;
juce::Slider amDepthKnob;
juce::Label amDepthLabel;
juce::TextButton amCurveButton{"Linear"};
```

From src/plugin/PluginEditor.cpp (AM section layout pattern):
- Section label positioned with setBounds
- Toggle, Rate knob, Depth knob, Curve button laid out in the effect section area
- Knob onChange lambdas write to processor atomics via getAmRateHz().store(...)
- Timer callback reads processor state to grey out toggle when higher-priority effects active
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase modulator GUI section with rate, depth, and mutual exclusion feedback</name>
  <files>src/plugin/PluginEditor.h, src/plugin/PluginEditor.cpp</files>
  <action>
  Add Phase Mod section to the sampler GUI following the identical pattern as the AM section, but simpler (no curve button -- linear-only per ADR-0060).

  In PluginEditor.h:
  - Add after the AM section members:
    juce::Label phaseModSectionLabel
    juce::ToggleButton phaseModEnableToggle with text "Enable"
    juce::Slider phaseModRateKnob (rotary)
    juce::Label phaseModRateLabel
    juce::Slider phaseModDepthKnob (rotary)
    juce::Label phaseModDepthLabel

  In PluginEditor.cpp constructor:
  - Initialize phaseModSectionLabel with text "Phase Mod" and the same bold font + style as other section labels
  - Configure phaseModRateKnob: Rotary style, range 0.5 to 19.0 Hz, default 4.0, suffix " Hz", skew factor for perceptual spacing (same as tremolo rate knob)
  - Configure phaseModDepthKnob: Rotary style, range 0.0 to 1.0, default 1.0, suffix "%", display multiplied by 100
  - Wire phaseModEnableToggle onChange to processor.getPhaseModEnabled().store(toggled)
  - Wire phaseModRateKnob onChange to processor.getPhaseModSpeedHz().store(value)
  - Wire phaseModDepthKnob onChange to processor.getPhaseModDepth().store(value)
  - addAndMakeVisible for all 5 components

  In PluginEditor.cpp resized():
  - Position the Phase Mod section AFTER the AM section, using the same column/row layout pattern. Follow the vertical stacking of effect sections. The section needs less width than AM since there is no curve button (just enable + rate + depth).

  In PluginEditor.cpp timerCallback():
  - Add mutual exclusion visual feedback: when tremoloEnabled OR autoPanEnabled OR amEnabled is true, set phaseModEnableToggle.setEnabled(false) and setAlpha(0.4f). When all three are false, set enabled(true) and alpha(1.0f). Same pattern as existing AM greying.
  - When phaseModEnabled is true, grey out tremolo/auto-pan/AM toggles (bidirectional exclusion feedback, same as auto-pan does with tremolo).

  Keep the same PS1 color scheme (teal accents for section labels, dark gray background).
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb && cmake --build build --config Release 2>&1 | tail -5</automated>
  </verify>
  <done>Phase Mod section visible in sampler GUI with Enable toggle, Rate knob (0.5-19 Hz), Depth knob (0-100%), mutual exclusion greying when other sweep effects are active, bidirectional exclusion feedback, project builds clean in Release</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI slider -> atomic | Slider values constrained by JUCE range; no additional validation needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Tampering | GUI slider range | accept | JUCE Slider range constraints (0.5-19 Hz, 0.0-1.0 depth) prevent out-of-bounds; processBlock already clamps at point of use per T-49-01 |
</threat_model>

<verification>
- cmake --build build --config Release passes
- Phase Mod section visible in sampler window when launched
- Toggle enables/disables phase modulator
- Rate and Depth knobs write to processor atomics
- Mutual exclusion visual: toggle greys out when tremolo/auto-pan/AM active
</verification>

<success_criteria>
Phase Mod section with Enable, Rate (0.5-19 Hz), and Depth (0-100%) is visible and functional in the sampler GUI, mutual exclusion visual feedback works bidirectionally with other sweep effects, project builds clean.
</success_criteria>

<output>
Create `.planning/phases/49-phase-modulator/49-02-SUMMARY.md` when done
</output>
