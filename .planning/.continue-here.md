---
context: default
phase: none
task: 0
total_tasks: 0
status: complete
last_updated: 2026-05-30T19:55:34.970Z
---

# Paused at a clean boundary — NOTHING half-finished

> This is a session-end checkpoint, **not** a mid-task pause. The post-v1.11.0
> codebase cleanup is complete, the working tree is clean, and the full suite is
> green. There is no in-progress task to resume. Do not re-run the cleanup audit.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Piped exit code masks failure | `cmd \| tail` returns *tail's* exit code (0), so a real build/test failure looks like success. Bit once this session — the #23 plugin build failed but the backgrounded `cmake ... \| tail` reported exit 0. | advisory | Capture the real status: `cmd > log 2>&1; echo "EXIT=$?"` or `${PIPESTATUS[0]}`. Never trust a piped command's exit code as the underlying command's. |
| Trusting audit labels | CODEBASE-AUDIT.md labels were wrong twice (spu94_voice.h "absent from .h body" — it wasn't; spu94_zero_bytes "consolidate" — it's a deliberate invariant). | advisory | Verify every removal/change with your own eyes (grep all refs) and let the build/tests be the final word before committing. |

<current_state>
Between milestones. v1.11.0 (Live Input Sampling) shipped + tagged. The
post-v1.11.0 cleanup pass (`.planning/CODEBASE-AUDIT.md`) is DONE: 33/39 items
resolved, 6 (Tier 4 #33-38) deliberately deferred. Working tree clean, full suite
118/118 green (ctest -E packaging).
</current_state>

<completed_work>

This session (5 commits, `6019925` → `c226bdf`):
- Tier 3 C-core hygiene (#22 stale includes, #25 static CLI fn, #29 stale comments; #24 kept) — `6019925`
- Tier 3 Python hygiene (#26 dead imports, #27 dead helper + orphaned import, #28 annotation) — `16b1427`
- Tier 3 plugin header/comments (#23 include hygiene, #30 orphan comment, #31 renumber, #32 diagnostic/dup) — `247cec8`
- #39 test gap: wired + ran test_cli_mixer_dac (12 tests, all green; suite 119→120) — `d3034cb`
- Bookkeeping close-out (STATE/AUDIT/TODO; Tier 4 deferred; stale handoff removed) — `c226bdf`
</completed_work>

<remaining_work>

None for the cleanup. The only open audit items are Tier 4 (#33-38), **intentionally
deferred** — see decisions below. Next move is a new milestone or a TODO.md item.
</remaining_work>

<decisions_made>

- **Tier 4 dup-code consolidation (#33-38) DEFERRED, not declined.** Folding the 4
  VCA-effect blocks (tremolo/auto-pan/ring-mod/AM) into one shared helper is a real
  future optimization, parked for a dedicated **critical-listening session**: it's
  sound-shaping code with no test guard, so a "behaviour-preserving" merge can only
  be verified by ear. Tracked in `TODO.md`. User: "I don't want it lost as a
  potential optimization target, I just don't have the bandwidth right now."
- **Kept 3 items the audit flagged for removal/change** (audit is a starting point,
  not gospel): `spu94_zero_bytes` (documented no-libc/MCU-link invariant),
  `spu94_voice.h` stays in `PluginProcessor.h` (header declares `spu94_adsr_state_t`),
  3 `from __future__ import annotations` lines (load-bearing).
</decisions_made>

<blockers>
- None.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — current position (cleanup complete, between milestones)
2. `.planning/CODEBASE-AUDIT.md` — the audit; Tier 4 header carries the DEFERRED decision + a safe how-to-do-it-later plan
3. `.planning/TODO.md` — Tier 4 deferred optimization + the 2 environmental packaging timeouts

## Infrastructure State
- Build: `build/` is warm CMake **Release**; full plugin + libs + tests build green.
- Tests: full suite 118/118 (ctest -E packaging). The 2 packaging tests time out on
  network pip install (environmental) — skippable via `SPU94_SKIP_PIP_INSTALL` /
  `SPU94_SKIP_WHEEL_BUILD`.
- Background processes: none.

<context>
User (Anthony) is a recording/broadcast engineer, not a coder — handle invisible
internal tidying and report progress in plain language; surface only decisions that
touch the sound or the interface. The whole cleanup was post-milestone housekeeping;
none of it changed the sound or the UI.
</context>

<next_action>
Nothing to resume. When ready, start a new milestone (`/gsd:new-milestone`) or pick
an item from `.planning/TODO.md`. Do NOT re-open the cleanup audit — Tier 4 is
deferred on purpose.
</next_action>
