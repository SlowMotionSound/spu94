---
context: default
phase: none
task: 0
total_tasks: 0
status: paused
last_updated: 2026-05-30T18:32:55.809Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Disconnect-and-leave dead code | Removing a feature by only unplugging it (deleting the call site) while leaving the implementation orphaned in the tree. Manifested as the whole CODEBASE-AUDIT backlog; `AdsrDisplay.h` left behind after the ADSR draw was unwired at `PluginEditor.cpp:440`. | advisory | **Cleanup is part of the commit process:** after any removal — grep for orphans, delete them, Release build, commit change+cleanup together. No commit lands dead code. No automated check (user declined) — manual sweep every commit. |
| Trusting audit labels without verifying | Audit tagged `AdsrDisplay.h` as "dead weight"; it was actually the planned sustain-level reference-line renderer — real semantic value. | advisory | Verify with own eyes (grep all refs) before deleting. Flag semantic/creative value to the user before removing, even when git-recoverable. |

<current_state>
Between milestones — v1.11.0 shipped/tagged. Working through `.planning/CODEBASE-AUDIT.md` (38 items + 1 test gap). **Tier 1 (#1–5) and all of Tier 2 (#6–21) are complete.** Side fixes this session: plug15 alloc-hang, adsr sustain-zero test, stale init-preset removal. Next: Tier 3 hygiene (#22–32). No blockers.
</current_state>

<completed_work>

This session:
- Tier 1 inert dead weight (#1, #3, #4, #5) → one commit `47ca8c0`: `tests/golden_v1.2/` (110 files, ~19MB superseded v1.2 DAC goldens), `main.sh` (scaffold stub), `tests/.gitkeep` (vestigial), `tests/cli/__pycache__/` (regenerable cache).
- Tier 1 #2 → commit `37448d9`: `src/plugin/AdsrDisplay.h` FULLY deleted (not just unplugged), Release standalone build verified green.
- Established + saved the core working agreement (see Required Reading): "remove" = full delete; cleanup is part of the commit process; no automated checks.
- Ticked CODEBASE-AUDIT.md #1–5; synced STATE.md.
</completed_work>

<remaining_work>

Recommended order from here:
1. **Tier 3 #22–32** — mechanical hygiene (stale includes/comments/imports).
2. **Tier 4 #33–38** — duplicate-code consolidation; dedicated pass with VCA-effect tests as a regression guard.
3. **#39** — wire + RUN `test_cli_mixer_dac.py` (12 never-executed CLI tests). Highest value / only item that could expose a real bug. Deferred earlier because the user chose lowest-stakes-first; still pending.

Done this session: all of **Tier 2** — C-core #6–11 (one ABI-bump commit) + JUCE #12–21 (two commits: plugin internals `ce4e670`, then sampler-GUI/loader leftovers). Plus side fixes: plug15 alloc-hang, adsr sustain-zero test (true silence), stale `init` preset removal (C back to 10 Sony presets, consistent with Python). Full suite 117/117 green excluding the 2 environmental packaging timeouts (the only remaining reds, logged in `.planning/TODO.md`).
</remaining_work>

<decisions_made>

- "Remove" means fully delete the code (file + refs), not just disconnect it — git is the safety net.
- Cleanup is part of the commit process (change → grep orphans → delete → build → commit together); no dead code ever lands in a commit.
- No automated pre-commit checks — user declined; **do not re-offer**. Manual sweep every commit.
- Tier 1 grouping: inert deletions bundled, AdsrDisplay.h separate with build check (user's choice).
- AdsrDisplay.h held the planned ADSR sustain-reference-line renderer; recoverable at `git show 47ca8c0:src/plugin/AdsrDisplay.h`.
- DSP math untouchable; CLI not cuttable; protective code (limiters/guards) never removed without asking.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/CODEBASE-AUDIT.md` — the cleanup checklist; this is the work. Tier 1 + Tier 2 done; start Tier 3 (#22–32).
2. Memory `feedback_remove_means_delete` — the working agreement; apply to every removal this session and beyond.

## Infrastructure State
- Build: `build/` configured CMake **Release**, warm; target `spu94_plugin_Standalone` builds green.
- Background processes: none.
- Model: session ran on `opus-4-8[1M]`; user's saved default is `opus-4-6[1m]` (4.6 is his preferred model). Resuming will use 4.6 unless switched — that's fine.

<context>
User is NOT a coder — a recording/broadcast engineer. Use plain language, define jargon, drip one decision at a time, no menus. The pattern we're cleaning is exactly the thing he flagged: features unplugged from the UI/signal path but never deleted. Verify everything with your own eyes (grep) before deleting; the audit's "dead weight" labels are a starting point, not gospel. One change per build+verify cycle.
</context>

<next_action>
Start with: Tier 3 #22–32 — mechanical hygiene (stale includes, stale "Phase N will…" comments, ~17 stale Python imports, dead test helper, a couple of judgment items). Working agreement: "remove" = full delete; grep for orphans and excise them in the same commit; build Release + run the suite to verify; no automated checks. Lesson from this session: do NOT walk the user through invisible code internals (he found it confusing) — handle them and report progress; only surface things that touch the sound or the interface.
</next_action>
