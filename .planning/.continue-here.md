---
context: default
phase: post-beta
task: 0
total_tasks: 0
status: paused
last_updated: 2026-05-15T23:30:00Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Missing SPU signal chain components | ADPCM was implemented in isolation without the downsample + Gaussian interpolation that surrounds it on real hardware. Produced audible mid-high distortion. | blocking | Before implementing any SPU component, map the COMPLETE signal chain it lives in from psx-spx. Cross-check against DuckStation source. Never implement a stage without its neighbors. |
| Dual-counter voice path (non-faithful) | Used two separate phase accumulators (decim_counter + gauss_counter) instead of the PS1's single pitch counter. Produced 98%+ sample divergence from hardware-faithful architecture. | blocking | The PS1 SPU uses ONE pitch counter per voice. Bits 12+ = sample index, bits 4-11 = Gaussian interpolation index. Never split these into independent state. See .planning/research/DEEP-SPU-VOICE-PATH.md. |
| Adding textbook-DSP corrections without flagging | Added an AA filter before decimation because "decimation needs AA filtering" — standard DSP but NOT what the PS1 does. The real hardware has no AA filter at that stage. | blocking | Default signal path must match real PS1 hardware exactly. If something seems "missing" from the spec (no AA filter, no dither, truncation), it's probably intentional — it IS the sound. Flag departures in conversation before implementing. |
| Substituting own estimates for user-provided values | Anthony said 950px, I used 700px. Three times. | advisory | When the user gives a specific number, use it exactly. |

<current_state>
v1.7 DAW Plugin Port milestone phases 21-26 complete. Post-beta refinement session shipped three changes:

1. Single-counter voice path (PS1-faithful architecture — one pitch counter drives both decimation and Gaussian interpolation)
2. Fast/Slow morph speed toggle (Fast 0-0.5s / Slow 0.5-8s)
3. Gaussian interpolation defaults ON (matches real PS1 hardware)

All committed. Beta artifacts on ~/Desktop are STALE — built before these changes. Next step is rebuilding plugin artifacts for all 3 platforms.
</current_state>

<completed_work>

- Single-counter voice path: replaced dual decim_counter/gauss_counter with one voice_counter matching PS1 SPU hardware — Done
- Fast/Slow morph speed: mauve button strip on MorphPanel, Fast 0-0.5s / Slow 0.5-8s — Done
- Gaussian interpolation defaults ON — Done
- Golden files regenerated (80 total: 50 reverb + 30 ADPCM) — Done
- Research document: .planning/research/DEEP-SPU-VOICE-PATH.md — Done
</completed_work>

<remaining_work>

- Rebuild beta plugin artifacts (Linux local, macOS + Windows via CI) with all three changes
- Distribute updated builds to testers
- Phase 27 (Code Signing) remains conditional — only if testers report install friction
</remaining_work>

<decisions_made>

- Single-counter replaces dual-counter: real PS1 uses one pitch counter, test showed 98%+ sample divergence
- Gauss defaults ON: PS1 hardware has no Gauss bypass, off-by-default was textbook-DSP instinct
- Slow morph ceiling 8 seconds: tried 60s (feedback blowup), 1.5s (too short), 5s (good), 8s (sweet spot)
- AA filter stays as opt-in toggle (off by default): PS1 has no AA on voice decimation
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/research/DEEP-SPU-VOICE-PATH.md` — PS1 SPU voice path internals (pitch counter bit fields, decode timing, Gaussian indexing)
2. `src/spu94/spu94_process.c` lines 54-155 — the single-counter voice path implementation
3. `src/spu94/spu94_state_internal.h` lines 155-178 — voice_counter and Gaussian ring state fields
4. `.planning/STATE.md` — current project state

## Infrastructure State
- Local plugin installed at ~/.vst3/SPU-94.vst3 and ~/.lv2/SPU-94.lv2 (STALE — pre-single-counter)
- Beta artifacts on ~/Desktop (STALE — need rebuild)
- GitHub repo is private (was temporarily public for CI builds)
- No background processes running

<context>
Session started with signal flow discussion. Anthony asked about the decimator and why Gauss upsamples to 44.1k only to be decimated again by the reverb FIR. Then asked about the phase accumulator. Investigation revealed the dual-counter architecture was non-faithful to PS1 hardware. Research (spawned gsd-phase-researcher) confirmed PS1 uses a single pitch counter. Implemented, tested with comparison tools, verified 98%+ divergence. Then added Fast/Slow morph speed — Anthony identified slow transitions as "secret sauce" for evolving feedback textures. Iterated ceiling by ear: 60s (blowup) -> 1.5s (too short) -> 5s (nice) -> 8s (sweet spot). Anthony also flagged that I should not add textbook-DSP corrections (like AA filters) without flagging them as departures from spec.
</context>

<next_action>
Start with: Rebuild beta plugin artifacts for all 3 platforms (Linux local build, macOS + Windows via GitHub Actions CI). Then distribute to testers replacing the stale builds on ~/Desktop.
</next_action>
