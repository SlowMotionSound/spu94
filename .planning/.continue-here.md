---
context: default
phase: none
task: 0
total_tasks: 0
status: paused
last_updated: 2026-05-29T21:25:13.000Z
---

<current_state>
Between milestones (v1.11.0 Live Input Sampling shipped/tagged). A full-codebase cleanup audit is COMPLETE and verified — but NO fixes have been applied yet. The deliverable is a master verification & checklist at `.planning/CODEBASE-AUDIT.md`: 38 cleanup items across 4 tiers plus 1 real test gap. Resume = work through that checklist.
</current_state>

<completed_work>

This session:
- Audited the full codebase for dead code, stale/unused code, dead paths, duplicates, and inefficiencies. Framework: `~/.claude/agents/Code Optimization Agent.md` (measure-first, behavior-preserving).
- Two-pass blind methodology: Pass 1 (Opus 4.6, 3 parallel agents) produced 29 items. Pass 2 (Opus 4.8, 3 fresh agents) re-audited blind. Every disagreement reconciled by direct grep.
- Net result: all 29 of pass-1's items held; pass-2 added 9 verified net-new and corrected 1 false negative (checkModified is dead, not live).
- Wrote master checklist to `.planning/CODEBASE-AUDIT.md`.
</completed_work>

<remaining_work>

Execute the audit. Nothing applied yet. Recommended order (from the checklist):
1. #39 first — wire the unregistered `test_cli_mixer_dac.py` into `tests/cli/CMakeLists.txt` and RUN it. Only item that can expose a hidden bug.
2. Tier 1 — pure deletions (~285 lines + 19 MB orphaned goldens), near-zero risk.
3. Tier 3 — mechanical hygiene (stale includes, comments, imports).
4. Tier 2 C core (#6-11) — bundle as ONE ABI-bump commit (these are exported API).
5. Tier 2 JUCE (#12-21) — one change per build+verify cycle.
6. Tier 4 — duplicate-code consolidation last; dedicated pass with VCA-effect tests as regression guard.
</remaining_work>

<decisions_made>

- DSP math is untouchable — no changes to truncation, fixed-point, or reverb/voice algorithms. Cleanup only. Bit-accuracy IS the sound.
- Dead C functions in public headers (#6-11) are an ABI change — batch on next intentional ABI bump, not piecemeal.
- The CLI is NOT cuttable. Anthony doesn't use it directly, but `regenerate_goldens.py` + `witness_diff.py` drive it to build/validate the golden corpus.
- Execution method: one change per build+verify cycle, Release build, commit after each. No batching (especially GUI).
- Do not remove protective code (limiters/guards) even if it looks unused — flag and ask first.
- Blind-pass + grep verification is the trusted method — neither audit pass alone was complete or correct.
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/CODEBASE-AUDIT.md` — the master verification & checklist. This is the work.

<context>
User prefers Opus 4.6 for the interactive fix work (the collaboration style fits; 4.8 was used only as a silent second-pass workhorse). When resuming the fixes, drive on 4.6. The audit found no behavioral bugs on its own — #39 (running the 12 never-executed CLI tests) is the only item that might surface one.
</context>

<next_action>
Open `.planning/CODEBASE-AUDIT.md` and start with item #39 — wire `test_cli_mixer_dac.py` into `tests/cli/CMakeLists.txt`'s `_cli_tests` list and run it. Then proceed to Tier 1 deletions. One change per build+verify cycle.
</next_action>
