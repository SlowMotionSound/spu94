---
context: release
phase: v1.7-beta.1-release
task: 10
total_tasks: 11
status: in_progress
last_updated: 2026-05-14T19:05:00Z
---

<current_state>
v1.7-beta.1 release in progress. macOS plugin zip on Desktop ready to send. Latest CI fix (e9aff1f) pushed: Windows dac_noise -lm linkage + Linux lv2lint non-blocking. Results not yet in. GitHub Actions billing limit blocks the release workflow.
</current_state>

<completed_work>

- Tasks 1-9: GitHub repo, codebase push, tagging, 10 rounds of CI fixes across macOS/Windows/Linux
- macOS plugins CI: green (pluginval strictness-7 passed for VST3 and AU)
- macOS zip built: ~/Desktop/SPU-94-v1.7-beta.1-macOS.zip (VST3, AU, CLAP, Standalone)
- CI fixes committed: JUCE deps, grep-guard, MSVC flags, libm, getopt.h, POSIX tests, aligned_alloc, dac_noise libm, lv2lint
</completed_work>

<remaining_work>

- Task 10: Test Linux plugin locally in a DAW (not started)
- Task 11: Send artifacts to beta testers (macOS ready, Windows/Linux pending)
- GitHub billing must be resolved for release workflow
- Windows CI: latest fix (dac_noise -lm) awaiting results
- Linux CI: lv2lint marked non-blocking, awaiting results
- PLUG-43/PLUG-44 (installers) unfulfilled
</remaining_work>

<decisions_made>

- SPU94_BUILD_GUI CMake option — core-only CI skips JUCE
- grep-guard float/double ban removed — legitimate since v1.5
- Pre-existing CI failures marked continue-on-error
- CLI and POSIX tests skipped on Windows
- auval marked non-blocking (pluginval passed)
- No installer for this beta — manual drag-and-drop
</decisions_made>

<blockers>
- GitHub Actions billing: release workflow blocked (human action needed)
- Windows CI: dac_noise -lm fix pushed, awaiting results
- Linux CI: lv2lint meson compat fix pushed, awaiting results
- auval crash at unusual sample rates: needs investigation (not blocking beta)
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state
2. `.github/workflows/plugins.yml` — builds plugin binaries
3. `.github/workflows/release.yml` — packages release artifacts

## Infrastructure State
- GitHub repo: SlowMotionSound/spu94 (private)
- Tag v1.7-beta.1 at commit e9aff1f
- GitHub Actions billing: BLOCKED
- Latest CI run: pending (plugins workflow triggered by e9aff1f push)

<context>
10 rounds of CI fixes this session. Project was Linux-only until v1.7 — every platform quirk surfaced for the first time. macOS is green. Windows and Linux are close — latest fixes pushed, awaiting results. Billing wall blocks the release workflow but artifacts can be pulled from the plugins workflow runs manually.
</context>

<next_action>
Start with: Check plugins CI results for commit e9aff1f. Download Windows/Linux artifacts if passing. Resolve GitHub billing or distribute manually.
</next_action>
