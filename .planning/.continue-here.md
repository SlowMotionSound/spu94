---
context: v1.10.0-uat
phase: 55-effects-uat
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-05-27T22:30:00Z
---

<current_state>
v1.10.0 Voice Dynamics — Phase 55 (Effects UAT) in progress.
Three items shipped this session: removed Morph Grit FRACT mode (PS1 uses integer
addressing only), fixed GUI freeze on oversized sample loads, extended preset format
to save full system state. All five effect modes UAT-verified. ADSR calibration
rework from prior session confirmed — all modes use direct PS1 rate indexing.
</current_state>

<completed_work>

This session (2026-05-27):
- Removed Morph Grit FRACT mode (afbcae1):
  - FRACT was linear interpolation between buffer positions — functionally a lowpass
  - Stripped from C core (reverb body, state, slew, preset IO, public API),
    plugin (parameter, GUI, state serializer), and tests
  - Binary state format unchanged (slot 3 reserved, writes 0.0f)
- Fixed oversized sample load freeze (afbcae1):
  - Early bail-out before SRC_SINC_BEST_QUALITY resampling when ADPCM output
    would exceed 512KB voice RAM
- Extended preset save/load with full system state (4f00269):
  - [voice] — pitch, vol L/R, loop, AA, NON, PMON, noise shift, fader/send/drive,
    encode rate, start/end markers
  - [adsr] — enabled, attack, decay, sustain lvl/rate, release, lin/exp toggles
  - [effects] — mode, shape, per-mode speed/depth/curve/ratio for all 5 types
  - [mod_bus] — pitch/vol/pan depth
- Cleaned up stale state references:
  - Removed "piecewise knob curve" claims about attack (was already direct indexing)
  - Updated STATE.md phase map (52-54 done), session dates, progress
  - Removed stale Split-Output Bus blocker section (Phase 51 done)

Prior sessions (already committed):
- ADSR calibration rework (984608a, 0568769)
- Pan-during-effects bug fix (63ed386)
- 8-sample anti-click ramp on saw retrigger (09e7ff4)
- Old VCA ramp GUI removal (09e7ff4)
</completed_work>

<remaining_work>

- One-shot trigger: click to play full ADSR without holding
- GUI cleanup: Noise section, Speed encoder musical divisions
- Sidechain Duck UAT (blocked — needs MIDI controller)
- Preset extension UAT: save/load cycle with settings verification
</remaining_work>

<decisions_made>

- Remove FRACT mode: PS1 hardware uses integer halfword addressing only. FRACT was
  a linear interpolation overlay that acted as a lowpass filter — dulled morph character
- Presets save settings only, not sample audio (Anthony's choice)
- Extend existing .spu94 text format with new INI sections — backwards compatible
- All ADSR modes use direct PS1 rate indexing (confirmed, stale docs cleaned)
</decisions_made>

<blockers>
Sidechain Duck UAT blocked on MIDI controller availability.
</blockers>

## Required Reading (in order)
1. This file
2. src/plugin/PluginProcessor.cpp — savePresetToString() and loadPresetFromString() for new preset sections
3. src/spu94/spu94_reverb.c — reverb_buf_read_tap() is now integer-only (FRACT removed)

## Infrastructure State
- No background processes running
- Build is Release
- Working tree clean (all committed)

<next_action>
1. UAT the new preset save/load — save with settings, change everything, load back, verify
2. One-shot trigger (click to play full ADSR without holding)
3. GUI cleanup: noise section, speed divisions
4. Sidechain Duck UAT when MIDI available
</next_action>
