---
context: v1.10.0-uat
phase: 51-gui-integration
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-05-25T17:06:52Z
---

<current_state>
v1.10.0 Voice Dynamics & Stereo Effects — all 9 phases (43-51) executed.
UAT in progress. Split-output bus shipped. Stereo widener dropped (SPU can't
do true stereo decorrelation). AM and Phase Mod GUI repositioned to right half
of sampler panel but not yet visually verified or UAT tested.
</current_state>

<completed_work>

This session:
- Implemented spu94_process_split() in C core (d3b8599)
  - Writes voice (dry+ADPCM+sampler) and reverb to separate int16 buffers
  - Original spu94_process() untouched (bit-identical)
  - Both standalone and plugin paths rewired to side-limit reverb only
- Removed stereo widener entirely (d3b8599)
  - PS1 SPU has no native stereo decorrelation mechanism
  - vol_l/vol_r offset was panning, not widening — mis-designed feature
  - Removed: DSP, GUI (slider + mono indicator), atomic, timer callback, layout
  - Also removed non-native M/S post-processing boost
- UAT verified: Tremolo, Auto-Pan, Mod Bus (noise-to-pitch)
  - Mod bus clicks at max vol/pan depth are authentic (no SPU smoothing)
- Moved AM Synthesis and Phase Mod GUI to right half of sampler panel (uncommitted)
- Added deferred ideas: pitch quantizer for mod bus, raw LFSR CV for Eurorack
</completed_work>

<remaining_work>

- Verify AM/Phase Mod GUI is visible in sampler window right half
- UAT AM Synthesis
- UAT Phase Modulator
- UAT Sidechain Duck (blocked — needs MIDI controller)
- GUI layout polish pass
- Commit layout changes
- Update STATE.md to reflect widener dropped, milestone status
</remaining_work>

<decisions_made>

- Split-output bus: side-limit reverb only, voice passes clean
- Stereo widener dropped — not physically possible on PS1 SPU
- Non-native M/S post-processing removed — Anthony requires SPU-faithful DSP only
- Mod bus clicks at max depth are expected (authentic SPU behavior)
- Side limiter MUST remain on reverb output path (Anthony's explicit instruction)
- Overflow GUI controls go in the right half (x=410+) of the sampler panel, NOT in the main window, NOT stacked below existing content off-screen
</decisions_made>

<blockers>
Sidechain duck UAT blocked — needs MIDI input to trigger KON on source voice.
C-level test (test_sidechain_duck_trigger_and_recovery) passes.
</blockers>

## Critical Anti-Patterns

| Pattern | Description | Severity |
|---------|-------------|----------|
| Off-screen GUI placement | Repeatedly stacked controls below visible area. Anthony explicitly requires overflow items placed in visible empty space (right half of sampler panel). NEVER stack below y~900 in sampler window. | blocking |
| Non-native DSP | Added M/S width boost (textbook DSP) to a PS1-faithful project. All creative effects must use native SPU mechanisms only. Flag any departure BEFORE implementing. | blocking |
| Promising impossible features | Designed stereo widener without verifying the SPU has a mechanism for stereo decorrelation. Validate physical feasibility of proposed effects against SPU architecture before committing to a phase. | advisory |

## Required Reading (in order)
1. This file — blocking anti-patterns above
2. src/spu94/spu94_process.c — the split function starts after spu94_flush()
3. src/plugin/PluginProcessor.cpp — split-bus wiring in both standalone and plugin paths

## Infrastructure State
- No background processes running
- Build is Release, standalone target clean
- 27/27 critical DSP tests pass (process, sweep, voice, golden, rt_safety)
- One uncommitted file: src/plugin/PluginEditor.cpp (layout changes)

<next_action>
1. Launch standalone, verify AM and Phase Mod controls are visible
2. If not visible, adjust y positions — they should be at x=410, near top of sampler panel
3. UAT AM Synthesis and Phase Modulator
4. Commit layout changes
5. Update STATE.md — drop widener from phase map, update session continuity
</next_action>
