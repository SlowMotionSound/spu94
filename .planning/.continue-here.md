---
context: release
phase: v1.7-beta.1-release
task: 5
total_tasks: 11
status: in_progress
last_updated: 2026-05-14T04:08:18.583Z
---

<current_state>
Pushing v1.7-beta.1 release to GitHub. CI failed on macOS and Windows — two fixes applied locally but not yet committed or verified. Linux build already exists as a tarball on Desktop.
</current_state>

<completed_work>

- GitHub repo created: SlowMotionSound/spu94 (private, HTTPS)
- Full codebase pushed to origin/main
- v1.7-beta.1 tag pushed, triggered release.yml CI
- CI failures diagnosed: macOS (C99/C11 _Static_assert mismatch) and Windows (.lib name collision)
- Both fixes edited in local files
- Linux tarball built locally: ~/Desktop/SPU-94-v1.7-beta.1-linux.tar.gz (13 MB)
</completed_work>

<remaining_work>

- ~~Verify Linux build still passes with both fixes~~ — DONE (100% clean)
- Commit the two fix files: include/spu94/spu94_q15.h, src/spu94/CMakeLists.txt
- Delete old v1.7-beta.1 tag locally and on remote
- Re-tag v1.7-beta.1 on the new commit and push
- Wait for CI to pass on all 3 platforms (Linux, macOS, Windows)
- Test Linux plugin in a DAW (Anthony's local testing)
- Send GitHub Release page link to beta testers for macOS/Windows downloads
</remaining_work>

<decisions_made>

- GitHub repo is private at SlowMotionSound/spu94
- HTTPS protocol for git (simpler setup, no SSH keys)
- macOS fix: added pre-C11 fallback in SPU94_STATIC_ASSERT macro (negative-array-size trick)
- Windows fix: static library OUTPUT_NAME changed to spu94_static on WIN32 to avoid .lib collision with shared library's import lib
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `include/spu94/spu94_q15.h` lines 19-25 — the SPU94_STATIC_ASSERT macro with the new C99 fallback
2. `src/spu94/CMakeLists.txt` lines 46-55 — the WIN32 conditional for static lib output name

## Infrastructure State
- GitHub repo: https://github.com/SlowMotionSound/spu94 (private)
- Git remote `origin` configured via `gh` (HTTPS + credential helper)
- v1.7-beta.1 tag exists on remote but points to pre-fix commit (needs delete + re-tag)
- Linux tarball on Desktop: ~/Desktop/SPU-94-v1.7-beta.1-linux.tar.gz (13 MB, built from pre-fix code — rebuild after fixes if needed)

<context>
Two straightforward CI fixes. The macOS one is a header portability issue — the public header claimed C99 compatibility but used a C11 feature. The Windows one is a CMake naming collision specific to how Windows handles shared library import libs (.dll produces a companion .lib import library, which collided with the static .lib). Both fixes are mechanical and low-risk. Just need to verify, commit, re-tag, and push.
</context>

<next_action>
Start with: commit both fix files (spu94_q15.h + CMakeLists.txt), delete old v1.7-beta.1 tag locally and on remote, re-tag, push to re-trigger CI on all 3 platforms. Linux build already verified clean.
</next_action>
