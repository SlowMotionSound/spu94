---
context: default
milestone: v1.7
milestone_name: DAW Plugin Port
phase: none
task: research-or-skip-decision
total_tasks: none
status: paused_pending_gsd_update_and_research_decision
last_updated: 2026-05-10T23:30:00Z
---

# Resume Notes — v1.7 DAW Plugin Port (paused mid-setup)

v1.7 was opened in this session. PROJECT.md and STATE.md are committed. The workflow is paused at the **research-vs-skip** decision because the `gsd-sdk` CLI shim isn't on PATH and the user is restarting the session to run `/gsd-update` first.

<current_state>
- Branch: `main` at commit `04df431` (docs: start milestone v1.7 DAW Plugin Port)
- Milestone: **v1.7 DAW Plugin Port** opened, paused before requirements gathering
- No code in flight; no uncommitted files (clean working tree apart from pre-existing untracked items)
- Untracked items in repo root (REVIEW-*.md, screenshots, build_test/, tools/*.html, .claude/scheduled_tasks.lock) are pre-existing — not touched this session
</current_state>

<locked_decisions>
Anthony locked three foundational decisions before the milestone was written:

1. **Plugin formats:** all four — VST3 + AU + LV2 + CLAP (AU is macOS-only, so 10 unique binaries across the 3-OS × 4-format matrix)
2. **OS coverage:** all three — Linux + macOS + Windows
3. **Audio compatibility:** plugin compatible with any host SR/BD, **but the bit-faithful C core MUST stay exactly as designed**. SR/BD conversion is a real-time wrapper concern only (float ↔ int16, host-rate ↔ 44.1k via JUCE SRC).

Additional decisions logged during summary review:

4. Standalone (v1.6) continues to ship alongside plugin formats
5. Beta-tester preset return-loop is **out of scope** for v1.7 (existing channel handles it)
6. Custom plugin UI redesign is **out of scope** — reuse current standalone GUI
7. Code signing / notarization may defer for beta — revisit on install friction
</locked_decisions>

<completed_work>
1. Cleaned up MILESTONES.md (chronological order, backfilled v1.5, expanded v1.0/v1.2/v1.3 from archives) — commit `ec2537d`
2. Pruned stale Bugfix carries from STATE.md (Morph artifacting + JUCE volume bug — not real on post-v1.6 main) — commit `ec2537d`
3. Updated PROJECT.md: added v1.5/v1.6 to Shipped; replaced "Current Milestone: v1.5" with v1.7 DAW Plugin Port; moved plugin + Windows/macOS out-of-scope entries to ACTIVE; moved marooned decision row back into Key Decisions table; added v1.7 hard-constraint decision — commit `04df431`
4. Reset STATE.md for v1.7 / planning / 0% / defining-requirements — commit `04df431`
</completed_work>

<blocker>
**`gsd-sdk` shim missing on PATH.** Local GSD install is `1.41.0` (`~/.claude/get-shit-done/bin/`); latest published is `1.41.2`. The npm package `get-shit-done-cc` declares both `gsd-sdk` and `gsd-tools` bin shims, but local install only has `gsd-tools.cjs` (note the `.cjs`; package now ships `.js`) and no `query` subcommand. The new-milestone workflow uses `gsd-sdk query init.new-milestone`, `gsd-sdk query state.milestone-switch`, etc., none of which work locally.

**Action:** Anthony will run `/gsd-update` in the new session.
</blocker>

<next_steps>
After the new session starts and `/gsd-update` lands:

1. Run `/gsd-resume-work` — it should detect this HANDOFF and resume cleanly to the research-vs-skip question
2. Answer **research-first** (recommended) or **skip research** for v1.7
   - If research-first: 4 parallel `gsd-project-researcher` agents on Stack / Features / Architecture / Pitfalls → `gsd-research-synthesizer` → SUMMARY.md
   - If skip: gather requirements conversationally from PROJECT.md + the 7 locked decisions above
3. Define REQUIREMENTS.md (REQ-ID prefix TBD — candidates: `PLUG`, `DAW`, `PORT`)
4. Spawn `gsd-roadmapper` to create ROADMAP.md — continue phase numbering from 20 (last v1.6 phase)
5. Begin `/gsd-discuss-phase` or `/gsd-plan-phase` on the first v1.7 phase
</next_steps>

<watch_outs>
- **Don't assume branch state from commit dates.** `master` has the abandoned dual-engine/Error-Accumulator timeline; `archive/v1.5-v1.6-macro-approach` has the abandoned macro-control timeline. `main` is the live branch. PROJECT.md line 28 + line 173 are the authoritative docs explaining the pivot. Read PROJECT.md before asserting branch state.
- **`.planning/milestones/v1.6-phases/`** currently contains the abandoned master-branch macro work (phases 20–23: macro-engine, macro-controls, echo-speed-diffusion-snap, gui-overlay-display) — NOT the live v1.6 user-waypoint phases. Live v1.6 phases (18–20) sit in `.planning/phases/`. Flagged in STATE.md Deferred Items as a separate cleanup pass; **do not touch during v1.7 work** without explicit ask.
- **Tone:** Anthony pushes back hard on guesses and on docs being out of sync. Always verify by reading actual files before asserting state. Don't surface stale Deferred Ideas as next-step menus.
</watch_outs>
