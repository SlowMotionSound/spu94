---
context: research
milestone: v1.7
milestone_name: DAW Plugin Port
phase: none
task: requirements-drafting
total_tasks: none
status: paused_after_research_bundle
last_updated: 2026-05-11T13:49:49Z
---

# Resume Notes — v1.7 DAW Plugin Port (research bundle committed, requirements not started)

Research-first lane is complete. Four parallel `gsd-project-researcher` agents produced STACK / FEATURES / ARCHITECTURE / PITFALLS files for v1.7, plus `SUMMARY-v1.7.md` synthesizing them. Bundle committed at `6f562bb`. Paused before drafting `REQUIREMENTS.md`.

<current_state>
- Branch: `main` at commit `6f562bb` (docs: v1.7 research bundle)
- Milestone: **v1.7 DAW Plugin Port** — research done, requirements not started
- No code in flight; clean working tree apart from pre-existing untracked items (REVIEW-*.md, screenshots, build_test/, tools/*.html, .claude/scheduled_tasks.lock — unchanged from before this session)
- `gsd-sdk v1.41.2` is on PATH (prior session's blocker is fully cleared)
</current_state>

<completed_work>
1. Resumed session; confirmed `gsd-sdk` is on PATH
2. Locked research-first over skip-research for v1.7 (Anthony's call)
3. Spawned 4 parallel researchers; all four landed without intervention
4. Synthesizer was Write-blocked by harness rule; orchestrator wrote `SUMMARY-v1.7.md` from the returned content
5. Committed `STACK-v1.7.md`, `FEATURES-v1.7.md`, `ARCHITECTURE-v1.7.md`, `PITFALLS-v1.7.md`, `SUMMARY-v1.7.md` as commit `6f562bb`
</completed_work>

<remaining_work>
1. Answer the pending product question: **conversational drip vs strawman REQUIREMENTS.md draft** for v1.7
2. Resolve the 7 open decisions enumerated in `SUMMARY-v1.7.md §3`
3. Confirm beta-tester DAW mix (research assumed Reaper / Logic / Live / FL Studio — needs direct confirmation)
4. Decide REQ-ID prefix (PLUG / DAW / PORT candidates)
5. Write `REQUIREMENTS.md` for v1.7
6. Spawn `gsd-roadmapper` → `ROADMAP.md` continuing phase numbering from **20** (last v1.6 phase)
7. Begin `/gsd-discuss-phase` or `/gsd-plan-phase` on Phase 21 (Build skeleton)
</remaining_work>

<decisions_made>
1. **Research-first over skip-research** — first plugin port, 10-binary matrix, real gotcha density; cost of unknowns ≥ cost of ~10 min of parallel research
2. **4-way split (Stack / Features / Architecture / Pitfalls)** — matches prior milestones' naming convention
3. **`-v1.7` suffix on research artifacts** — prior milestones used `-v1.2` / `-v1.3` suffixes alongside unversioned originals; v1.7 follows the convention
</decisions_made>

<blockers>
- **Pending product question**: drip-style vs strawman approach to REQUIREMENTS. Last assistant message asked; user paused before answering. Not a technical blocker — a workflow-shape decision.
</blockers>

## Required Reading (in order)

1. **`.planning/research/SUMMARY-v1.7.md`** — single load-bearing input for the requirements step. Read fully. §3 (7 open decisions), §4 (proposed 6+1 phase decomposition), §5 (top 5 risks), §6 (12 unknowns), §7 (out-of-scope restated).
2. **`.planning/research/ARCHITECTURE-v1.7.md`** — needed if SRC pick or RT-safety questions come up. §2 (SRC library options), §3 (float↔int16 + dither), §5 (parameter handling), §8 (build layout).
3. **`.planning/research/PITFALLS-v1.7.md`** — needed before writing the channel-bus / validation / signing scope requirements. Severity-ranked.
4. **`.planning/PROJECT.md`** — milestone section + Key Decisions table for the 8 locked decisions. PROJECT.md line 28 + line 173 explain the branch / archive layout.
5. **`.planning/HANDOFF.json`** — structured snapshot of this state, including `research_findings` array distilled from the bundle.

## Critical Anti-Patterns (do NOT repeat these)

- **Do not assume Anthony's beta-tester DAW mix.** Research assumed Reaper / Logic / Live / FL Studio because those are common; the actual mix is unverified. Ask directly before Phase 26 UAT scope is locked.
- **Do not lock the SRC library by table lookup.** ARCHITECTURE-v1.7.md gives latency *claims* from project READMEs; PITFALLS-v1.7.md B6 says trust the measurement. The library pick should be decided in requirements; the *exact group-delay number* gets measured during Phase 22 integration.
- **Do not drift into custom plugin UI work.** Locked decision #7: reuse standalone GUI. Watch for scope creep dressed up as "while we're in PluginProcessor.cpp anyway".
- **Do not surface the `.planning/milestones/v1.6-phases/` archive issue inside v1.7 work.** Flagged in STATE.md Deferred Items as separate cleanup pass.
- **Don't pull luxury features into v1.7 by accident.** SUMMARY-v1.7.md §7 catalogues what's out-of-scope (A/B compare, resizable window, tempo-sync, surround, MIDI-learn UI, distro packages).

## Infrastructure State

- `gsd-sdk` v1.41.2 on PATH at `/home/ubuntu-studio/.local/bin/gsd-sdk` — prior session's blocker cleared
- No background processes from this session
- Branch `main` clean (research bundle committed); pre-existing untracked items unchanged

<context>
The research bundle confirms a non-obvious thing: **v1.7 is much smaller than the milestone framing implies**. Reading the actual `PluginProcessor.cpp:587-595`, the architecture researcher found that the v1.6 standalone is already a `juce::AudioProcessor` with canonical RT-safe threading + atomic parameter handoff. The build target is already `juce_add_plugin(... FORMATS Standalone)`. So v1.7's real engineering scope is four surgical additions: expand FORMATS list, add float↔int16 conversion, add real-time SRC, fill empty `getStateInformation` stubs over the existing v1.4 `.spu94` serializer.

The single biggest engineering decision is **SRC library** — leaning libsamplerate Sinc-Medium (BSD-2). The single biggest *sound* decision is **dither at float→int16 input** — leaning no-dither per North Star (the PS1 SPU's authentic input stage is already int16; dither is a modern courtesy it never had). Both are DSP-character calls Anthony should make in requirements, not engineering-detail decisions for plan-phase.

The proposed roadmap is 6 firm phases (21–26) + 1 conditional (27 = signing only if beta install friction). Phase 22 is the highest-risk (SRC + PDC); phase 25 is auval-folklore-heavy; the rest are well-trodden JUCE template territory.

Anthony's working style for this milestone:
- Drip one decision at a time (especially for DSP-character calls like dither / SRC quality)
- Define abbreviations on first use
- Plain language with surprising analogies welcome
- Discuss sound AND tactile feel of controls, not just correctness
- Never guess — verify by reading actual files / measuring before asserting state
- Stop suggesting README / install-path / PyPI / GitHub setup findings
- No screenshots without explicit permission
- Don't oscillate between drip and lock — frustration is not permission to lock decisions on his behalf
</context>

<next_action>
Start with:

1. Brief one-paragraph re-orient: "v1.7 paused after research bundle. Resume question pending: drip vs strawman REQUIREMENTS approach."
2. Re-ask the pending question (or proceed if Anthony's first message in the new session answers it).
3. Once approach is chosen: open `.planning/research/SUMMARY-v1.7.md` and walk the 7 open decisions in §3 in the chosen mode.
</next_action>
