---
context: v1.10.0-uat
phase: 55-effects-uat
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-05-26T22:30:00Z
---

<current_state>
v1.10.0 Voice Dynamics — Phase 55 (Effects UAT) in progress.
Pan bug fixed, anti-click ramp added, old VCA ramp GUI removed.
ADSR knob calibration rework in progress — partially working but
not yet to Anthony's satisfaction. Hold-to-play gate behavior restored.
</current_state>

<completed_work>

This session:
- Pan-during-effects bug fixed: added base_vol_l/r fields, sweep scales relative
  to pan position instead of hardcoded 0x7FFF (63ed386)
- 8-sample anti-click ramp on saw retrigger boundaries (09e7ff4)
- Removed old VCA ramp GUI controls (ARM, Up/Down, Lin/Exp, Speed) (09e7ff4)
- ADSR knob calibration rework:
  - Built 84-entry attack timing table (all shift+step combos)
  - Built 21-entry decay/release timing table (step=0 only, decrease direction)
  - Unified display and engine mapping (display calls buildAdsrConfig)
  - Log-scale knob curve with power=0.38 shaping
  - Decay/release use separate timing table from attack (different step math)
</completed_work>

<remaining_work>

ADSR calibration (UNFINISHED — needs more work):
- Release calibration still feels off to Anthony despite multiple iterations
- The PS1 ADSR has only 21 discrete release rates (shift 0-20, each doubles time)
- The knob curve (pow 0.38 + log interpolation) may need further tuning
- Consider: is the problem the curve shape, or is 21 discrete steps simply too coarse?
- One-shot trigger behavior was attempted (auto-KOFF) but reverted to hold-to-play
  because auto-KOFF skips decay/sustain phases entirely
- Anthony wants single-click trigger that plays the full envelope — this requires
  either a gate duration parameter or a modified ADSR that auto-releases from sustain

GUI cleanup remaining:
- Organize Noise section in sampler panel
- Musical divisions for Speed encoder (note values, BPM sync)

Deferred:
- Sidechain Duck UAT (needs MIDI controller)
</remaining_work>

<decisions_made>

- Pan fix: base_vol_l/r as sweep ceiling — sweep modulates within pan position
- Anti-click: 8-sample linear ramp at saw retrigger boundaries (our creative extension)
- VCA ramp controls removed — superseded by unified effects dropdown
- ADSR display now calls buildAdsrConfig() for unified engine/display mapping
- Separate timing tables for attack (84 entries) vs decay/release (21 entries)
- Hold-to-play gate behavior is correct for PS1 ADSR model (KOFF on mouse-up)
- Auto-KOFF one-shot was tried and reverted — too short gate skips D and S phases
</decisions_made>

<blockers>
ADSR calibration not meeting expectations — the 21 discrete PS1 release rates
with power-curve knob mapping don't feel like a standard synth ADSR. May need
a fundamentally different approach (e.g., direct time-to-shift lookup with
carefully chosen breakpoints, or accept the PS1's coarse granularity and
document it as character).
</blockers>

## Anti-Patterns from This Session

| Pattern | Description | Severity |
|---------|-------------|----------|
| No end-to-end testing | Made multiple ADSR changes without verifying the actual sound path | blocking |
| Wrong timing tables | Used attack timings for release (different step math), causing calibration mismatch | blocking |
| Auto-KOFF without thinking through phases | Sent KOFF 1 block after KON, skipping D/S entirely | blocking |
| Multiple launches without cleanup | Left 8+ SPU-94 instances running | advisory |

## Required Reading (in order)
1. This file — anti-patterns above
2. src/plugin/PluginProcessor.cpp — buildAdsrConfig(), knobToRate(), kAttackTable, kDecayTable
3. src/spu94/spu94_adsr.c — ADSR state machine (attack uses shift+step, decay/release hardcode step=0)
4. src/spu94/spu94_envelope_step.c — counter-accumulate math (increase step=7-idx, decrease step=8-idx)

## Infrastructure State
- No background processes running (all killed)
- Build is Release
- Working tree has uncommitted ADSR changes (PluginEditor.cpp, PluginProcessor.cpp)

<next_action>
1. Fix ADSR calibration to Anthony's satisfaction — the knob-to-time mapping needs
   to feel like a standard synth. Consider testing each stage independently with
   printed timing verification before launching the GUI.
2. Solve one-shot trigger (click to play full ADSR without holding)
3. GUI cleanup: noise section, speed divisions
4. Sidechain Duck UAT when MIDI available
</next_action>
