---
context: v1.10.0-uat
phase: 55-effects-uat
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-05-27T20:45:00Z
---

<current_state>
v1.10.0 Voice Dynamics — Phase 55 (Effects UAT) in progress.
ADSR calibration major rework completed this session. Architecture changed from
knob-curve interpolation to direct PS1 rate indexing with measured timing tables.
GUI changed from rotary encoders to vertical faders with ms/s readout.
</current_state>

<completed_work>

This session (2026-05-27):
- ADSR calibration rework (984608a, 0568769):
  - 5 separate timing tables measured from actual engine runs (not theoretical)
  - kAttackLinTable (53), kAttackExpTable (52), kDecayTable (43),
    kReleaseLinTable (49), kReleaseExpTable (54)
  - Direct PS1 rate indexing: each fader position = one hardware rate
  - Vertical faders replace rotary encoders, ms/s text readout
  - ADSR visualizer removed (was inaccurate)
  - decay_step and release_step fields added to ADSR struct and engine
  - Sustain level 0 = true zero (ADSR_OFF, not 6.25%)
  - Decay minimum set to 11ms for audible transient
  - Attack uses direct PS1 rate indexing (same as decay/release)

Prior session work (already committed):
- Pan-during-effects bug fixed (63ed386)
- 8-sample anti-click ramp on saw retrigger (09e7ff4)
- Old VCA ramp GUI removed (09e7ff4)
</completed_work>

<remaining_work>

- One-shot trigger: click to play full ADSR without holding
- GUI cleanup: Noise section, Speed encoder musical divisions
- Sidechain Duck UAT (blocked — needs MIDI controller)
</remaining_work>

<decisions_made>

- Direct PS1 rate indexing for all ADSR slopes (no interpolation, no curves for decay/release)
- 5 measured tables per mode: external AI review identified that timing tables used
  linear calculations but engine runs exponential (3-4x discrepancy for decay)
- Sustain level 0 diverges from PS1 spec (was 6.25%) for musical utility
- Vertical faders chosen over rotary encoders for better discrete-step resolution
- ADSR visualizer removed — was misleading, fader text readout is more useful
</decisions_made>

<blockers>
Sidechain Duck UAT blocked on MIDI controller availability.
</blockers>

## Anti-Patterns from This Session

| Pattern | Description | Severity |
|---------|-------------|----------|
| Research without implementation | Identified timing table bug (linear vs exponential), measured correct values, documented it, then didn't apply the fix | blocking |
| Assumed instead of verified | Called the PS1 sampler a "creative extension" when it IS the core PS1 hardware | blocking |
| pkill corrupting builds | `pkill -9 -f SPU` matched the linker process, creating 0-byte binaries | blocking |
| Over-analyzing knob curves | Spent hours on power curves when the real fix was using measured engine times (resolved — all ADSR modes now use direct indexing) | resolved |

## Required Reading (in order)
1. This file — anti-patterns above
2. src/plugin/PluginProcessor.cpp — 5 kAdsr*Table arrays, knobToRate(), buildAdsrConfig()
3. src/spu94/spu94_adsr.c — ADSR state machine with decay_step/release_step support, sustain-zero fix
4. ADSR-CALIBRATION-BRIEF.md — full problem writeup with measured engine times (generated for external review)

## Infrastructure State
- No background processes running
- Build is Release
- Working tree clean (all committed)
- ADSR-CALIBRATION-BRIEF.md is untracked (reference doc, not part of build)

<next_action>
1. Solve one-shot trigger (click to play full ADSR without holding)
3. GUI cleanup: noise section, speed divisions
4. Sidechain Duck UAT when MIDI available
</next_action>
