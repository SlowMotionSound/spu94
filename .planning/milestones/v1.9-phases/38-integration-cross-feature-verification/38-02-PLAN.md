---
phase: 38-integration-cross-feature-verification
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified: []
autonomous: true
requirements: [INT-03, INT-04]

must_haves:
  truths:
    - "All ADSR unit tests pass (12 tests in test_adsr)"
    - "All sweep unit tests pass (in test_sweep)"
    - "All noise generator unit tests pass (6 tests in test_noise_gen)"
    - "All 57 voice_tick unit tests pass (including Phase 38 integration tests)"
    - "rt_no_heap gate passes (no malloc/calloc/realloc/free in link closure)"
    - "rt_no_locks gate passes (no pthread/sem/futex in link closure)"
    - "rt_no_syscalls gate passes (zero syscalls in hot path)"
    - "hotpath_alloc_gate passes (zero heap syscalls in steady state)"
    - "Existing features are unbroken: ADSR, loop mechanics, EON reverb send, Gaussian interpolation, anti-aliasing toggle"
  artifacts: []
  key_links:
    - from: "tests/unit/voice/test_voice_tick.c"
      to: "src/spu94/spu94_voice.c"
      via: "spu94_voice_mixer_tick + spu94_voice_tick"
      pattern: "spu94_voice_mixer_tick"
    - from: "tests/rt_safety/hotpath_alloc_gate.sh"
      to: "src/spu94/spu94_process.c"
      via: "strace syscall filter on spu94_process hot path"
      pattern: "spu94_process"
---

<objective>
Run the full regression suite and all rt_safety gates to verify that all existing voice
features remain unbroken (INT-03) and that no new features introduce heap allocation,
locking, or syscalls on the audio hot path (INT-04).

Purpose: This is the final integration gate for v1.9 Complete Voice. Every feature added in
Phases 33-37 (ADSR correction, signed volume, PMON, NON, sweep) must coexist without
regressions, and the RT-safety contract established in Phase 5 must hold.

Output: Verification results confirming all gates pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/rt_safety/CMakeLists.txt
@tests/unit/voice/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full voice engine regression suite (INT-03)</name>
  <files></files>
  <read_first>
    tests/unit/voice/CMakeLists.txt (to identify all test targets)
    tests/unit/voice/test_voice_tick.c (lines 2079-2143 for main() to count expected tests)
  </read_first>
  <action>
    Build all voice test targets (test_voice_tick, test_noise_gen, test_adsr, test_sweep,
    test_sample_loader) from the build-test directory with SPU94_BUILD_GUI=OFF and
    SPU94_BUILD_PLUGIN=OFF.

    Run each test binary individually and record pass/fail counts:
    - test_voice_tick: expect 57 tests 0 failures (51 original + 6 Phase 38 integration)
    - test_noise_gen: expect 6 tests 0 failures
    - test_adsr: expect current count, 0 failures
    - test_sweep: expect current count, 0 failures
    - test_sample_loader: expect current count, 0 failures

    Also run ctest with the voice-related labels to get a consolidated result.

    This verifies INT-03: ADSR, loop mechanics, EON reverb send, Gaussian interpolation,
    anti-aliasing toggle. Each of these features has dedicated tests in test_voice_tick.c
    from their respective phases (28, 29, 30, 32). MIDI dispatch is verified by the JUCE
    plugin build which is out of scope for the C core test suite -- it was validated during
    Phase 31 and the MIDI code path has not been modified in Phases 33-37.
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb/build-test && cmake --build . 2>&1 | tail -3 && ./tests/unit/voice/test_voice_tick 2>&1 | tail -3 && ./tests/unit/voice/test_noise_gen 2>&1 | tail -3 && ./tests/unit/voice/test_adsr 2>&1 | tail -3 && ./tests/unit/voice/test_sweep 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - test_voice_tick: 57 Tests 0 Failures
    - test_noise_gen: N Tests 0 Failures (N >= 6)
    - test_adsr: N Tests 0 Failures (N >= 12)
    - test_sweep: N Tests 0 Failures
    - test_sample_loader: N Tests 0 Failures
    - No compiler warnings from voice engine source files
  </acceptance_criteria>
  <done>All voice engine unit tests pass with zero failures across all test targets, confirming ADSR, loop mechanics, EON, Gaussian interpolation, anti-aliasing, PMON, NON, sweep, and signed volume features are unbroken</done>
</task>

<task type="auto">
  <name>Task 2: Run rt_safety gates with all new features enabled (INT-04)</name>
  <files></files>
  <read_first>
    tests/rt_safety/CMakeLists.txt (to understand gate targets and env vars)
    tests/rt_safety/hotpath_alloc_gate.sh (to understand the heap syscall filter)
  </read_first>
  <action>
    Build the rt_safety test targets (test_phase5_linksym, test_no_syscalls,
    hotpath_alloc_gate_target, hotpath_alloc_gate_target_with_malloc) from build-test.

    Run the four rt_safety axes via ctest:
    1. rt_no_heap: nm -u on the static-linked binary must show zero heap symbols
       (malloc, calloc, realloc, free)
    2. rt_no_locks: nm -u must show zero lock symbols (pthread_mutex_*, sem_*, futex)
    3. rt_no_syscalls: strace on 10^5-iter spu94_process loop must show zero syscalls
       between the SIGUSR1 markers
    4. hotpath_alloc_gate: strace with brk/mmap filter on spu94_process must show
       zero heap syscalls in steady state
    5. hotpath_alloc_gate_negative: the poisoned target must FAIL (WILL_FAIL property)

    Run: ctest -L rt_safety --output-on-failure from build-test.

    All five gates must pass. The new features (sweep module spu94_sweep.c, noise module
    spu94_noise.c, envelope_step helper, signed volume, PMON formula, NON branch) are all
    statically linked into the test binaries. If any gate fails, the new code introduced
    a forbidden operation on the hot path.
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb/build-test && cmake --build . 2>&1 | tail -3 && ctest -L rt_safety --output-on-failure 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ctest -L rt_safety reports all tests passed (0 failures, 0 errors)
    - rt_no_heap: PASS
    - rt_no_locks: PASS
    - rt_no_syscalls: PASS (or skipped if strace unavailable -- note in summary)
    - hotpath_alloc_gate: PASS
    - hotpath_alloc_gate_negative: PASS (expected failure = test system works)
  </acceptance_criteria>
  <done>All rt_safety gates pass with the full v1.9 feature set compiled in, confirming no heap allocation, no locks, no syscalls, and bounded latency on the audio hot path</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strace -> spu94_process | syscall monitoring of hot path |
| nm -> link closure | symbol analysis of static binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-02 | Denial of Service | audio hot path | mitigate | rt_no_heap + rt_no_locks + rt_no_syscalls gates verify no unbounded operations |
| T-38-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
cd /home/ubuntu-studio/Desktop/PSX Reverb/build-test && ctest --output-on-failure
Expected: All tests pass (voice + rt_safety)
</verification>

<success_criteria>
- All voice unit tests pass across all targets (57 voice_tick + noise_gen + adsr + sweep + sample_loader)
- All 5 rt_safety gates pass (or gracefully skip on platforms without strace)
- Zero regressions in any existing feature
- No new heap, lock, or syscall operations introduced on the audio hot path
</success_criteria>

<output>
Create `.planning/phases/38-integration-cross-feature-verification/38-02-SUMMARY.md` when done
</output>
