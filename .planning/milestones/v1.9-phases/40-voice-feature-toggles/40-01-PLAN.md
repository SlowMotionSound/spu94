---
phase: 40-voice-feature-toggles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/plugin/PluginProcessor.h
  - src/plugin/PluginProcessor.cpp
  - src/plugin/PluginEditor.h
  - src/plugin/PluginEditor.cpp
  - src/plugin/SamplerWindow.h
autonomous: false
requirements:
  - TOG-01
  - TOG-02
  - TOG-03
  - TOG-04

must_haves:
  truths:
    - "Per-voice NON toggle in sampler GUI enables/disables noise generator for voice 0"
    - "Per-voice PMON toggle in sampler GUI enables/disables pitch modulation for voice 0"
    - "NON toggle replaces ADPCM output with global noise (ADPCM still decodes for flags)"
    - "PMON toggle on voice N modulates pitch by voice N-1 outx (voice 0 PMON has no effect)"
  artifacts:
    - path: "src/plugin/PluginProcessor.h"
      provides: "Atomic stores and accessors for guiVoiceNon and guiVoicePmon"
    - path: "src/plugin/PluginProcessor.cpp"
      provides: "Audio-thread call to spu94_voice_mixer_set_non/set_pmon from atomic state"
    - path: "src/plugin/PluginEditor.h"
      provides: "ToggleButton declarations for NON and PMON"
    - path: "src/plugin/PluginEditor.cpp"
      provides: "Toggle setup, onClick handlers, resized layout in sampler window"
  key_links:
    - from: "src/plugin/PluginEditor.cpp"
      to: "src/plugin/PluginProcessor.h"
      via: "guiVoiceNon/guiVoicePmon atomic stores"
      pattern: "guiVoiceNon\\.store\\|guiVoicePmon\\.store"
    - from: "src/plugin/PluginProcessor.cpp"
      to: "include/spu94/spu94_voice.h"
      via: "spu94_voice_mixer_set_non / spu94_voice_mixer_set_pmon calls"
      pattern: "voice_mixer_set_non\\|voice_mixer_set_pmon"
---

<objective>
Add per-voice NON (noise) and PMON (pitch modulation) toggle controls to the sampler GUI, wired through the processor to the C core voice mixer API.

Purpose: The C core NON and PMON features (built in Phases 35-36) have no GUI surface. This plan exposes them as simple toggle buttons so the user can enable noise generator output and pitch modulation from the sampler window.

Output: Two toggle buttons in the sampler window ("Voice Mode" section), atomic GUI-to-audio handoff, C core API calls on the audio thread.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-voice-feature-toggles/40-CONTEXT.md
@include/spu94/spu94_voice.h
@src/plugin/PluginProcessor.h
@src/plugin/PluginProcessor.cpp
@src/plugin/PluginEditor.h
@src/plugin/PluginEditor.cpp
@src/plugin/SamplerWindow.h

<interfaces>
<!-- Key types and contracts the executor needs. -->

From include/spu94/spu94_voice.h:
```cpp
spu94_result_t spu94_voice_mixer_set_non(spu94_voice_mixer_t *m, int voice_idx, int enabled);
spu94_result_t spu94_voice_mixer_set_pmon(spu94_voice_mixer_t *m, int voice_idx, int enabled);
spu94_voice_mixer_t *spu94_get_voice_mixer(void);
```

From src/plugin/PluginProcessor.h (existing atomic pattern):
```cpp
std::atomic<int16_t> guiVoiceVolL{0x3FFF};
std::atomic<int16_t> guiVoiceVolR{0x3FFF};
void setGuiVoiceVolL(int16_t v) { guiVoiceVolL.store(v, std::memory_order_relaxed); }
```

From src/plugin/PluginProcessor.cpp (audio thread voice 0 control, around line 580):
```cpp
spu94_voice_mixer_set_pitch(spu94_get_voice_mixer(), 0,
    guiVoicePitch.load(std::memory_order_relaxed));
```

From src/plugin/PluginEditor.cpp (sampler panel layout constants):
```cpp
constexpr int tgy = aky + 12 + akh + 2;  // = 450
constexpr int voly = tgy + 28;            // = 478
// INV indicator bottom edge: voly + 68 = 546
// Window height: 710
// Available right column space: x=160..380, y=546..710 (164px vertical)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NON/PMON atomics, toggles, and audio-thread wiring</name>
  <files>
    src/plugin/PluginProcessor.h
    src/plugin/PluginProcessor.cpp
    src/plugin/PluginEditor.h
    src/plugin/PluginEditor.cpp
    src/plugin/SamplerWindow.h
  </files>
  <read_first>
    src/plugin/PluginProcessor.h (lines 66-78 for guiVoicePitch/guiVoiceVolL/R atomic pattern)
    src/plugin/PluginProcessor.cpp (lines 540-600 for audio-thread GUI command application, especially line 580 where set_pitch is called)
    src/plugin/PluginEditor.h (lines 129-136 for voicePan/voiceLevel/voiceInv declarations)
    src/plugin/PluginEditor.cpp (lines 344-387 for Pan/Level/INV constructor setup, lines 1037-1048 for resized layout)
    include/spu94/spu94_voice.h (lines 170-178 for set_pmon and set_non signatures)
    src/plugin/SamplerWindow.h (lines 17-19 for contentPanel size)
  </read_first>
  <action>
    **PluginProcessor.h changes:**
    1. Add two atomic bool members near the guiVoiceVolL/guiVoiceVolR declarations (around line 381):
       std::atomic_bool guiVoiceNon{false} and std::atomic_bool guiVoicePmon{false}
    2. Add public accessor methods following the existing pattern (near line 67-69):
       std::atomic_bool& getGuiVoiceNon() returning guiVoiceNon
       std::atomic_bool& getGuiVoicePmon() returning guiVoicePmon

    **PluginProcessor.cpp changes:**
    3. In the audio-thread standalone voice control section, right after the spu94_voice_mixer_set_pitch call for voice 0 (around line 580-581), add two calls:
       spu94_voice_mixer_set_non(spu94_get_voice_mixer(), 0, guiVoiceNon.load(std::memory_order_relaxed) ? 1 : 0)
       spu94_voice_mixer_set_pmon(spu94_get_voice_mixer(), 0, guiVoicePmon.load(std::memory_order_relaxed) ? 1 : 0)
       These run every audio block, same as set_pitch. The C core API is idempotent (just sets/clears a bit).

    **PluginEditor.h changes:**
    4. Add two ToggleButton member declarations near the voiceInvToggle declaration (around line 133):
       juce::ToggleButton voiceNonToggle{"NON"} and juce::ToggleButton voicePmonToggle{"PMON"}

    **PluginEditor.cpp constructor changes (inside the standalone sampler window setup block):**
    5. After the voiceInvIndicator setup (around line 383), add setup for both toggles:
       - voiceNonToggle: setClickingTogglesState(true), initial state false, onClick stores to processorRef.getGuiVoiceNon(). Use panel.addAndMakeVisible.
       - voicePmonToggle: setClickingTogglesState(true), initial state false, onClick stores to processorRef.getGuiVoicePmon(). Use panel.addAndMakeVisible.

    **PluginEditor.cpp resized() layout changes (inside the sampler window layout block):**
    6. Place NON and PMON toggles in the right column below the INV indicator. The INV indicator bottom edge is at voly + 68 (y=546). Place them as:
       - A "Voice Mode" group label at (160, voly + 80, 100, 16) in dark gray/white text
       - NON toggle at (160, voly + 98, 70, 30) — clearly labeled "NON"
       - PMON toggle at (160, voly + 132, 70, 30) — clearly labeled "PMON"
       This groups them visually below the INV toggle as a "Voice Mode" section.

    **SamplerWindow.h changes:**
    7. The current window height is 710. The bottom of the voice controls section after adding NON/PMON will be around voly + 162 = 640, well within the 710 height. No resize needed unless the layout runs tight — verify after building.

    Color scheme: use the PS1 color palette. For the Voice Mode label, use light gray text. The toggle buttons use JUCE default styling (checkbox style), consistent with existing Loop, Latch, Lock, Gauss, INV, and Exp toggles in the sampler window.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && cmake --build build --config Release --target SPU94_standalone 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds with zero errors
    - PluginProcessor.h has guiVoiceNon and guiVoicePmon atomic members with public accessors
    - PluginProcessor.cpp calls spu94_voice_mixer_set_non and spu94_voice_mixer_set_pmon on every audio block for voice 0
    - PluginEditor.h declares voiceNonToggle and voicePmonToggle
    - PluginEditor.cpp constructor sets up both toggles with onClick handlers that store to processor atomics
    - PluginEditor.cpp resized() positions both toggles in the sampler window below the INV indicator
  </acceptance_criteria>
  <done>NON and PMON toggle buttons appear in the sampler GUI, wired through atomics to the C core voice mixer API. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of NON and PMON toggles</name>
  <what-built>Two toggle buttons (NON and PMON) in the sampler window, positioned below the INV toggle. NON replaces the voice's ADPCM output with the global noise generator. PMON enables pitch modulation from the previous voice (has no effect on voice 0 since there is no predecessor).</what-built>
  <how-to-verify>
    1. Launch the standalone app:
       cd "/home/ubuntu-studio/Desktop/PSX Reverb" && ./build/SPU94_artefacts/Release/Standalone/SPU94

    2. Load a sample in the sampler window (Load Sample button)

    3. Verify NON toggle:
       - Trigger the voice with NON off — you should hear the loaded sample
       - Toggle NON on, trigger again — the voice should output noise instead of the sample
       - Toggle NON off, trigger again — the sample should play normally again

    4. Verify PMON toggle:
       - Toggle PMON on for voice 0 — since voice 0 has no predecessor, this should have no audible effect (the pitch stays the same). This is correct PS1 behavior per PMON-04.
       - Verify the toggle does not cause any crash or audio glitch

    5. Verify layout:
       - NON and PMON toggles are visible and clearly labeled
       - They are positioned below the INV toggle in a logical "Voice Mode" group
       - They do not overlap with any existing controls
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI thread -> audio thread | Atomic bool stores cross thread boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Tampering | atomic bool stores | accept | Values are boolean (true/false only), clamped by C API (voice_idx validated, bit set/clear). No risk from out-of-range values. |
| T-40-02 | Denial of Service | set_non/set_pmon called every block | accept | O(1) bit operations, same pattern as existing set_pitch. No performance concern. |
</threat_model>

<verification>
Build: cmake --build build --config Release --target SPU94_standalone succeeds
Runtime: toggles visible, NON produces noise output, PMON accepted without crash
</verification>

<success_criteria>
- NON toggle enables noise generator output on voice 0 (audible noise instead of sample)
- PMON toggle accepted on voice 0 without crash (no audible effect expected — no predecessor)
- Both toggles wired through atomic stores to C core API on audio thread
- Layout is clean, toggles grouped below INV in sampler window
- Build passes, no regressions
</success_criteria>

<output>
Create `.planning/phases/40-voice-feature-toggles/40-01-SUMMARY.md` when done
</output>
