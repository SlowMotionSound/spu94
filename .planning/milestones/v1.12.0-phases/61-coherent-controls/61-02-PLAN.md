---
phase: 61-coherent-controls
plan: 02
type: tdd
wave: 2
depends_on: ["61-01"]
files_modified:
  - src/plugin/PluginProcessor.cpp
  - src/plugin/PluginProcessor.h
autonomous: true
requirements: [VCTRL-01, VCTRL-02, VCTRL-03]
user_setup: []

must_haves:
  truths:
    - "Moving Level changes the loudness of EVERY active voice, not just voice 0 (VCTRL-01)"
    - "Moving Pan places EVERY active voice at the same stereo position (VCTRL-02, D-02)"
    - "Toggling NON/PMON changes EVERY active voice; ADSR remains shared (VCTRL-03, D-04)"
    - "Velocity survives the per-block fan-out — harder hit is louder, Level rides on top (D-01)"
    - "At count=24 the loop reaches all 24 and the voice-0/Trigger audition is bit-identical to v1.11.0 (D-06)"
    - "A voice outside [0, activeVoiceCount) receives no control update (D-06 bound)"
    - "INV (phase-invert) rides the sign of the fanned base_vol, applied to every active voice with no separate plumbing (D-03)"
    - "Continuous controls (Level/Pan/INV/NON/PMON) apply live to all sounding voices via the per-block apply call from processBlock (D-05)"
    - "The Trigger/voice-0 audition plays at full velocity x Level, matching the fader (D-08)"
  artifacts:
    - path: "src/plugin/PluginProcessor.cpp"
      provides: "real applyContinuousVoiceControls() fan-out + per-note velocity capture + range reconciliation"
      contains: "for (int v = 0; v < count"
      min_lines: 20
  key_links:
    - from: "src/plugin/PluginProcessor.cpp (MIDI note-on ~1426)"
      to: "noteVelocity[voice]"
      via: "store velToQ15(vel) at allocation time"
      pattern: "noteVelocity\\["
    - from: "src/plugin/PluginProcessor.cpp (applyContinuousVoiceControls)"
      to: "voices[v].base_vol_l/r"
      via: "base_vol = q15_mul_truncate(guiVol, noteVelocity[v]) across [0,count)"
      pattern: "base_vol_l"
    - from: "src/plugin/PluginProcessor.cpp (processBlock)"
      to: "applyContinuousVoiceControls()"
      via: "call replaces the old voice-0-only block at ~866-885"
      pattern: "applyContinuousVoiceControls\\(\\)"
---

<objective>
Turn Plan 01's RED tests GREEN: implement the real continuous control fan-out so
Level, Pan, INV, NON, and PMON govern every active voice `[0, activeVoiceCount)`,
with per-note velocity retained so "Level rides on top of velocity" (D-01) holds,
and the velocity/base_vol ranges unified onto the engine's `0x3FFF` scale (D-08).

This is the behavioral heart of Phase 61. It:
- replaces the no-op stub with a loop over `[0, activeVoiceCount)` that recomputes
  `base_vol = q15_mul_truncate(guiVol, noteVelocity[v])` and sets NON/PMON per voice
- captures per-note velocity at MIDI note-on (and seeds full velocity for the
  Trigger/voice-0 audition, D-08) into the `noteVelocity[24]` array Plan 01 declared
- reconciles the divergent velocity full-scale (`0x7FFF`) and GUI full-scale
  (`0x3FFF`) so velocity-127 converges with Trigger-at-100% (Pitfall 2)
- locks the apply-vs-sweep/duck ordering so the fan-out does not fight the L/R
  sweep state machines or the sidechain-duck restore (Pitfall 4)

Purpose: deliver VCTRL-01/02/03 with velocity-faithful, hardware-faithful behavior
and bit-identical default-24 regression safety.
Output: All 8 `voice_controls_*` cases GREEN; full suite + rt_safety green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-coherent-controls/61-CONTEXT.md
@.planning/phases/61-coherent-controls/61-RESEARCH.md
@.planning/phases/61-coherent-controls/61-VALIDATION.md
@.planning/phases/61-coherent-controls/61-01-SUMMARY.md

# The seam + array + stub this plan fills in (read the SUMMARY for the exact RED baseline):
@tests/plugin/test_voice_controls.cpp

<interfaces>
<!-- Current state of the call sites this plan edits. Extracted this session. -->

CURRENT voice-0-only continuous apply block to REPLACE (PluginProcessor.cpp:863-885):
```cpp
        spu94_voice_mixer_set_pitch(spu94_get_voice_mixer(), 0,
            guiVoicePitch.load(std::memory_order_relaxed));        // pitch stays voice-0-only

        // Apply Pan/Level continuously — writes base_vol so sweep scales relative to pan position
        {
            auto* mx = spu94_get_voice_mixer();
            mx->voices[0].base_vol_l = guiVoiceVolL.load(std::memory_order_relaxed);  // VOICE 0 ONLY
            mx->voices[0].base_vol_r = guiVoiceVolR.load(std::memory_order_relaxed);
        }
        // Apply NON/PMON toggles on voice 0 (Phase 40)
        {
            bool nonOn = guiVoiceNon.load(std::memory_order_relaxed);
            spu94_voice_mixer_set_non(spu94_get_voice_mixer(), 0, nonOn ? 1 : 0);     // VOICE 0 ONLY
            auto* mx = spu94_get_voice_mixer();
            int shift = noiseShift.load(std::memory_order_relaxed);  // noise_gen is GLOBAL — keep voice-0-scoped
            ... noise_gen.shift / .step ...
        }
        spu94_voice_mixer_set_pmon(spu94_get_voice_mixer(), 0,
            guiVoicePmon.load(std::memory_order_relaxed) ? 1 : 0);                     // VOICE 0 ONLY
```

CURRENT MIDI note-on (PluginProcessor.cpp:1420-1431) — velocity-only, centered pan:
```cpp
                    int vel = msg.getVelocity();
                    int16_t vol = static_cast<int16_t>((vel * 0x7FFF) / 127);   // 0x7FFF full-scale!
                    int voice = allocateVoice(note);
                    spu94_adsr_state_t midiAdsr = buildAdsrConfig();
                    spu94_voice_mixer_key_on(spu94_get_voice_mixer(), voice,
                        0, pitch, vol, vol, 1,                                   // vol,vol = centered, no Level/Pan
                        midiAdsr.enabled ? &midiAdsr : nullptr);
```

CURRENT Trigger/voice-0 audition key-on (PluginProcessor.cpp:830-833) — already uses guiVoiceVolL/R:
```cpp
                spu94_voice_mixer_key_on(mx, 0, startAddr, trigPitch,
                                         guiVoiceVolL.load(std::memory_order_relaxed),
                                         guiVoiceVolR.load(std::memory_order_relaxed),
                                         1, adsrCfg.enabled ? &adsrCfg : nullptr);
```

CONFIRMED reverb-send tap (spu94_voice.c:737-752) — D-01a: send tapped AFTER base_vol, BEFORE master_vol:
```c
        dry_sum_l += vl; dry_sum_r += vr;
        if (m->eon_flags & (1u << v)) { rev_sum_l += vl; rev_sum_r += vr; }   // <-- reverb send (post base_vol)
    ...
    *dry_l = q15_mul_truncate(mixed_l, m->master_vol_l);                       // master applied AFTER, dry only
```

Phase 60 count read pattern (PluginProcessor.cpp:2609):
```cpp
    const int count = activeVoiceCount.load(std::memory_order_acquire);
```

Q15 combine (include/spu94/spu94_q15.h:112): q15_mul_truncate(a,b) = sat_s16((a*b)>>15), sign of `a` kept.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add range-reconciliation helpers + capture per-note velocity at note-on and Trigger</name>
  <files>src/plugin/PluginProcessor.cpp</files>
  <read_first>
    - .planning/phases/61-coherent-controls/61-01-SUMMARY.md (the RED baseline — which cases failed, and the exact noteVelocity[24] + method-stub locations Plan 01 created)
    - src/plugin/PluginProcessor.cpp (lines 822-858 Trigger/voice-0 key-on path; lines 1414-1438 MIDI note-on; lines 2593-2616 Phase 60 count helpers — for the acquire-load pattern)
    - .planning/phases/61-coherent-controls/61-RESEARCH.md (Pitfall 2 lines 161-166 for the range-reconciliation rationale; Code Examples lines 186-199 for the velToQ15/combineVel reference forms; Open Question 3 lines 248 for the Trigger velocity-seeding mechanism)
    - include/spu94/spu94_q15.h (lines 99-114 — q15_mul_truncate contract: sign of first arg preserved, so passing signed guiVol first keeps INV's negative)
  </read_first>
  <behavior>
    - A `velToQ15(int vel)` helper maps MIDI velocity 0..127 -> Q15 scalar [0, 0x7FFF].
    - A `combineVoiceVol(int16_t guiVol, int16_t velQ15)` helper returns
      `q15_mul_truncate(guiVol, velQ15)` — full velocity (0x7FFF) x full Level (0x3FFF)
      yields ~0x3FFE (the GUI's own max), so velocity-127 converges with Trigger-100% (D-08).
    - MIDI note-on stores `noteVelocity[voice] = velToQ15(vel)` AND keys on with
      `combineVoiceVol(guiVoiceVolL, velQ15)` / `...VolR` instead of centered `vol,vol`.
    - The Trigger/voice-0 audition path seeds `noteVelocity[0] = 0x7FFF` (full velocity)
      so the apply loop keeps voice 0 at exactly the Level setting (D-08).
  </behavior>
  <action>
    1. Add two file-local static inline helpers near the top of PluginProcessor.cpp
       (or in an anonymous namespace), matching RESEARCH.md Code Examples:
         - velToQ15(int vel): clamp 0..127, return (int16_t)((vel * 0x7FFF) / 127).
         - combineVoiceVol(int16_t guiVol, int16_t velQ15): return
           q15_mul_truncate(guiVol, velQ15). Pass guiVol FIRST so its sign (INV)
           is preserved per the q15_mul_truncate contract.

    2. MIDI note-on (lines 1420-1431): after `int voice = allocateVoice(note);`,
       compute `int16_t velQ15 = velToQ15(vel);`, store `noteVelocity[voice] = velQ15;`,
       then change the key_on volume arguments from `vol, vol` to
       `combineVoiceVol(guiVoiceVolL.load(relaxed), velQ15),`
       `combineVoiceVol(guiVoiceVolR.load(relaxed), velQ15)`. This makes velocity AND
       Level/Pan/INV co-govern the note at key-on (D-01). Keep buildAdsrConfig() exactly
       as-is (ADSR already shared — D-07). The old `int16_t vol = (vel*0x7FFF)/127;` line
       is now superseded; remove it (its 0x7FFF full-scale was the Pitfall-2 mismatch).

    3. Trigger/voice-0 audition path (the pendingGuiTriggerPitch block, ~822-858): at the
       point voice 0 is keyed on (~830), set `noteVelocity[0] = 0x7FFF;` (full velocity)
       so the continuous apply loop (Task 2) recomputes voice-0 base_vol as
       combineVoiceVol(guiVol, 0x7FFF) == guiVol (the Level setting) — keeping the
       Trigger audition exactly at the fader, D-08. The existing key_on already passes
       guiVoiceVolL/R, so its immediate behavior is unchanged; the seed only governs
       what the per-block apply loop does next block.

    Do NOT touch the apply block itself in this task (Task 2 owns the fan-out loop).
    After this task, the velocity-capture cases still depend on Task 2's loop to pass,
    but note-on now writes noteVelocity and the unified range.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && grep -q "velToQ15" src/plugin/PluginProcessor.cpp && grep -q "combineVoiceVol" src/plugin/PluginProcessor.cpp && grep -q "noteVelocity\[voice\]" src/plugin/PluginProcessor.cpp && grep -q "noteVelocity\[0\] = 0x7FFF" src/plugin/PluginProcessor.cpp && cmake --build build --target test_voice_controls 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep "velToQ15" src/plugin/PluginProcessor.cpp` and `grep "combineVoiceVol" src/plugin/PluginProcessor.cpp` both match (helpers defined).
    - In the MIDI note-on path, `noteVelocity[voice] =` is assigned, and the key_on call passes `combineVoiceVol(...)` for both L and R args (no longer `vol, vol`).
    - The old line `int16_t vol = static_cast<int16_t>((vel * 0x7FFF) / 127);` no longer appears (range mismatch removed).
    - In the Trigger block, `noteVelocity[0] = 0x7FFF;` is set at voice-0 key-on.
    - `cmake --build build --target test_voice_controls` still compiles (the apply loop is still the Task-1-state no-op stub, so cases remain RED until Task 2).
  </acceptance_criteria>
  <done>
    Velocity is captured per note on a unified 0x3FFF scale; the Trigger path seeds
    full velocity for voice 0. The fan-out loop (Task 2) will consume noteVelocity.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement applyContinuousVoiceControls() fan-out and call it from processBlock</name>
  <files>src/plugin/PluginProcessor.cpp</files>
  <read_first>
    - src/plugin/PluginProcessor.cpp (lines 863-885 — the voice-0-only block to relocate into the method; the stub at ~2616 from Plan 01; lines 1441-1500 Phase 46 duck region — to decide apply-vs-duck ordering for Task 3)
    - .planning/phases/61-coherent-controls/61-RESEARCH.md (Pattern 1 lines 79-102 for the exact method shape and the "pitch stays voice-0-only" note; Anti-Patterns lines 122-128; Pitfall 5 lines 179-183 for the precise regression invariant — do NOT assert MIDI voices 1-23 are unchanged)
    - include/spu94/spu94_voice.h (lines 179-187 set_pmon/set_non — they validate index and own the PMON bit-0-ignore rule)
  </read_first>
  <behavior>
    - `applyContinuousVoiceControls()` reads count via acquire, reads guiVoiceVolL/R
      and guiVoiceNon/Pmon (relaxed), then for v in [0, count): writes
      `voices[v].base_vol_l = combineVoiceVol(guiVolL, noteVelocity[v])` (and R), and
      calls set_non(v,...) / set_pmon(v,...).
    - Pitch stays voice-0-only: the `set_pitch(mx, 0, guiVoicePitch)` call remains
      OUTSIDE this method (not fanned out — explicit scope decision).
    - The global noise_gen shift/step configuration (currently inside the voice-0 NON
      block) stays a single global write, NOT per-voice.
    - processBlock calls `applyContinuousVoiceControls();` exactly where the old
      voice-0 block was (~866-885), preserving ordering relative to the trigger/stop
      handling above it and the MIDI dispatch below it.
    - After this task, all VCTRL-01/02/03 + D-01 + D-06 cases pass; only the
      sweep/duck-ordering case (Task 3) may still need the ordering fix.
  </behavior>
  <action>
    Replace the no-op stub body of `SPU94AudioProcessor::applyContinuousVoiceControls()`
    (added by Plan 01, ~line 2616) with the real fan-out, following RESEARCH.md Pattern 1:
      - `auto* mx = spu94_get_voice_mixer();`
      - `const int count = activeVoiceCount.load(std::memory_order_acquire);`
      - load guiVoiceVolL/R (relaxed, into int16_t), guiVoiceNon/Pmon (relaxed, into bool).
      - `for (int v = 0; v < count; ++v) {`
          `mx->voices[v].base_vol_l = combineVoiceVol(guiL, noteVelocity[v]);`
          `mx->voices[v].base_vol_r = combineVoiceVol(guiR, noteVelocity[v]);`
          `spu94_voice_mixer_set_non(mx, v, nonOn ? 1 : 0);`
          `spu94_voice_mixer_set_pmon(mx, v, pmonOn ? 1 : 0);`
        `}`
      - NO allocations, NO locks, NO syscalls — atomic loads + existing O(1) setters only
        (RT invariant; Anti-Pattern list).

    Then in processBlock, at the site of the OLD voice-0-only block (lines 863-885):
      - KEEP the `spu94_voice_mixer_set_pitch(..., 0, guiVoicePitch...)` call exactly
        where it is (pitch is voice-0-only, NOT fanned out).
      - KEEP the global noise_gen shift/step configuration as a single global write
        (move it out of the per-voice path if needed, but it must run once, not per voice).
      - DELETE the three voice-0-only writes (voices[0].base_vol_l/r, set_non(0,...),
        set_pmon(0,...)) and REPLACE them with a single call to
        `applyContinuousVoiceControls();`. Voice 0 is now covered by the loop (it's in
        [0, count) for any count>=1), and its base_vol is recomputed from
        noteVelocity[0] (seeded full by the Trigger path in Task 1) x guiVol — bit-identical
        to the old voice-0 write when noteVelocity[0]==0x7FFF and Level is at the value
        guiVol already held (D-06 / D-08 regression).

    Regression-invariant guardrail (Pitfall 5): the default-24 case must still pass —
    it asserts voice-0 bit-identity and that the loop reaches all 24. It does NOT assert
    MIDI voices 1-23 are unchanged (those change by design — that IS the fix).
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && cmake --build build --target test_voice_controls 2>&1 | tail -2 && ctest --test-dir build -R "voice_controls_(level_all_active|pan_all_active|non_pmon_all_active|velocity_rides_level|default24_regression|out_of_range_untouched)" --output-on-failure 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `applyContinuousVoiceControls()` body contains `for (int v = 0; v < count` and writes `mx->voices[v].base_vol_l`/`base_vol_r` plus `set_non`/`set_pmon` inside the loop.
    - The method reads `activeVoiceCount.load(std::memory_order_acquire)`.
    - processBlock no longer contains `mx->voices[0].base_vol_l = guiVoiceVolL.load` (the voice-0-only write is gone), and DOES contain a call to `applyContinuousVoiceControls();` at that site.
    - `spu94_voice_mixer_set_pitch(..., 0, ...)` (voice-0-only pitch) is still present and NOT inside applyContinuousVoiceControls.
    - These 6 cases PASS: voice_controls_level_all_active, voice_controls_pan_all_active, voice_controls_non_pmon_all_active, voice_controls_velocity_rides_level, voice_controls_default24_regression, voice_controls_out_of_range_untouched.
    - No allocation/lock/syscall added inside the loop (atomic loads + existing setters only).
  </acceptance_criteria>
  <done>
    The fan-out is live: Level/Pan/INV/NON/PMON reach every active voice, velocity
    survives, default-24 voice-0 path is bit-identical, out-of-range voices untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock apply-vs-sweep/duck ordering; turn the last case green; full-suite + rt_safety gate</name>
  <files>src/plugin/PluginProcessor.cpp</files>
  <read_first>
    - src/plugin/PluginProcessor.cpp (the processBlock region from the applyContinuousVoiceControls() call site through the Phase 46 duck block at ~1441-1520 and any tremolo/auto-pan sweep config blocks ~898-1000 — to confirm the apply call runs at a point where base_vol is the ceiling the sweeps read, and the duck's base_vol restore lands AFTER the apply so duck depth is preserved)
    - .planning/phases/61-coherent-controls/61-RESEARCH.md (Pitfall 4 lines 173-177 + Open Question 2 lines 246 + Assumption A2 line 238 — the sweep-reads-ceiling-same-tick fact and the duck-ordering flag; this task makes the ordering explicit and tested)
    - src/spu94/spu94_voice.c (lines 130-157 STEP 0 — confirms sweep reads base_vol as the ceiling each tick, so writing base_vol every block before the tick is the intended precedence)
  </read_first>
  <behavior>
    - `voice_controls_sweep_interaction` passes: a voice with `sweep_l.active = 1` keeps
      that flag after applyContinuousVoiceControls() runs with a new Level (the apply
      re-bases base_vol as the ceiling; it must not clear/reset sweep state).
    - The sidechain-duck restore of base_vol still lands correctly relative to the apply
      loop (duck depth preserved when Level moves on a ducked voice) — ordering decision
      documented in a code comment at the call site.
    - Full CTest suite green; rt_safety label green.
  </behavior>
  <action>
    1. Confirm and lock ordering. The apply loop writes base_vol (the sweep CEILING);
       the C-core tick (STEP 0) reads base_vol as the ceiling on the SAME tick, so the
       sweep correctly tracks the new ceiling — no sweep reset needed, and the
       sweep_interaction case must already pass once Task 2 landed (the loop writes
       base_vol but never touches sweep_l/sweep_r .active or phase). If that case still
       fails, the cause is the loop inadvertently resetting sweep state — fix the loop
       to write ONLY base_vol_l/r (plus set_non/set_pmon), never sweep fields.

    2. Sidechain-duck ordering (Pitfall 4 / Open Question 2): the Phase 46 duck snapshots
       duckOrigLevel and writes per-voice base_vol during its ramp. Decide and document
       the precedence at the applyContinuousVoiceControls() call site with a comment:
       the continuous apply runs BEFORE the duck block so the duck reads the freshly
       Level-scaled base_vol as its pre-duck reference (or, if the existing code order
       already runs duck after apply, state that explicitly). The required outcome:
       moving Level on a ducked voice changes the ceiling, and the duck depth (the
       ratio it applies) is preserved — it does not double-apply or get erased. Add a
       one-line comment at the call site naming the chosen order and why (sweep/duck
       read base_vol as ceiling; apply must run before the tick that consumes it).

    3. Run the FULL suite and rt_safety. Fix any pre-existing case that the header /
       call-site change disturbed. Expected: all 8 voice_controls cases green, all
       prior plugin + C-core cases green, rt_safety green (the new code is host-side
       glue with no heap/lock/syscall, so the C-core nm-gates are unaffected).
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && cmake --build build 2>&1 | tail -2 && ctest --test-dir build -R voice_controls --output-on-failure 2>&1 | tail -12 && ctest --test-dir build -L rt_safety --output-on-failure 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `ctest --test-dir build -R voice_controls --output-on-failure` reports 8/8 passed (including voice_controls_sweep_interaction and voice_controls_adsr_shared).
    - `ctest --test-dir build -L rt_safety --output-on-failure` reports all rt_safety gates passed.
    - `ctest --test-dir build --output-on-failure` (full suite) reports 0 failures — no pre-existing plugin or C-core test regressed.
    - A comment at the applyContinuousVoiceControls() call site names the apply-vs-duck ordering and the rationale (base_vol is the sweep/duck ceiling consumed by the tick).
    - The apply loop writes only base_vol_l/r + set_non/set_pmon — it does not write any sweep_l/sweep_r field (verified by reading the method body).
  </acceptance_criteria>
  <done>
    All 8 voice_controls cases green; sweep + duck interactions verified; full suite
    and rt_safety green. Phase 61 behavior is complete and regression-safe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Same as Plan 01 — no new input/network/format boundary. The fan-out is host-internal glue across an in-process singleton mixer; the MIDI->audio path topology is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03 | Tampering (out-of-bounds write) | fan-out loop `voices[v]` / `noteVelocity[v]` | mitigate | Loop bound `count = activeVoiceCount.load(acquire)` is clamped to [1,24] by setActiveVoiceCount (Phase 60); `noteVelocity[24]` and `voices[24]` are fixed-size. Loop is strictly `v < count <= 24`. Engine setters re-validate voice_idx (defense in depth). No external index source. |
| T-61-04 | Denial of Service (RT-safety / xrun) | applyContinuousVoiceControls in audio callback | mitigate | Body is atomic loads + a bounded (<=24) loop of O(1) existing setters; no heap/lock/syscall. Verified by the rt_safety gate staying green and by the no-alloc/no-lock acceptance criterion. Per RESEARCH.md the C-core nm-gates do not scan PluginProcessor.cpp, so this is preserved by construction + code review. |
| T-61-05 | Tampering (state corruption) | apply loop vs. sweep/duck writers of base_vol | mitigate | Task 3 locks precedence: apply writes base_vol (the ceiling) before the tick that consumes it; the loop never touches sweep_l/r or duck state. voice_controls_sweep_interaction asserts sweep state survives a Level move; duck ordering documented + the regression invariant holds. |
| T-61-SC | Tampering | npm/pip/cargo installs | accept | No package installs (zero new dependencies; RESEARCH.md Package Legitimacy Audit = Not Applicable). |
</threat_model>

<verification>
- 8/8 `voice_controls_*` cases green (VCTRL-01/02/03, D-01, D-06, Pitfall 4).
- `ctest --test-dir build --output-on-failure` full suite: 0 failures.
- `ctest --test-dir build -L rt_safety --output-on-failure`: all gates green.
- Source assertions: fan-out loop over `[0, count)` writes base_vol + set_non/set_pmon only;
  voice-0-only writes removed; pitch + global noise_gen stay voice-0/global; velocity captured
  at note-on; Trigger seeds noteVelocity[0]=0x7FFF; range unified to 0x3FFF (no `*0x7FFF)/127`
  feeding base_vol directly).
- Manual UAT items (from VALIDATION.md, non-blocking): voice-0/Trigger audition sounds
  identical to v1.11.0; PMON-chain character audible across the active set.
</verification>

<success_criteria>
- Level/Pan/INV govern every active voice via per-voice base_vol (VCTRL-01/02).
- NON/PMON reach every active voice; ADSR confirmed shared (VCTRL-03).
- Velocity rides on top of Level — two velocities yield two base_vol values (D-01).
- Default-24 voice-0/Trigger path bit-identical to v1.11.0; loop reaches all 24 (D-06).
- Out-of-range voices receive no control update (D-06 bound).
- Sweep + duck interactions preserved; full suite + rt_safety green.
</success_criteria>

<output>
Create `.planning/phases/61-coherent-controls/61-02-SUMMARY.md` when done. Record:
- The final form of applyContinuousVoiceControls() (loop bound, fields written).
- The locked apply-vs-duck ordering and the rationale comment placed at the call site.
- The exact velocity-x-Level combine form used (velToQ15 + combineVoiceVol via q15_mul_truncate on the unified 0x3FFF scale) and confirmation velocity-127 == Trigger-at-100%.
- Final test results: 8/8 voice_controls green, full suite green, rt_safety green.
</output>
