---
context: phase
phase: 07-pipeline-integration
task: 0
total_tasks: 1
status: paused
last_updated: 2026-04-29T22:16:01Z
---

<current_state>
Phase 7 (Pipeline Integration) is 2/3 plans complete. Plans 07-01 (state expansion + API) and 07-02 (mixer architecture rewrite) are done and committed. Plan 07-03 (integration tests) has NOT started — the executor was about to spawn when the user paused. Two test files exist as untracked files (`test_process_mixer.c`, `test_process_dac_integration.c`) — these may have been partially written by the 07-02 executor as deviation handling but are NOT committed or tested.

The full send/return mixer architecture is live in the C core. `spu94_process` implements a 7-stage signal flow: input gain → ADPCM → latency comp delay → reverb sends → reverb → master mixer → DAC section. All 22 setter/getter functions are implemented. JUCE wet/dry crossfade has been deleted and replaced with straight passthrough. All 94 existing tests pass.
</current_state>

<completed_work>
**Plan 07-01: State Struct + API + Init/Reset (2 tasks, 19min)**
- Task 1: Added seed parameter to `spu94_dac_noise_init` (WR-02 fix, commit 5284bf3)
- Task 2: Expanded `spu94_state` with mixer/DAC fields, declared 22 public API functions, fixed init/reset for noise seeds and latency_comp default (commit 55227b0)

**Plan 07-02: Mixer Architecture Rewrite (2 tasks, 54min)**
- Task 1: Implemented all 22 setter/getter functions in `spu94_io_chain.c` (commit 615adb6)
- Task 2: Rewrote `spu94_process` to 7-stage mixer architecture, updated JUCE passthrough, CLI defaults, golden/witness thresholds (commit 33c25a1)
- 4 auto-fixed deviations: Off-preset silence, ADPCM patina fader, Python self_test, golden thresholds
</completed_work>

<remaining_work>
**Plan 07-03: Integration Tests (1 task)**
- Task 1: Create 21+ integration tests covering mixer routing, DAC toggle hierarchy, latency compensation. Three test files + CMakeLists.txt updates.
- Note: `test_process_mixer.c` and `test_process_dac_integration.c` exist as untracked files — verify their contents before using them.
</remaining_work>

<decisions_made>
- Send/return mixer architecture: 3 buses (dry, patina, reverb), 2 reverb sends, master mixer, DAC section at end
- Q15 int16 for all fader/send values, no parameter smoothing in C core
- DAC section: master toggle + FIR sub-toggle + noise sub-toggle (all on = faithful PS1)
- Latency comp toggle (default ON) with 28-sample dry bus delay; OFF enables creative comb filtering
- Per-channel noise seeds (L=0xACE1u, R=0x1ECAu) for independent noise streams
- Off preset produces silence regardless of fader settings (mixer contract)
- Unity fader defaults for non-Off presets in CLI and JUCE
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — Current position and accumulated context
2. `.planning/phases/07-pipeline-integration/07-CONTEXT.md` — 12 locked decisions (D-01 through D-12)
3. `.planning/phases/07-pipeline-integration/07-03-PLAN.md` — The remaining plan to execute
4. `.planning/phases/07-pipeline-integration/07-02-SUMMARY.md` — What the mixer rewrite actually built (deviations noted)
5. `src/spu94/spu94_process.c` — The 7-stage mixer implementation (source of truth for signal flow)

<context>
v1.2 DAC Modeling milestone is progressing well. Phase 7 is the integration phase that wires the DAC modules (built in Phase 6) into the new mixer architecture (designed collaboratively with the user earlier in this session). The signal flow redesign was a significant scope expansion from the original ROADMAP — the original plan was a simple DAC toggle, but the user wanted a full send/return mixer with independent bus controls. This is now implemented and working.

The session included a long collaborative design discussion about signal flow, mixer architecture, parameter smoothing philosophy, and DAC component independence before any code was written. Key insight from the user: parameter smoothing removes digital character, which conflicts with the project's philosophy of exploiting digital artifacts musically. A "parameter slew control" was deferred to M4.
</context>

<next_action>
Start with: `/gsd-execute-phase 7` — this will detect plans 07-01 and 07-02 as complete (SUMMARY.md exists) and resume from plan 07-03 (integration tests). Alternatively, run the tests plan directly if you want to execute inline.
</next_action>
