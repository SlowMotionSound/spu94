---
phase: 05-interpolation-filter-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/dac_filter_design.py
  - plots/dac_interpolation_response.png
  - docs/DECISIONS.md
autonomous: true
requirements: [DAC-FILT-01, DAC-FILT-03]

must_haves:
  truths:
    - "Running `python3 tools/dac_filter_design.py --verify` prints ALL PASS and exits 0"
    - "Running `python3 tools/dac_filter_design.py --plot` generates a frequency response PNG"
    - "docs/DECISIONS.md contains ADR-0054 documenting the passband ripple gray area"
    - "The designed filter meets +/-0.05dB passband ripple, 41dB stopband attenuation, -0.2dB at 20kHz"
    - "Q15-quantized coefficients still meet all three datasheet specs"
  artifacts:
    - path: "tools/dac_filter_design.py"
      provides: "AK4309 interpolation filter design, verification, and coefficient export"
      contains: "ALL PASS"
    - path: "plots/dac_interpolation_response.png"
      provides: "Frequency response plot with datasheet spec reference lines"
    - path: "docs/DECISIONS.md"
      provides: "ADR-0054 passband ripple resolution"
      contains: "ADR-0054"
  key_links:
    - from: "tools/dac_filter_design.py"
      to: "plots/dac_interpolation_response.png"
      via: "--plot flag generates the PNG"
      pattern: "savefig.*dac_interpolation_response"
    - from: "tools/dac_filter_design.py"
      to: "Phase 6 C implementation"
      via: "C-array coefficient export printed to stdout"
      pattern: "int16_t.*stage"
---

<objective>
Design the AK4309's 8x digital interpolation filter as three cascaded 2x half-band FIR stages using scipy, with automated pass/fail verification against the AK4309B datasheet specs, a frequency response plot, and an ADR documenting the passband ripple gray area.

Purpose: This is the v1.2 DAC modeling entry point. The scipy prototype produces verified filter coefficients that Phase 6 will port to C as Q15 fixed-point. The ADR resolves the ambiguity between AK4309B datasheet specs and Stereophile's measured "audible ripple."

Output: Working design script (`tools/dac_filter_design.py`), frequency response plot (`plots/dac_interpolation_response.png`), ADR-0054 in `docs/DECISIONS.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-interpolation-filter-design/05-CONTEXT.md
@.planning/phases/05-interpolation-filter-design/05-RESEARCH.md

<interfaces>
<!-- Existing FIR reference pattern (tests/python/derive_fir_reference.py) -->
<!-- The new script follows a similar role: Python reference that Phase 6's C code must match -->

From tests/python/derive_fir_reference.py (pattern reference):
```python
# Q15 coefficient format: int16 hex values in arrays
SPU94_FIR_COEF = [
    -0x0001,  0x0000,  0x0002,  0x0000, -0x000A, ...
]
# Saturation helper
def sat_s16(x: int) -> int:
    if x > INT16_MAX: return INT16_MAX
    if x < INT16_MIN: return INT16_MIN
    return x
```

ADR format (from docs/DECISIONS.md):
```markdown
## ADR-NNNN: Title

**Status:** Accepted (date, phase)
**Context:** ...
**Decision:** ...
**Consequences:** ...
**Sources:** ...
```
ADRs are prepended at the top of docs/DECISIONS.md. Latest is ADR-0053.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create filter design script with automated verification</name>
  <files>tools/dac_filter_design.py</files>
  <read_first>
    - tests/python/derive_fir_reference.py (pattern: Q15 coefficient format, script structure)
    - .planning/phases/05-interpolation-filter-design/05-RESEARCH.md (verified code patterns, tap counts, design parameters)
  </read_first>
  <action>
Create `tools/dac_filter_design.py` with the following structure. Create the `tools/` directory if it does not exist.

**Script arguments:**
- `--verify` : Run pass/fail assertions, print results, exit 0 on pass / exit 1 on fail
- `--plot` : Generate frequency response PNG (requires matplotlib; script must handle ImportError gracefully and print a message directing user to install via venv if matplotlib is missing)
- `--export-c` : Print Q15 coefficient arrays in C `int16_t` format to stdout
- No args: print usage

**Design parameters (per D-01, D-03):**
- `FS_AUDIO = 44100`
- `F_PASS = 20000` (audio passband edge)
- Stage 1: 55-tap half-band at 88200 Hz (2x FS_AUDIO)
- Stage 2: 11-tap half-band at 176400 Hz (4x FS_AUDIO)
- Stage 3: 7-tap half-band at 352800 Hz (8x FS_AUDIO)
- All designed with `scipy.signal.remez`, equal weights `[1, 1]`

**Design function `design_halfband_stage(ntaps, fs_operating, f_passband)`:**
- Compute normalized passband edge: `f_passband / fs_operating`
- Compute normalized stopband edge: `(fs_operating / 2 - f_passband) / fs_operating`
- Call `scipy.signal.remez(ntaps, [0, f_pass_norm, f_stop_norm, 0.5], [1, 0], weight=[1, 1])`
- Enforce half-band zeros: set any coefficient with `abs(h[i]) < 1e-10` to exactly `0.0`
- Return coefficient array

**Composite cascade verification `verify_cascade(h1, h2, h3)`:**
- Upsample h1 by 4 (insert 3 zeros between each sample), h2 by 2 (insert 1 zero)
- Convolve all three upsampled responses into `h_composite`
- Compute frequency response with `scipy.signal.freqz(h_composite, worN=16384, fs=352800)`
- Normalize to 0 dB at DC
- Measure: passband peak-to-peak ripple over 0-20000 Hz, stopband attenuation above 22050 Hz, response at 20000 Hz
- Return dict with measurements and pass/fail booleans

**Q15 quantization verification (per RESEARCH.md Open Question 1):**
- Quantize each stage's coefficients to Q15: `round(h * 32768)`, clip to [-32768, 32767]
- Re-verify the composite cascade with quantized coefficients (convert back to float: `h_q15 / 32768.0`)
- Assert the quantized cascade still meets all three specs
- Print both float and Q15-quantized results

**Pass/fail assertions (per D-04, D-06):**
Three assertions, each printing PASS or FAIL with the measured value:
1. Passband ripple (peak-to-peak) <= 0.10 dB (i.e., +/-0.05 dB)
2. Stopband attenuation >= 41.0 dB
3. Response at 20 kHz >= -0.2 dB

Print individual stage results (tap count, ripple, attenuation, non-zero coefficient count).
Print composite results (float coefficients).
Print composite results (Q15 quantized coefficients).
Final line: `"ALL PASS"` if all 6 assertions pass (3 float + 3 quantized), or `"FAIL"` with details.
Exit code 0 on all pass, 1 on any failure.

**C-array export function `export_c_arrays(h1, h2, h3)`:**
- Quantize to Q15 as above
- Print each stage as `static const int16_t dac_interp_stageN[NN]` with hex values (0xNNNN format), 8 values per line
- Include a comment header with tap count and "half-band FIR, Q15"

**Script must NOT import matplotlib at module level.** Only import inside the `--plot` handler, wrapped in try/except ImportError.

Script shebang: `#!/usr/bin/env python3`
Script docstring: Reference DAC-FILT-01, phase 5, AK4309B datasheet specs.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && python3 tools/dac_filter_design.py --verify</automated>
  </verify>
  <acceptance_criteria>
    - tools/dac_filter_design.py exists and is executable (has shebang)
    - `python3 tools/dac_filter_design.py --verify` exits 0
    - stdout contains "ALL PASS"
    - stdout contains "Stage 1" with "N=55"
    - stdout contains "Stage 2" with "N=11"
    - stdout contains "Stage 3" with "N=7"
    - stdout contains "Q15 quantized" verification results
    - stdout contains passband ripple value less than 0.10
    - stdout contains stopband attenuation value greater than 41.0
    - stdout contains @20kHz value greater than -0.2
    - `python3 tools/dac_filter_design.py --export-c` prints `static const int16_t dac_interp_stage1[55]`
    - `python3 tools/dac_filter_design.py --export-c` prints `static const int16_t dac_interp_stage2[11]`
    - `python3 tools/dac_filter_design.py --export-c` prints `static const int16_t dac_interp_stage3[7]`
    - `grep -c "scipy.signal" tools/dac_filter_design.py` returns at least 2
    - `grep -c "remez" tools/dac_filter_design.py` returns at least 1
    - `grep -c "matplotlib" tools/dac_filter_design.py` returns at least 1 (conditional import)
    - `grep -v "^#" tools/dac_filter_design.py | grep -c "import matplotlib"` equals 0 (no top-level matplotlib import)
  </acceptance_criteria>
  <done>Script runs `--verify` with ALL PASS, designs 55+11+7 tap cascade, verifies both float and Q15 specs, exports C arrays</done>
</task>

<task type="auto">
  <name>Task 2: Generate frequency response plot with datasheet spec lines</name>
  <files>plots/dac_interpolation_response.png</files>
  <read_first>
    - tools/dac_filter_design.py (just created in Task 1 -- read to understand the plot hook point)
    - .planning/phases/05-interpolation-filter-design/05-RESEARCH.md (Pitfall 5: matplotlib venv setup)
  </read_first>
  <action>
**Step 1: Install matplotlib in a venv.**

Create a venv at `.venv` in the project root (if it does not already exist) and install matplotlib:
```
python3 -m venv .venv
.venv/bin/pip install matplotlib
```

**Step 2: Create the `plots/` directory** if it does not exist.

**Step 3: Run the plot command** using the venv Python so matplotlib is available:
```
.venv/bin/python3 tools/dac_filter_design.py --plot
```

The `--plot` handler in `dac_filter_design.py` (created in Task 1) must produce a frequency response plot saved to `plots/dac_interpolation_response.png` with these elements (per D-05):

- X-axis: frequency 0 to 40000 Hz (log scale or linear, designer's choice -- linear is fine for showing the transition band clearly)
- Y-axis: magnitude in dB, normalized to 0 dB at DC
- Blue line: composite cascade frequency response (all three stages convolved)
- Red dashed horizontal line at +0.05 dB (upper passband limit)
- Red dashed horizontal line at -0.05 dB (lower passband limit)
- Green dashed horizontal line at -41 dB (stopband attenuation limit)
- Orange marker or vertical line at 20 kHz with annotation showing the measured dB value
- Vertical dashed line at 22050 Hz (original Nyquist / passband-stopband boundary)
- Title: "AK4309 8x Interpolation Filter -- Composite Response (55+11+7 taps)"
- Legend identifying the spec limit lines
- Grid enabled
- Save to `plots/dac_interpolation_response.png` at 150 DPI minimum

If the `--plot` handler is not yet implemented in Task 1's script (because Task 1 focused on verify/export), add it now. The plot code goes inside a function guarded by `try: import matplotlib.pyplot as plt except ImportError: print("matplotlib not found..."); sys.exit(1)`.

No Stereophile or Archimago overlay (per D-02).
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && .venv/bin/python3 tools/dac_filter_design.py --plot && test -f plots/dac_interpolation_response.png && python3 -c "import os; s=os.path.getsize('plots/dac_interpolation_response.png'); assert s > 10000, f'PNG too small: {s} bytes'; print(f'Plot OK: {s} bytes')"</automated>
  </verify>
  <acceptance_criteria>
    - plots/dac_interpolation_response.png exists
    - File size is greater than 10000 bytes (not a trivial/empty image)
    - .venv/bin/python3 exists (venv was created)
    - `grep -c "savefig" tools/dac_filter_design.py` returns at least 1
    - `grep -c "dac_interpolation_response" tools/dac_filter_design.py` returns at least 1
    - `grep -c "0.05" tools/dac_filter_design.py` returns at least 2 (spec lines)
    - `grep -c "41" tools/dac_filter_design.py` returns at least 2 (stopband spec line)
  </acceptance_criteria>
  <done>Frequency response PNG exists in plots/ showing composite response with datasheet spec reference lines, no Stereophile overlay</done>
</task>

<task type="auto">
  <name>Task 3: Write ADR-0054 documenting passband ripple gray area</name>
  <files>docs/DECISIONS.md</files>
  <read_first>
    - docs/DECISIONS.md (read first 30 lines to see prepend pattern and ADR-0053 as latest)
    - .planning/phases/05-interpolation-filter-design/05-CONTEXT.md (D-07, D-08, D-09 lock the ADR stance)
    - .planning/phases/05-interpolation-filter-design/05-RESEARCH.md (D-13 investigation findings, assumption A1)
  </read_first>
  <action>
Prepend ADR-0054 at the top of docs/DECISIONS.md (after the `---` separator that follows the discipline section, before ADR-0053). Use the exact ADR format established in the file.

**ADR content (per D-07, D-08, D-09, D-12, D-13):**

```markdown
## ADR-0054: AK4309 interpolation filter passband ripple -- datasheet is authoritative

**Status:** Accepted (2026-04-28, v1.2 Phase 5)

**Context:**

The AK4309B datasheet specifies +/-0.05 dB passband ripple for the digital interpolation filter. Stereophile's 1995 PS1 review measured "ripple in the top three octaves" which they attributed to an "underspecified digital filter." This raised a question: does the AK4309's digital filter have more ripple than the datasheet claims, or was Stereophile measuring the composite output of the entire analog chain (digital filter + switched-capacitor filter + continuous-time filter + op-amps + cabling)?

The AK4309 output path is: 8x FIR interpolator -> switched-capacitor filter (SCF) -> continuous-time filter (CTF) -> analog output. The datasheet's +/-0.05 dB spec applies to the digital FIR stage. The SCF and CTF contribute their own passband ripple and rolloff. The -0.2 dB at 20 kHz composite spec covers all stages combined.

Investigation per D-13: no AKM-specific documentation (application notes, AK43xx family datasheets, academic papers) was found confirming the internal architecture of the AK4309's digital interpolation filter. The cascaded half-band architecture used in this implementation is an era-typical engineering assumption supported by universal industry practice for 8x interpolation in 1990s consumer audio silicon, the AK4309's cost-optimized positioning (80mW, 20-pin SSOP), and the implausibility of a single-stage 8x FIR (would require 400+ taps) in mid-90s gate budgets.

**Decision:**

The AK4309B datasheet is authoritative for the digital interpolation filter's passband ripple specification (+/-0.05 dB). Stereophile's measured ripple is attributed to the composite analog output chain, not the digital filter alone. The SPU-94 DAC model reproduces the digital interpolation stage only and makes no claim about reproducing the full PS1 analog output "sound."

The filter is implemented as three cascaded 2x half-band FIR stages (55+11+7 taps = 73 total, 38 non-zero coefficients) designed with the Parks-McClellan (remez) algorithm. The composite cascade achieves approximately 0.078 dB peak-to-peak passband ripple, 53.6 dB stopband attenuation, and -0.016 dB at 20 kHz -- all within datasheet limits with margin.

**Confidence:**
- Digital filter specs (passband ripple, stopband attenuation): HIGH -- the datasheet is explicit and the design meets it
- Cascaded half-band architecture: MEDIUM-HIGH -- industry-standard assumption, not confirmed by AKM documentation
- Full-chain PS1 output reproduction: LOW -- the analog stages (SCF, CTF, op-amps) are not modeled in v1.2

**Consequences:**

The v1.2 DAC model adds the digital interpolation filter's passband ripple character as a toggleable coloration. Users should understand this reproduces the DAC's digital conversion artifacts, not the full PS1 output chain. Hardware calibration (deferred to M5 / DAC-HW-01 through DAC-HW-03) may later refine the model against captures from real PS1 hardware, which would include the analog stages.

The Q15 fixed-point port in Phase 6 must verify that coefficient quantization does not push the composite ripple outside the +/-0.05 dB datasheet spec. The scipy design script verifies this for the designed coefficients; Phase 6 must re-verify after any implementation-specific coefficient adjustments.

**Sources:**
- AK4309B datasheet (AllDatasheet): +/-0.05 dB passband ripple, 41 dB stopband attenuation, -0.2 dB at 20 kHz, "8 times FIR Interpolator"
- Stereophile PS1 review (1995): "ripple in the top three octaves" -- composite measurement, not isolated digital filter
- DSPRelated (Neil Robertson): cascaded half-band as standard 8x interpolation architecture
- Analog Devices MT-017 (Walt Kester): cascaded interpolation as standard for oversampling DACs
- Phase 5 scipy design exploration: empirical verification of 55+11+7 minimum-order cascade
```

The ADR must be inserted AFTER the `---` separator line and BEFORE the existing `## ADR-0053` entry. Add a blank line between the new ADR and ADR-0053.
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && grep -c "ADR-0054" docs/DECISIONS.md && grep -c "datasheet is authoritative" docs/DECISIONS.md && grep -n "ADR-0054" docs/DECISIONS.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ADR-0054" docs/DECISIONS.md` returns at least 1
    - `grep "ADR-0054" docs/DECISIONS.md` contains "passband ripple"
    - `grep -c "Stereophile" docs/DECISIONS.md` returns at least 2 (existing refs + new ADR)
    - `grep -c "MEDIUM-HIGH" docs/DECISIONS.md` returns at least 1 (confidence for cascaded half-band)
    - `grep -c "Accepted (2026-04-28, v1.2 Phase 5)" docs/DECISIONS.md` returns 1
    - ADR-0054 appears on a lower line number than ADR-0053 (prepended, not appended)
    - The ADR contains "55+11+7" referencing the actual designed cascade
    - The ADR contains "LOW" confidence for full-chain reproduction (per D-09)
  </acceptance_criteria>
  <done>ADR-0054 prepended to docs/DECISIONS.md with passband ripple gray area resolution per D-07/D-08/D-09, D-13 investigation findings documented, confidence levels stated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 5 is an offline Python design tool -- no user input, no network, no runtime code. It produces static coefficients consumed by Phase 6. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | tools/dac_filter_design.py coefficients | accept | Coefficients are verified by automated pass/fail assertions (D-04, D-06). Any coefficient change that violates specs causes --verify to fail. Phase 6 re-verifies after Q15 port. |
| T-05-02 | Information Disclosure | N/A | accept | No secrets, credentials, or user data involved. All outputs are public design artifacts. |
</threat_model>

<verification>
1. `python3 tools/dac_filter_design.py --verify` exits 0 and prints "ALL PASS"
2. `python3 tools/dac_filter_design.py --export-c` prints three `int16_t` arrays
3. `.venv/bin/python3 tools/dac_filter_design.py --plot` generates plots/dac_interpolation_response.png
4. `grep "ADR-0054" docs/DECISIONS.md` finds the passband ripple ADR
5. All three datasheet specs verified for both float and Q15 coefficients
</verification>

<success_criteria>
- The AK4309 interpolation filter is fully characterized as a scipy prototype
- Automated pass/fail verification confirms all three datasheet specs (float + Q15)
- Frequency response plot with spec reference lines exists for documentation
- ADR-0054 resolves the passband ripple gray area with stated confidence levels
- C-array coefficient export is ready for Phase 6 consumption
</success_criteria>

<output>
After completion, create `.planning/phases/05-interpolation-filter-design/05-01-SUMMARY.md`
</output>
