---
phase: 09-verification-documentation
plan: 03
type: execute
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - docs/COVERAGE.md
autonomous: true
requirements:
  - DAC-TEST-04

must_haves:
  truths:
    - "COVERAGE.md has a DAC Model Coverage section with rows for every DAC behavior"
    - "COVERAGE.md has an ADPCM Coverage section backfilling existing v1.1 tests"
    - "Every row in both new sections has a valid test: cell referencing an existing test"
    - "check_coverage.py validates all new rows"
  artifacts:
    - path: "docs/COVERAGE.md"
      provides: "Complete coverage map for all coloration stages"
      contains: "## DAC Model Coverage"
  key_links:
    - from: "docs/COVERAGE.md"
      to: "scripts/ci/check_coverage.py"
      via: "CI enforcement validates test: cells"
      pattern: "check_coverage"
---

<objective>
Update COVERAGE.md with DAC Model Coverage and ADPCM Coverage sections per D-11 and D-12.

Purpose: Make COVERAGE.md the single source of truth for all coloration stage test mappings. The DAC section maps every DAC requirement to its test. The ADPCM backfill maps existing v1.1 tests that were never entered.

Output: Two new sections appended to `docs/COVERAGE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-verification-documentation/09-CONTEXT.md
@.planning/phases/09-verification-documentation/09-01-SUMMARY.md
@.planning/phases/09-verification-documentation/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DAC Model Coverage and ADPCM Coverage sections to COVERAGE.md</name>
  <files>docs/COVERAGE.md</files>
  <read_first>
    - docs/COVERAGE.md (current state — must see existing format, table structure, Known Gaps section)
    - tests/unit/dac_fir/CMakeLists.txt (ctest names for DAC FIR tests)
    - tests/unit/dac_noise/CMakeLists.txt (ctest names for DAC noise tests)
    - tests/unit/process/CMakeLists.txt (ctest names for DAC integration + toggle transition tests)
    - tests/unit/adpcm/CMakeLists.txt (ctest names for ADPCM tests)
    - tests/python/binding/test_binding_adpcm.py (ADPCM Python test for backfill)
    - tests/python/binding/test_binding_mixer_dac.py (DAC Python binding test)
    - tests/conformance/test_goldens_present.py (golden conformance ctest name)
    - scripts/ci/check_coverage.py (validation format requirements)
    - .planning/phases/09-verification-documentation/09-CONTEXT.md (D-11, D-12)
  </read_first>
  <action>
Add two new sections to COVERAGE.md, inserted AFTER the existing "## Per-Behavior Coverage" table and BEFORE the "## Per-Spec-Paragraph Coverage" section.

**Section 1: `## ADPCM Coverage`** (D-12 backfill)

Introductory paragraph: "Rows map the v1.1 ADPCM codec behaviors to their existing tests. Backfilled in Phase 9 for completeness — no new tests created."

Table columns: `| Behavior | Spec reference | test: | Notes |`

Rows (map existing tests — read the actual ctest names from the CMakeLists.txt files):

| Behavior | Spec reference | test: | Notes |
|----------|----------------|-------|-------|
| ADPCM 4-bit decode | Sony SPU ADPCM | `tests/unit/adpcm/test_adpcm_decode.c::adpcm_decode` | All 5 filter pairs |
| ADPCM 4-bit encode | Sony SPU ADPCM | `tests/unit/adpcm/test_adpcm_encode.c::adpcm_encode` | Round-trip encode/decode |
| ADPCM Python binding | N/A (API) | `tests/python/binding/test_binding_adpcm.py::test_binding_adpcm` | ctypes toggle |
| ADPCM golden corpus (30 WAVs) | N/A (regression) | `tests/conformance/test_goldens_present.py::goldens_present` | 10 presets x 3 inputs |
| ADPCM + reverb interaction | N/A (integration) | `tests/unit/process/test_process_adpcm.c::test_process_adpcm` | ADPCM in mixer chain |

Note: read the actual ctest registration names from each CMakeLists.txt. The left side of `::` is the source file path, the right side is the ctest name. Use the exact names found in the CMakeLists.txt files. If the ctest name for adpcm_decode is different (e.g., `test_adpcm_decode`), use that.

**Section 2: `## DAC Model Coverage`** (D-11)

Introductory paragraph: "Rows map the v1.2 DAC model behaviors to their tests. Added Phase 9."

Table columns: `| Behavior | Spec reference | test: | Notes |`

Rows:

| Behavior | Spec reference | test: | Notes |
|----------|----------------|-------|-------|
| FIR coefficient correctness | AK4309B datasheet | `tests/unit/dac_fir/test_dac_fir_coef_table.c::dac_fir_coef_table` | 55+11+7 tap values |
| FIR DC gain unity | AK4309B datasheet | `tests/unit/dac_fir/test_dac_fir_dc_gain.c::dac_fir_dc_gain` | Sum of Q15 coefficients |
| FIR impulse response | AK4309B datasheet | `tests/unit/dac_fir/test_dac_fir_impulse.c::dac_fir_impulse` | Cascade output shape |
| FIR overflow proof | N/A (safety) | `tests/unit/dac_fir/test_dac_fir_overflow_proof.c::dac_fir_overflow_proof` | int32 accumulator headroom |
| FIR frequency response | AK4309B datasheet | `tools/dac_measure.py::dac_measure` | Passband ripple <=0.078dB |
| Noise LFSR sequence | AK4309B 1-bit DSM | `tests/unit/dac_noise/test_dac_noise_lfsr.c::dac_noise_lfsr` | Galois LFSR period |
| Noise amplitude calibration | AK4309B ~90dB DR | `tests/unit/dac_noise/test_dac_noise_amplitude.c::dac_noise_amplitude` | DAC_NOISE_SHIFT=14 |
| Noise spectral slope (+12dB/oct) | AK4309B 2nd-order | `tests/unit/dac_noise/test_dac_noise_spectral.c::dac_noise_spectral` | HP shaping verification |
| Noise spectral (measured) | AK4309B 2nd-order | `tools/dac_measure.py::dac_measure` | Full-pipeline slope check |
| DAC toggle transitions | N/A (integration) | `tests/unit/process/test_process_dac_toggle_transitions.c::test_process_dac_toggle_transitions` | on/off/on bit-identity |
| DAC + reverb interaction | N/A (integration) | `tests/unit/process/test_process_dac_toggle_transitions.c::test_process_dac_toggle_transitions` | Output differs with DAC on |
| DAC + ADPCM composition | N/A (integration) | `tests/unit/process/test_process_dac_toggle_transitions.c::test_process_dac_toggle_transitions` | Both stages compose |
| DAC master gate / sub-toggles | N/A (integration) | `tests/unit/process/test_process_dac_integration.c::test_process_dac_integration` | Master off bypasses all |
| DAC state reset on disable | N/A (integration) | `tests/unit/process/test_process_dac_integration.c::test_process_dac_integration` | FIR zeroed, noise reseeded |
| DAC L/R noise decorrelation | WR-02 | `tests/unit/process/test_process_dac_integration.c::test_process_dac_integration` | Different LFSR seeds |
| DAC Python binding | N/A (API) | `tests/python/binding/test_binding_mixer_dac.py::test_binding_mixer_dac` | ctypes DAC toggle |
| DAC golden regression (55 WAVs) | N/A (regression) | `tests/conformance/test_goldens_present.py::goldens_present` | 50 full-pipeline + 5 isolated |

Note: verify the exact ctest registration names by reading each CMakeLists.txt. Use the actual ctest NAME value, not the executable name. For Python tests, the ctest name may differ — check `tests/python/binding/CMakeLists.txt` and `tests/conformance/CMakeLists.txt`.

After adding both sections, verify no existing content was displaced. The file order should be:
1. Header + intro
2. Per-Register Coverage
3. Per-Behavior Coverage
4. ADPCM Coverage (NEW)
5. DAC Model Coverage (NEW)
6. Per-Spec-Paragraph Coverage
7. Known Gaps
  </action>
  <verify>
    <automated>cd "/home/ubuntu-studio/Desktop/PSX Reverb" && grep -c "## ADPCM Coverage" docs/COVERAGE.md && grep -c "## DAC Model Coverage" docs/COVERAGE.md && python3 scripts/ci/check_coverage.py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/COVERAGE.md contains `## ADPCM Coverage`
    - docs/COVERAGE.md contains `## DAC Model Coverage`
    - docs/COVERAGE.md contains `FIR coefficient correctness`
    - docs/COVERAGE.md contains `Noise spectral slope`
    - docs/COVERAGE.md contains `DAC toggle transitions`
    - docs/COVERAGE.md contains `DAC golden regression`
    - docs/COVERAGE.md contains `ADPCM 4-bit decode`
    - docs/COVERAGE.md contains `ADPCM golden corpus`
    - `## ADPCM Coverage` appears BEFORE `## DAC Model Coverage` in the file
    - `## DAC Model Coverage` appears BEFORE `## Per-Spec-Paragraph Coverage` in the file
    - `python3 scripts/ci/check_coverage.py` exits 0
  </acceptance_criteria>
  <done>COVERAGE.md has both ADPCM and DAC Model sections with valid test mappings. CI check_coverage.py passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Documentation-only plan, no runtime boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | R (Repudiation) | Coverage map accuracy | mitigate | check_coverage.py CI script validates every test: cell references an existing, passing test |
</threat_model>

<verification>
- `grep "## ADPCM Coverage" docs/COVERAGE.md` finds the section
- `grep "## DAC Model Coverage" docs/COVERAGE.md` finds the section
- `python3 scripts/ci/check_coverage.py` exits 0
- ADPCM section has at least 5 rows
- DAC Model section has at least 15 rows
</verification>

<success_criteria>
COVERAGE.md is the single source of truth for all coloration stage test mappings. Both ADPCM (backfill) and DAC Model sections have valid test: cells that pass CI enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-documentation/09-03-SUMMARY.md`
</output>
