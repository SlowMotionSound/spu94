---
phase: 04-verification-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/regenerate_goldens.py
  - tests/conformance/test_goldens_present.py
  - tests/conformance/CMakeLists.txt
autonomous: true
requirements: [ADPCM-TEST-03]

must_haves:
  truths:
    - "ADPCM golden files exist for all 10 presets x 3 inputs (impulse, sine_1khz, chirp) = 30 WAV + 30 SHA-256 sidecars"
    - "Golden files live in tests/golden/<preset>/adpcm/ subdirectories"
    - "regenerate_goldens.py --adpcm regenerates all 30 ADPCM goldens"
    - "regenerate_goldens.py --check-adpcm verifies sidecars against re-rendered output"
    - "ctest goldens_present detects missing ADPCM golden files"
  artifacts:
    - path: "scripts/regenerate_goldens.py"
      provides: "ADPCM golden generation with --adpcm and --check-adpcm flags"
      contains: "--adpcm"
    - path: "tests/conformance/test_goldens_present.py"
      provides: "Updated structural test covering 80 WAV + 80 SHA-256 (50 reverb + 30 ADPCM)"
      contains: "adpcm"
    - path: "tests/golden/hall/adpcm/impulse.wav"
      provides: "Example ADPCM golden file"
    - path: "tests/golden/hall/adpcm/impulse.wav.sha256"
      provides: "Example ADPCM golden sidecar"
  key_links:
    - from: "scripts/regenerate_goldens.py"
      to: "tests/golden/<preset>/adpcm/"
      via: "--adpcm flag renders with ADPCM enabled"
      pattern: "adpcm"
    - from: "tests/conformance/test_goldens_present.py"
      to: "tests/golden/<preset>/adpcm/"
      via: "parametrized presence assertions"
      pattern: "adpcm"
---

<objective>
Create ADPCM golden files for all 10 presets with 3 inputs each, extend regenerate_goldens.py with --adpcm support including a new chirp input, and update the conformance test to cover the expanded corpus.

Purpose: Per D-03/D-04/D-05, ADPCM golden files provide regression detection -- if the codec or pipeline changes, SHA-256 mismatches catch it immediately.
Output: 30 ADPCM golden WAV files + 30 SHA-256 sidecars, updated regeneration script, updated conformance test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-verification-documentation/04-CONTEXT.md

<interfaces>
From scripts/regenerate_goldens.py:
- PRESETS = ["off", "room", "studio_a", "studio_b", "studio_c", "hall", "half_echo", "space_echo", "echo", "delay"]
- INPUTS = ["impulse", "white_noise", "sine_1khz", "silence", "sweep"]
- FS = 44100, DURATION_SEC = 2.0, AMP = 16000
- generate_input(name) -> np.ndarray (N,2) int16
- render_golden(preset, input_name, out_path, tmp_in_path, spu94_bin)
- sha256_of(path) -> str
- main() with --check mode

From tests/conformance/test_goldens_present.py:
- PRESETS, INPUTS lists (mirrored from regenerate_goldens.py)
- test_wav_exists, test_sidecar_exists_and_format, test_sidecar_matches_wav, test_expected_count
- GOLDEN_ROOT = repo/tests/golden/

CLI flag: spu94 --adpcm --preset <preset> <input.wav> <output.wav>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend regenerate_goldens.py with ADPCM golden generation</name>
  <files>scripts/regenerate_goldens.py</files>
  <read_first>
    - scripts/regenerate_goldens.py
    - .planning/phases/04-verification-documentation/04-CONTEXT.md (D-03, D-04, D-05)
  </read_first>
  <action>
Per D-03/D-04/D-05, extend regenerate_goldens.py to generate ADPCM golden files:

1. Add a new ADPCM_INPUTS list: `["impulse", "sine_1khz", "chirp"]` (per D-04, 3 inputs not 5).

2. Add chirp generation to generate_input(). The chirp input (per D-05) is a 20Hz-20kHz logarithmic sweep, same sample rate (44100), same duration (2.0s), same amplitude (16000) as existing inputs. The existing sweep input already generates this exact signal -- BUT verify the existing sweep uses the same parameters. If it does, chirp can reuse the same generation logic. If not, add a "chirp" case with:
   ```python
   if name == "chirp":
       t = np.arange(N, dtype=np.float64) / FS
       y = (AMP * chirp(t, 20.0, DURATION_SEC, 20000.0, method="logarithmic")).astype(np.int16)
       return np.stack([y, y], axis=1)
   ```

   IMPORTANT: The existing "sweep" input IS a 20Hz-20kHz logarithmic chirp with the same parameters. Per D-05 the chirp is a "new test signal" -- but it has the SAME generation formula as sweep. Resolution: name the ADPCM input "chirp" but generate it identically to the existing "sweep". This means ADPCM-on + sweep and ADPCM-off + sweep produce a useful before/after comparison since the underlying input is the same waveform.

3. Add a render_adpcm_golden() function (or modify render_golden to accept an --adpcm flag). The function must:
   - Generate the input WAV
   - Run: `spu94 --adpcm --preset <preset> <input.wav> <output.wav>`
   - Write output to `tests/golden/<preset>/adpcm/<input_name>.wav`
   - Generate SHA-256 sidecar at `tests/golden/<preset>/adpcm/<input_name>.wav.sha256`

4. Add `--adpcm` CLI flag to the argparse:
   - `--adpcm` (no --check): regenerate all 30 ADPCM goldens + sidecars
   - `--check-adpcm`: verify committed ADPCM sidecars by re-rendering and comparing SHA-256

5. Keep existing behavior unchanged: `python scripts/regenerate_goldens.py` (no flags) still regenerates the original 50 reverb-only goldens. `--check` still verifies the original 50.

6. Combined mode: `python scripts/regenerate_goldens.py --adpcm` generates only ADPCM goldens. Running both `--adpcm` and without generates all 80.

7. Use same determinism discipline: LC_ALL=C, TZ=UTC, SOURCE_DATE_EPOCH=1704067200.

8. After updating the script, RUN IT to generate the actual golden files:
   ```bash
   cd /home/ubuntu-studio/Desktop/PSX\ Reverb
   python scripts/regenerate_goldens.py --adpcm
   ```
   This creates all 30 ADPCM golden files + 30 sidecars.

9. Verify with --check-adpcm:
   ```bash
   python scripts/regenerate_goldens.py --check-adpcm
   ```
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb && python scripts/regenerate_goldens.py --check-adpcm 2>&1 | tail -5 && find tests/golden -path "*/adpcm/*.wav" | wc -l && find tests/golden -path "*/adpcm/*.wav.sha256" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - find tests/golden -path "*/adpcm/*.wav" | wc -l returns 30
    - find tests/golden -path "*/adpcm/*.wav.sha256" | wc -l returns 30
    - python scripts/regenerate_goldens.py --check-adpcm exits 0
    - python scripts/regenerate_goldens.py --check exits 0 (existing goldens unchanged)
    - grep -c "adpcm" scripts/regenerate_goldens.py returns at least 5
    - Each of the 10 preset dirs has an adpcm/ subdirectory with 3 .wav + 3 .sha256 files
  </acceptance_criteria>
  <done>30 ADPCM golden WAV files + 30 SHA-256 sidecars exist under tests/golden/<preset>/adpcm/. Script supports --adpcm and --check-adpcm modes. Original 50 goldens unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update conformance test for ADPCM goldens + add ctest regression gate</name>
  <files>tests/conformance/test_goldens_present.py, tests/conformance/CMakeLists.txt</files>
  <read_first>
    - tests/conformance/test_goldens_present.py
    - tests/conformance/CMakeLists.txt
  </read_first>
  <action>
Update test_goldens_present.py to also verify the 30 ADPCM golden files:

1. Add ADPCM_INPUTS list: `["impulse", "sine_1khz", "chirp"]`

2. Add new parametrized tests mirroring the existing reverb tests:
   - `test_adpcm_wav_exists(preset, input_name)`: asserts `tests/golden/<preset>/adpcm/<input_name>.wav` exists
   - `test_adpcm_sidecar_exists_and_format(preset, input_name)`: asserts sidecar exists and is sha256sum-formatted
   - `test_adpcm_sidecar_matches_wav(preset, input_name)`: spot-check 3 pairs (hall/impulse, room/sine_1khz, studio_a/chirp)

3. Update `test_expected_count()`:
   - Change from 50 .wav + 50 .sha256 to 80 .wav + 80 .sha256
   - Use `ROOT.glob("**/*.wav")` and `ROOT.glob("**/*.wav.sha256")` to pick up files in subdirectories (the existing `*/*.wav` glob won't find `*/adpcm/*.wav`)

4. Add a ctest registration for the ADPCM golden regression gate in CMakeLists.txt:
   ```cmake
   # Phase 4 Plan 02: ADPCM golden regression gate
   add_test(
       NAME adpcm_goldens_regression
       COMMAND ${Python3_EXECUTABLE}
           ${CMAKE_SOURCE_DIR}/scripts/regenerate_goldens.py
           --check-adpcm
       WORKING_DIRECTORY ${CMAKE_SOURCE_DIR}
   )
   set_tests_properties(adpcm_goldens_regression PROPERTIES
       LABELS "golden;adpcm"
       TIMEOUT 300
   )
   ```
   This ctest re-renders all 30 ADPCM goldens and checks SHA-256 sidecars. If anything drifts, this test fails.
  </action>
  <verify>
    <automated>cd /home/ubuntu-studio/Desktop/PSX\ Reverb && cmake --build build 2>&1 | tail -3 && cd build && ctest -R goldens_present -V 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "adpcm" tests/conformance/test_goldens_present.py returns at least 5
    - grep -c "80" tests/conformance/test_goldens_present.py returns at least 1 (updated count from 50)
    - grep "adpcm_goldens_regression" tests/conformance/CMakeLists.txt returns 1
    - ctest -R goldens_present passes
    - The glob pattern in test_expected_count uses ** to find files in subdirectories
  </acceptance_criteria>
  <done>Conformance test verifies 80 WAV + 80 SHA-256 files (50 reverb + 30 ADPCM). ctest adpcm_goldens_regression runs SHA-256 regression check on ADPCM goldens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI invocation | regenerate_goldens.py shells out to spu94 CLI binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-T | Tampering | golden files | mitigate | SHA-256 sidecars detect any modification; --check-adpcm ctest gate catches drift |
| T-04-02-E | Elevation | regenerate_goldens.py subprocess | accept | Closed allowlist for preset/input names prevents path traversal (existing T-07-02-D mitigation carries forward) |
</threat_model>

<verification>
- 30 ADPCM golden .wav files exist: `find tests/golden -path "*/adpcm/*.wav" | wc -l` = 30
- 30 ADPCM golden .sha256 sidecars exist: `find tests/golden -path "*/adpcm/*.wav.sha256" | wc -l` = 30
- `python scripts/regenerate_goldens.py --check-adpcm` exits 0
- `python scripts/regenerate_goldens.py --check` exits 0 (originals unchanged)
- `ctest -R goldens_present` passes
- `ctest -R adpcm_goldens_regression` passes
</verification>

<success_criteria>
- ADPCM-TEST-03 satisfied: golden files for all 10 presets x 3 inputs, SHA-256 sidecars, ctest regression gate
- Original 50 reverb goldens unmodified
- Chirp test signal generated programmatically (per D-05)
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-documentation/04-02-SUMMARY.md`
</output>
