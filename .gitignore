/build/
/build-*/
*.o
*.obj
*.a
*.so
*.dylib
*.dll
compile_commands.json
.cache/
.vscode/
.idea/
__pycache__/
*.pyc

# Phase 6: Python packaging artifacts
dist/
*.egg-info/
.pytest_cache/
# scikit-build-core writes a build dir distinct from `build/`; cover both
# to prevent accidental commits.
_skbuild/
wheelhouse/

# Local testing conveniences — desktop drag-drop launcher scripts.
# Used during M1 for easy WAV rendering from Finder; superseded by
# the M4 plugin UI. Not a shipped deliverable.
/scripts/desktop-launcher/

# Phase 7: pytest-benchmark output directory (Plan 07-05 / 07-06 also writes
# here; ignoring pre-emptively per Phase 7 Pitfall 4 so ad-hoc benchmark
# invocations from the dev tree never pollute the index).
.benchmarks/
